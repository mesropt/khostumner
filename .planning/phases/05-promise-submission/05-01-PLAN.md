---
phase: 05-promise-submission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/20260524_000001_add_promise_edits.py
  - backend/app/models/promise_edits.py
  - backend/app/schemas/promises.py
  - backend/requirements.txt
  - backend/tests/test_promises.py
  - frontend/package.json
autonomous: true
requirements:
  - PROM-03
  - PROM-04
  - ELEC-03

must_haves:
  truths:
    - "promise_edits and promise_edit_election_links tables exist in the DB after migration upgrade"
    - "PromiseEdit and PromiseEditElectionLink ORM models are importable from app.models.promise_edits"
    - "PromiseCreateIn, PromiseEditIn, PromiseEditOut schemas are importable from app.schemas.promises"
    - "All PROM-03, PROM-04, ELEC-03 test stubs exist in test_promises.py and fail (RED state)"
    - "python-slugify==8.0.4 is listed in requirements.txt"
    - "Radix UI + cmdk packages are listed in frontend/package.json"
  artifacts:
    - path: "backend/alembic/versions/20260524_000001_add_promise_edits.py"
      provides: "Migration creating promise_edits and promise_edit_election_links tables"
      contains: "create_table.*promise_edits"
    - path: "backend/app/models/promise_edits.py"
      provides: "PromiseEdit and PromiseEditElectionLink ORM models"
      exports: ["PromiseEdit", "PromiseEditElectionLink"]
    - path: "backend/app/schemas/promises.py"
      provides: "Input/output Pydantic schemas for promise submission"
      exports: ["PromiseCreateIn", "PromiseOut", "PromiseEditIn", "PromiseEditOut"]
    - path: "backend/tests/test_promises.py"
      provides: "RED-state test stubs for Phase 5 requirements"
      contains: "test_create_promise"
  key_links:
    - from: "backend/app/models/promise_edits.py"
      to: "backend/app/models/promises.py"
      via: "ModerationStatus import"
      pattern: "from app.models.promises import ModerationStatus"
    - from: "backend/alembic/versions/20260524_000001_add_promise_edits.py"
      to: "backend/alembic/versions/20260523_000001_auth_schema.py"
      via: "down_revision chain"
      pattern: "down_revision.*20260523_000001"
---

## Phase Goal

**As a** registered and verified user, **I want to** submit new promises and edits with election links, **so that** politicians' promises enter the admin review queue with a full audit trail.

<objective>
Wave 0 blocking scaffold: Alembic migration for promise_edits tables, ORM models, Pydantic input/output schemas, npm dependency installs, python-slugify, and RED-state test stubs. Nothing else in the phase can run until this plan completes.

Purpose: Establishes the database schema and code contracts that all subsequent plans in Phase 5 build against.
Output: Migration file, two ORM models, four Pydantic schemas, nine failing test stubs, updated requirements.txt, updated package.json.
</objective>

<execution_context>
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-promise-submission/05-CONTEXT.md
@.planning/phases/05-promise-submission/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/app/models/promises.py:
- ModerationStatus enum: pending / approved / rejected  (defined here; imported by evidence.py — do NOT redefine)
- Promise model: id (UUID PK), title_hy (String 500), quote_hy (Text), description_hy (Text?), politician_id (FK politicians.id), made_date (Date?), expected_date (Date?), source_url (Text), slug (String 300, unique), moderation_status (SAEnum name="moderation_status"), resolved_status (SAEnum name="resolved_status"), created_by (FK users.id?), created_at, updated_at
- PromiseElectionLink model: id (UUID PK), promise_id (FK promises.id CASCADE), election_id (FK elections.id CASCADE), UniqueConstraint(promise_id, election_id)

From backend/app/models/evidence.py (pattern for ModerationStatus reuse):
- Does NOT use create_type=False; it imports ModerationStatus from app.models.promises and uses SAEnum(ModerationStatus, name="moderation_status") — Alembic detects the type already exists and does NOT call CREATE TYPE again because the same Python object is reused.
- For the migration file itself, use sa.Enum("pending","approved","rejected", name="moderation_status", create_type=False) to avoid "type already exists" error.

From backend/alembic/versions/20260523_000001_auth_schema.py:
- revision = "20260523_000001"  ← this is down_revision for the new migration
- Migration naming convention: YYYYMMDD_NNNNNN_description.py

From backend/app/schemas/promises.py (existing — DO NOT remove):
- PromiseStubOut (model_config from_attributes=True): id, slug, title_hy, quote_hy, resolved_status
- PromiseListOut (no from_attributes): id, slug, title_hy, quote_hy, resolved_status, politician_name_hy, made_date
- PromiseDetailOut (no from_attributes): id, slug, title_hy, quote_hy, resolved_status, made_date, expected_date, source_url, archived_url, politician_name_hy, politician_slug

From backend/app/schemas/common.py:
- PaginatedResponse[T] generic — already exists; do NOT recreate

From frontend/src/types/index.ts (existing — do NOT remove existing types):
- All existing interfaces must remain; Phase 5 adds ElectionOut to be re-exported (already defined), plus new PromiseCreateIn, PromiseOut, PromiseEditIn, PromiseEditOut TypeScript interfaces are NOT needed in types/index.ts — they are only used as apiClient call shapes inline in the pages.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alembic migration + ORM models for promise_edits tables</name>
  <files>
    backend/alembic/versions/20260524_000001_add_promise_edits.py
    backend/app/models/promise_edits.py
    backend/requirements.txt
  </files>
  <read_first>
    - backend/alembic/versions/20260523_000001_auth_schema.py — down_revision value + migration file structure to replicate
    - backend/alembic/versions/20260521_000001_initial_schema.py — naming conventions, op.create_table patterns, constraint naming (pk__, uq__)
    - backend/app/models/promises.py — Promise model structure, ModerationStatus enum definition (reuse via import, do NOT redefine)
    - backend/app/models/evidence.py — pattern for importing and reusing ModerationStatus without CREATE TYPE collision
    - backend/requirements.txt — current contents; python-slugify line must be appended
  </read_first>
  <behavior>
    - Migration upgrade() creates promise_edits table with columns: id (UUID PK), promise_id (UUID FK promises.id CASCADE NOT NULL), submitted_by (UUID FK users.id SET NULL nullable), moderation_status (Enum pending/approved/rejected name=moderation_status create_type=False NOT NULL), title_hy (String 500 NOT NULL), quote_hy (Text NOT NULL), description_hy (Text nullable), source_url (Text NOT NULL), made_date (Date nullable), expected_date (Date nullable), created_at (DateTime timezone=True NOT NULL). PK constraint name: pk__promise_edits.
    - Migration upgrade() creates promise_edit_election_links table with columns: id (UUID PK), edit_id (UUID FK promise_edits.id CASCADE NOT NULL), election_id (UUID FK elections.id CASCADE NOT NULL). PK name: pk__promise_edit_election_links. UniqueConstraint(edit_id, election_id) name: uq__promise_edit_election_links__edit_id__election_id.
    - Migration downgrade() drops promise_edit_election_links then promise_edits (in dependency order).
    - PromiseEdit model imports ModerationStatus from app.models.promises (same pattern as evidence.py — no redefinition). Fields mirror the migration columns. Uses mapped_column with SAEnum(ModerationStatus, name="moderation_status") — Python object reuse, no create_type kwarg needed in ORM (only in migration).
    - PromiseEditElectionLink model mirrors PromiseElectionLink structure. UniqueConstraint in __table_args__.
    - requirements.txt gets one new line: python-slugify==8.0.4
  </behavior>
  <action>
    Create backend/alembic/versions/20260524_000001_add_promise_edits.py with revision="20260524_000001", down_revision="20260523_000001". In upgrade(): op.create_table("promise_edits", ...) using sa.Enum("pending","approved","rejected", name="moderation_status", create_type=False) for the moderation_status column. Then op.create_table("promise_edit_election_links", ...) with FK to promise_edits.id and elections.id, plus UniqueConstraint. In downgrade(): op.drop_table("promise_edit_election_links") then op.drop_table("promise_edits").

    Create backend/app/models/promise_edits.py. Import ModerationStatus from app.models.promises (identical to how evidence.py does it). Define PromiseEdit(Base) with __tablename__="promise_edits" and all columns from the migration. Define PromiseEditElectionLink(Base) with __tablename__="promise_edit_election_links" and UniqueConstraint("edit_id","election_id", name="uq__promise_edit_election_links__edit_id__election_id") in __table_args__.

    Append "python-slugify==8.0.4" to backend/requirements.txt.
  </action>
  <verify>
    <automated>docker compose exec backend alembic upgrade head && docker compose exec backend python -c "from app.models.promise_edits import PromiseEdit, PromiseEditElectionLink; print('OK')"</automated>
  </verify>
  <done>
    - Migration applies without "type already exists" error
    - promise_edits and promise_edit_election_links tables exist in DB
    - PromiseEdit and PromiseEditElectionLink are importable from app.models.promise_edits
    - requirements.txt contains python-slugify==8.0.4
  </done>
</task>

<task type="auto">
  <name>Task 2: Pydantic schemas + frontend npm deps + RED test stubs</name>
  <files>
    backend/app/schemas/promises.py
    backend/tests/test_promises.py
    frontend/package.json
  </files>
  <read_first>
    - backend/app/schemas/promises.py — existing PromiseStubOut, PromiseListOut, PromiseDetailOut that must NOT be removed; new schemas appended below them
    - backend/tests/test_promises.py — existing tests for PROM-01/02 that must NOT be removed; new stubs appended
    - frontend/package.json — current contents; verify @radix-ui/react-popover, cmdk, @radix-ui/react-checkbox, @radix-ui/react-radio-group, @radix-ui/react-label are NOT yet present before adding
    - .planning/phases/05-promise-submission/05-RESEARCH.md — Standard Stack table for exact npm package versions and install command
  </read_first>
  <action>
    Append to backend/app/schemas/promises.py (after existing schemas, before EOF):

    PromiseCreateIn(BaseModel) — NO from_attributes. Fields: title_hy: str, quote_hy: str, source_url: str, politician_id: uuid.UUID, made_date: date | None = None, expected_date: date | None = None, description_hy: str | None = None, election_ids: list[uuid.UUID] = [].

    PromiseOut(BaseModel) — response schema for POST /api/promises. Fields: id: uuid.UUID, slug: str, title_hy: str, moderation_status: ModerationStatus, created_at: datetime. Add "from datetime import date, datetime" import if not present; ModerationStatus already importable from app.models.promises.

    PromiseEditIn(BaseModel) — NO from_attributes. Fields: title_hy: str, quote_hy: str, source_url: str, made_date: date | None = None, expected_date: date | None = None, description_hy: str | None = None, election_ids: list[uuid.UUID] = [].

    PromiseEditOut(BaseModel) — response schema for PUT. Fields: id: uuid.UUID, promise_id: uuid.UUID, moderation_status: ModerationStatus, created_at: datetime.

    In backend/tests/test_promises.py, append RED-state stubs (all decorated with @pytest.mark.asyncio, all raise pytest.skip("RED") or simply have no body except "pass" to keep them discoverable but non-passing):
    - test_create_promise(client, verified_user_client) — will test POST /api/promises returns 201
    - test_create_promise_unauthenticated(client) — POST returns 401
    - test_create_promise_unverified(client, unverified_user_client) — POST returns 403
    - test_create_promise_too_young(client, young_user_client) — POST returns 403
    - test_create_promise_invalid_election(client, verified_user_client) — POST with fake election_id returns 422
    - test_create_promise_with_elections(client, verified_user_client) — POST creates PromiseElectionLink rows
    - test_edit_promise_creates_edit_row(client, verified_user_client) — PUT inserts into promise_edits not live promise
    - test_edit_promise_unauthenticated(client) — PUT returns 401

    For frontend deps: run the install command from inside frontend/:
    cd frontend && npm install @radix-ui/react-popover@^1.1.15 cmdk@^1.1.1 @radix-ui/react-checkbox@^1.3.3 @radix-ui/react-radio-group@^1.3.8 @radix-ui/react-label@^2.1.8 --legacy-peer-deps
    This updates frontend/package.json automatically.
  </action>
  <verify>
    <automated>docker compose exec backend python -c "from app.schemas.promises import PromiseCreateIn, PromiseOut, PromiseEditIn, PromiseEditOut; print('OK')" && docker compose exec backend pytest tests/test_promises.py -x --collect-only 2>&1 | grep -c "test_create_promise"</automated>
  </verify>
  <done>
    - PromiseCreateIn, PromiseOut, PromiseEditIn, PromiseEditOut importable from app.schemas.promises
    - All 8 new test stubs appear in pytest --collect-only output
    - frontend/package.json lists @radix-ui/react-popover, cmdk, @radix-ui/react-checkbox, @radix-ui/react-radio-group, @radix-ui/react-label
    - Existing test_list_promises / test_get_promise stubs still pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → package.json | New package additions must come from the vetted Package Legitimacy Audit in RESEARCH.md |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | promise_edits migration — moderation_status enum reuse | mitigate | Use sa.Enum(..., create_type=False) in migration; import ModerationStatus from promises.py in ORM model (no redefinition) |
| T-05-SC | Tampering | npm/pip installs (Wave 0) | mitigate | All packages in RESEARCH.md Package Legitimacy Audit; python-slugify [OK]; Radix/cmdk [ASSUMED-OK] per audit — proceed without blocking checkpoint |
</threat_model>

<verification>
Run after both tasks complete:
- docker compose exec backend alembic upgrade head — exits 0
- docker compose exec backend pytest tests/test_promises.py -x — existing tests pass, stubs collected
- docker compose exec backend python -c "from app.schemas.promises import PromiseCreateIn, PromiseOut, PromiseEditIn, PromiseEditOut; from app.models.promise_edits import PromiseEdit, PromiseEditElectionLink; print('all imports OK')"
- grep "python-slugify==8.0.4" backend/requirements.txt — exits 0
- grep "@radix-ui/react-popover" frontend/package.json — exits 0
</verification>

<success_criteria>
- Migration 20260524_000001 applies cleanly on top of 20260523_000001 without "type already exists" PostgreSQL error
- promise_edits and promise_edit_election_links tables present in DB
- PromiseEdit, PromiseEditElectionLink importable
- PromiseCreateIn, PromiseOut, PromiseEditIn, PromiseEditOut importable
- 8 new RED test stubs collected by pytest
- python-slugify==8.0.4 in requirements.txt
- 5 Radix/cmdk packages in frontend/package.json
</success_criteria>

<output>
Create `.planning/phases/05-promise-submission/05-01-SUMMARY.md` when done
</output>
