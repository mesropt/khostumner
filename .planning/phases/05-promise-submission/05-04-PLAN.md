---
phase: 05-promise-submission
plan: 04
type: execute
wave: 3
depends_on:
  - "05-02"
  - "05-03"
files_modified:
  - frontend/src/pages/PromiseSubmitPage.tsx
  - frontend/src/pages/PromisesListPage.tsx
  - frontend/src/components/Layout.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - PROM-03
  - ELEC-03

must_haves:
  truths:
    - "Unauthenticated user visiting /promises/new is redirected to /login"
    - "Authenticated eligible user can fill and submit the form at /promises/new"
    - "Ineligible user sees the form but submit button is replaced with eligibility gate message (not disabled)"
    - "Section 1 has politician combobox (required) and optional election cascading picker"
    - "Section 2 has title, quote, source URL (all required), dates, description"
    - "Successful submission redirects to /promises?submitted=1 and shows a green banner"
    - "Nav header shows Ներկայացնել link visible to all users"
    - "PromisesListPage reads ?submitted=1 and shows the green success banner"
  artifacts:
    - path: "frontend/src/pages/PromiseSubmitPage.tsx"
      provides: "Full /promises/new page with two-section form"
      contains: "PromiseSubmitPage"
    - path: "frontend/src/components/Layout.tsx"
      provides: "Nav link to /promises/new"
      contains: "Ներկայացնել"
    - path: "frontend/src/App.tsx"
      provides: "/promises/new route inside RequireAuth"
      contains: "/promises/new"
    - path: "frontend/src/pages/PromisesListPage.tsx"
      provides: "?submitted=1 success banner"
      contains: "submitted"
  key_links:
    - from: "frontend/src/pages/PromiseSubmitPage.tsx"
      to: "frontend/src/api/client.ts"
      via: "apiClient.post('/api/promises', payload)"
      pattern: "apiClient.post"
    - from: "frontend/src/pages/PromiseSubmitPage.tsx"
      to: "frontend/src/contexts/AuthContext.tsx"
      via: "useAuth().state.user.account_age_days and is_verified"
      pattern: "account_age_days"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/RequireAuth.tsx"
      via: "PromiseSubmitPage wrapped in RequireAuth"
      pattern: "RequireAuth"
---

<objective>
Wave 2 — PromiseSubmitPage: the full /promises/new page with two-section form (Ով section with politician combobox and election picker, Ինչ+Երբ section with content fields), eligibility gate, nav link in Layout.tsx, route in App.tsx, and success banner wired to PromisesListPage. This is the primary user-facing deliverable for PROM-03 and ELEC-03.

Purpose: Allows registered eligible users to submit new promises with election links entering the admin queue.
Output: PromiseSubmitPage.tsx, updated Layout.tsx and App.tsx, updated PromisesListPage.tsx for success banner.
</objective>

<execution_context>
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-promise-submission/05-CONTEXT.md
@.planning/phases/05-promise-submission/05-UI-SPEC.md
@.planning/phases/05-promise-submission/05-02-SUMMARY.md
@.planning/phases/05-promise-submission/05-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/App.tsx (current):
- RequireAuth element already used at line 42-44 with empty children block
- New routes: PromiseSubmitPage at /promises/new and PromiseEditPage at /promises/:slug/edit inside RequireAuth
- Import pattern: import PromiseSubmitPage from "@/pages/PromiseSubmitPage"

From frontend/src/components/Layout.tsx (current):
- navLinkClass function pattern: ({ isActive }) => isActive ? "text-sm font-semibold text-zinc-900" : "text-sm text-zinc-600 hover:text-zinc-900"
- Nav link order per UI-SPEC.md: after "Ընտրություններ" and before "Մեր մասին"
- Label: "Ներկայացնել" (shortened from "Ներկայացնել խոստում" per UI-SPEC.md Nav link section)

From frontend/src/pages/PromisesListPage.tsx (current — add success banner):
- Uses useSearchParams; pattern already established
- Banner: read searchParams.get("submitted") === "1"; show dismissible green banner above list if true
- Banner classes (UI-SPEC.md Success toast section): "bg-green-100 border border-green-200 text-green-800 text-sm px-4 py-3 rounded-md mb-6 flex justify-between items-center"
- Dismiss: useState<boolean> showBanner; button onClick sets showBanner false

From frontend/src/contexts/AuthContext.tsx (updated in plan 05-03):
- UserRead: { id, email, display_name, is_active, is_verified: boolean, role, account_age_days: number }
- useAuth().state.user gives UserRead | null

From frontend/src/hooks/usePoliticianSearch.ts (created in plan 05-03):
- usePoliticianSearch() → { politicians: PoliticianOut[], isLoading }

From frontend/src/hooks/useElectionsAll.ts (created in plan 05-03):
- useElectionsAll() → { elections: ElectionOut[], isLoading }

From frontend/src/components/ui/ (available after plan 05-03):
- Card, CardHeader, CardTitle, CardContent (card.tsx)
- Button (button.tsx)
- Input (input.tsx)
- Separator (separator.tsx)
- Combobox (combobox.tsx) — props: items, value, onChange, placeholder, emptyText
- Checkbox (checkbox.tsx)
- RadioGroup, RadioGroupItem (radio-group.tsx)
- Label (label.tsx)

PromiseCreateIn shape (for apiClient.post body):
{ title_hy: string, quote_hy: string, source_url: string, politician_id: string, made_date: string | null, expected_date: string | null, description_hy: string | null, election_ids: string[] }

UI-SPEC.md key specs:
- Form container: max-w-2xl mx-auto px-4 py-8
- Section separation: Separator with my-8
- Fields within sections: space-y-4 between groups, space-y-2 between label and input
- Textarea classes: "flex min-h-[80px] w-full rounded-md border border-input bg-background px-3 py-2 text-sm ring-offset-background placeholder:text-muted-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50 resize-y font-normal"
- Submit button: type="submit" className="w-full mt-6" disabled={isSubmitting}
- Eligibility gate replaces submit: p className="text-sm text-zinc-500 mt-6 p-4 bg-zinc-100 rounded-md"
- Section headings: text-lg font-semibold mb-4
- Card title: text-2xl font-semibold (CardTitle default)
- Error messages: p className="text-sm text-red-600 mt-1"
- Label: text-sm font-normal (shadcn Label default — do NOT add font-medium)
- Election toggle: Checkbox + Label flex row
- Election picker hidden class when toggle unchecked: "hidden" (not unmounted)
- Election level: RadioGroup horizontal flex gap-6
- Election list: space-y-2 vertical checkboxes
- Touch targets: py-3 on each RadioGroupItem row
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PromiseSubmitPage + App.tsx route + Layout nav link</name>
  <files>
    frontend/src/pages/PromiseSubmitPage.tsx
    frontend/src/App.tsx
    frontend/src/components/Layout.tsx
  </files>
  <read_first>
    - frontend/src/pages/LoginPage.tsx — form pattern: useState per field, handleSubmit, credentials, error state, Card + CardHeader + CardTitle + CardContent structure; replicate this pattern
    - frontend/src/App.tsx — current route structure; RequireAuth block location; where to add new routes
    - frontend/src/components/Layout.tsx — current nav link list; where to insert the new Ներկայացնել link
    - .planning/phases/05-promise-submission/05-UI-SPEC.md — full component inventory for PromiseSubmitPage; copywriting contract for all Armenian strings; interaction states; exact class names
    - .planning/phases/05-promise-submission/05-CONTEXT.md — D-02 (eligibility gate exact copy), D-09 (two sections), D-11 (combobox), D-13/D-14 (election toggle + cascading picker), D-15 (client-side filter by level)
  </read_first>
  <behavior>
    - /promises/new route renders PromiseSubmitPage inside RequireAuth (unauthenticated → redirected to /login by RequireAuth)
    - PromiseSubmitPage renders Card with CardTitle "Ներկայացնել խոստում"
    - Section 1 heading: h2 "Ով" text-lg font-semibold; Politician Combobox required (placeholder "Ընտրել քաղաքական գործչին", emptyText "Ոչ մի արդյունք"); election toggle Checkbox + Label "Կապել ընտրություններին"; when toggle checked: RadioGroup horizontal (Ազգային/Տեղական/Հանրաքվե) then Checkbox list of elections filtered by level (space-y-2); when no level selected: text-sm text-zinc-500 "Ընտրեք մակարդակ"
    - Section 2 heading: h2 "Ինչ + Երբ" text-lg font-semibold; fields: title_hy (Input), quote_hy (textarea), source_url (Input type=url), made_date (Input type=date), expected_date (Input type=date), description_hy (textarea); all use Label + field pattern with space-y-2
    - Eligible user (is_verified && account_age_days >= 7): submit Button present and active
    - Ineligible user: submit button REPLACED with p.text-sm.text-zinc-500.mt-6.p-4.bg-zinc-100.rounded-md containing exact eligibility gate text per D-02 and UI-SPEC.md
    - On submit: client-side validate required fields (title_hy min 5, quote_hy min 10, source_url must start with http/https, politician_id required); show inline errors on fail
    - On API success: navigate to /promises?submitted=1
    - On API error 403: show server error message "Ձեր հաշիվը դեռ պատրաստ չէ ներկայացնելու"
    - On API error 422: show "Ստուգեք դաշտերը:" above form
    - On network error: show "Կապի հետ կապված խնդիր կա: Փորձեք կրկին"
    - During submit: button shows "..." and is disabled
    - Nav link "Ներկայացնել" uses navLinkClass, inserted between Ընտրություններ and Մեր մասին links
  </behavior>
  <action>
    Create frontend/src/pages/PromiseSubmitPage.tsx. Imports: useState, useNavigate from react/react-router-dom; Card/CardHeader/CardTitle/CardContent from @/components/ui/card; Button, Input, Separator, Label, Checkbox, RadioGroup, RadioGroupItem, Combobox from their ui paths; useAuth from @/hooks/useAuth; usePoliticianSearch from @/hooks/usePoliticianSearch; useElectionsAll from @/hooks/useElectionsAll; apiClient from @/api/client; ElectionLevel from @/types (add "national"|"local"|"referendum" as a local string union if not in types/index.ts).

    State: politician_id (string ""), title_hy, quote_hy, source_url, made_date, expected_date, description_hy (all strings ""), linkElections (boolean false), selectedLevel (string ""), selectedElectionIds (string[] []), fieldErrors (Record<string, string> {}), serverError (string | null), isSubmitting (boolean false).

    Eligibility: const user = useAuth().state.user; const isEligible = !!user && user.is_verified && user.account_age_days >= 7.

    Election picker: elections filtered by selectedLevel using elections.filter(e => e.level === selectedLevel).

    Post payload: { title_hy, quote_hy, source_url, politician_id, made_date: made_date || null, expected_date: expected_date || null, description_hy: description_hy || null, election_ids: linkElections ? selectedElectionIds : [] }.

    In App.tsx: add import for PromiseSubmitPage; add Route path="/promises/new" element inside the existing RequireAuth Route block.

    In Layout.tsx: add NavLink to="/promises/new" with navLinkClass between Ընտրություններ and Մեր մասին links. Label: "Ներկայացնել".
  </action>
  <verify>
    <automated>cd C:\Users\MesropTarkhanyan\PycharmProjects\khostumner\frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - frontend/src/pages/PromiseSubmitPage.tsx exists and builds without TypeScript errors
    - App.tsx contains Route path="/promises/new" inside RequireAuth
    - Layout.tsx contains NavLink to="/promises/new" with label Ներկայացնել
    - npm run build exits 0
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    PromiseSubmitPage at /promises/new with two-section form, politician combobox, optional election cascading picker, eligibility gate, nav link, and success banner on /promises?submitted=1.
  </what-built>
  <how-to-verify>
    1. Start the dev server: docker compose up -d, then open http://localhost:5173 in a browser.
    2. Click "Ներկայացնել" in the nav — verify it appears between "Ընտրություններ" and "Մեր մասին".
    3. Without logging in, click the link — verify redirect to /login page.
    4. Log in with a test account (any account from seed data or newly registered).
    5. Navigate to /promises/new — verify:
       a. Page title card: "Ներկայացնել խոստում"
       b. Section 1 (Ով): politician combobox shows "Ընտրել քաղաքական գործչին" placeholder; typing in it filters politicians
       c. Checkbox "Կապել ընտրություններին" is unchecked; checking it reveals the election level radio buttons (Ազգային / Տեղական / Հանրաքվե) in a horizontal row
       d. Selecting a level reveals checkboxes for elections of that level
    6. For a freshly registered account (account_age_days = 0): verify the submit area shows the Armenian eligibility gate message, NOT a submit button.
    7. For an eligible account (account_age_days >= 7, is_verified): verify the "Ներկայացնել" button is present.
    8. Leave required fields empty and attempt to submit — verify inline field errors appear below each required field.
    9. Fill all required fields, click submit — verify redirect to /promises?submitted=1 and the green success banner appears.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

<task type="auto">
  <name>Task 3: PromisesListPage success banner</name>
  <files>
    frontend/src/pages/PromisesListPage.tsx
  </files>
  <read_first>
    - frontend/src/pages/PromisesListPage.tsx — current content; add showBanner state and conditional banner render at top of content area
    - .planning/phases/05-promise-submission/05-UI-SPEC.md — Success toast section: exact banner classes and Armenian copy
  </read_first>
  <behavior>
    - When URL has ?submitted=1: show green banner with text "Ձեր խոստումը ներկայացված է: Այն կհրապարակվի ստուգումից հետո" and a dismiss button "✕"
    - Dismiss button removes the banner from the DOM (useState showBanner)
    - Banner is NOT shown when ?submitted=1 is absent from URL
    - Existing filter/list behavior is unchanged
  </behavior>
  <action>
    In PromisesListPage.tsx: add const [showBanner, setShowBanner] = useState(searchParams.get("submitted") === "1"). Render the banner before the filter row when showBanner is true:
    div className="bg-green-100 border border-green-200 text-green-800 text-sm px-4 py-3 rounded-md mb-6 flex justify-between items-center":
    - span with the success message text
    - button onClick={() => setShowBanner(false)} className="text-green-800 font-semibold ml-4" with ✕

    No other changes to the existing page.
  </action>
  <verify>
    <automated>cd C:\Users\MesropTarkhanyan\PycharmProjects\khostumner\frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - PromisesListPage.tsx contains showBanner state derived from ?submitted=1
    - Green banner div present in JSX with correct class names and dismiss button
    - npm run build exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form → POST /api/promises | User-controlled form data crosses into API; client validates but server is authoritative |
| RequireAuth component → /promises/new | Unauthenticated access blocked by client-side guard; server also enforces via 401 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Spoofing | /promises/new — unauthenticated access | mitigate | RequireAuth redirects to /login; server also returns 401 (defense in depth) |
| T-05-12 | Tampering | PromiseSubmitPage — eligibility gate bypass via DevTools | accept | Client gate is UX only; server enforces eligibility with 403 (D-01 server-side guard in plan 05-02) |
| T-05-13 | Tampering | Source URL field — user inputs arbitrary URL | accept | Phase 5 stores as-is; source validation is admin moderation task (per RESEARCH.md threat analysis) |
| T-05-14 | Denial of Service | Rapid form submissions | accept | account_age_days >= 7 requirement; rate limiting deferred; all submissions enter moderation queue |
| T-05-SC | Tampering | No new packages in this plan | accept | No npm installs; all deps from plans 05-01/05-03 |
</threat_model>

<verification>
Run after all tasks complete:
- cd frontend && npm run build — exits 0
- grep "promises/new" frontend/src/App.tsx — match found
- grep "Ներկայացնել" frontend/src/components/Layout.tsx — match found
- grep "submitted" frontend/src/pages/PromisesListPage.tsx — match found
- Human verification checkpoint passed
</verification>

<success_criteria>
- /promises/new route exists inside RequireAuth in App.tsx
- Unauthenticated visit redirects to /login
- Eligible user sees the full form with submit button
- Ineligible user sees the form with eligibility gate message (not a disabled button)
- Politician combobox loads and filters politicians
- Election toggle reveals cascading level+election picker when checked
- Successful submission redirects to /promises?submitted=1
- Green success banner on /promises?submitted=1
- npm run build exits 0
</success_criteria>

<output>
Create `.planning/phases/05-promise-submission/05-04-SUMMARY.md` when done
</output>
