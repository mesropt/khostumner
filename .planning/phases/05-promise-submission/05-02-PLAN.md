---
phase: 05-promise-submission
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - backend/app/routers/promises.py
  - backend/app/schemas/auth.py
autonomous: true
requirements:
  - PROM-03
  - PROM-04
  - ELEC-03

must_haves:
  truths:
    - "POST /api/promises returns 201 with PromiseOut for eligible authenticated user"
    - "POST /api/promises returns 401 for unauthenticated request"
    - "POST /api/promises returns 403 for unverified user (fastapi-users enforces this)"
    - "POST /api/promises returns 403 when user.account_age_days < 7"
    - "POST /api/promises returns 422 when any election_id UUID does not exist in elections table"
    - "New promise row has moderation_status=pending and slug auto-generated from title_hy via python-slugify"
    - "PromiseElectionLink rows are created for all valid election_ids"
    - "PUT /api/promises/{slug} returns 201 with PromiseEditOut for eligible user"
    - "PUT /api/promises/{slug} inserts a new promise_edits row (does NOT update live promise row)"
    - "PUT /api/promises/{slug} finds pending promises (not filtered to approved-only)"
    - "GET /api/users/me response includes account_age_days field"
  artifacts:
    - path: "backend/app/routers/promises.py"
      provides: "POST /api/promises and PUT /api/promises/{slug} endpoints"
      contains: "async def create_promise"
    - path: "backend/app/schemas/auth.py"
      provides: "UserRead schema extended with account_age_days"
      contains: "account_age_days"
  key_links:
    - from: "backend/app/routers/promises.py"
      to: "backend/app/auth/users.py"
      via: "fastapi_users.current_user(active=True, verified=True)"
      pattern: "current_verified_user"
    - from: "backend/app/routers/promises.py"
      to: "backend/app/models/promise_edits.py"
      via: "PromiseEdit INSERT"
      pattern: "PromiseEdit("
---

<objective>
Wave 1a — Backend endpoints: POST /api/promises (create new promise with eligibility guard + slug generation + election linking) and PUT /api/promises/{slug} (edit → append-only promise_edits insert). Also extends the UserRead schema with account_age_days so the frontend eligibility check has the field it needs.

Purpose: Delivers the write-side API that the frontend pages in plans 04 and 05 will call.
Output: Two new route handlers in promises.py router + account_age_days added to UserRead schema.
</objective>

<execution_context>
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-promise-submission/05-CONTEXT.md
@.planning/phases/05-promise-submission/05-RESEARCH.md
@.planning/phases/05-promise-submission/05-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/app/routers/promises.py (existing — append routes; DO NOT modify existing GET routes):
- router = APIRouter(prefix="/promises", tags=["promises"])
- Existing: GET "" (list_promises), GET "/{slug}" (get_promise)
- Existing imports: uuid, date, fastapi (APIRouter, Depends, HTTPException, Query), sqlalchemy (func, select), AsyncSession, get_db, Politician, ModerationStatus/Promise/PromiseElectionLink/ResolvedStatus, PaginatedResponse, PromiseDetailOut/PromiseListOut

From backend/app/auth/users.py:
- fastapi_users = FastAPIUsers[User, uuid.UUID](get_user_manager, [...])
- Dependency pattern: current_verified_user = fastapi_users.current_user(active=True, verified=True)

From backend/app/models/users.py:
- User model has: id (UUID), is_verified (bool, via base), account_age_days (int, default=0), created_at (DateTime)

From backend/app/models/promise_edits.py (created in plan 05-01):
- PromiseEdit: id (UUID PK, default uuid4), promise_id (FK promises.id), submitted_by (FK users.id nullable), moderation_status (SAEnum ModerationStatus), title_hy, quote_hy, description_hy, source_url, made_date, expected_date, created_at (DateTime timezone=True)
- PromiseEditElectionLink: id (UUID PK), edit_id (FK promise_edits.id), election_id (FK elections.id)

From backend/app/schemas/promises.py (created in plan 05-01):
- PromiseCreateIn: title_hy, quote_hy, source_url, politician_id, made_date?, expected_date?, description_hy?, election_ids: list[uuid.UUID]
- PromiseOut: id, slug, title_hy, moderation_status, created_at
- PromiseEditIn: title_hy, quote_hy, source_url, made_date?, expected_date?, description_hy?, election_ids: list[uuid.UUID]
- PromiseEditOut: id, promise_id, moderation_status, created_at

From backend/app/models/elections.py:
- Election: id (UUID), name_hy, slug, election_date, level (ElectionLevel enum: national/local/referendum)

From backend/app/models/promises.py:
- Promise: id, title_hy, quote_hy, description_hy, politician_id, made_date, expected_date, source_url, slug (unique), moderation_status (default pending), resolved_status, created_by (nullable FK users.id), created_at

FastAPI-Users behavior (per RESEARCH.md Pattern 1):
- fastapi_users.current_user(active=True, verified=True) — raises 401 if not authenticated, 403 if not verified. account_age_days must still be checked manually.

Slug generation (per RESEARCH.md Pattern 2):
- from slugify import slugify; slugify(title_hy, allow_unicode=False) → ASCII slug
- SELECT slug FROM promises WHERE slug = :base LIMIT 1; if exists: slug = f"{base}-{str(uuid4())[:6]}"
- Wrap INSERT in try/except asyncpg.UniqueViolationError as safety net; re-raise as HTTP 409 or append another suffix

Election validation (per RESEARCH.md Pitfall 3):
- SELECT id FROM elections WHERE id = ANY(:ids) and verify count matches len(election_ids); 422 if mismatch

PUT slug lookup (per RESEARCH.md Pitfall 4):
- Look up promise by slug WITHOUT moderation_status=approved filter
- Filter: moderation_status != rejected (pending + approved are editable)
- Return 404 if rejected or not found
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: POST /api/promises — create promise endpoint with eligibility guard</name>
  <files>
    backend/app/routers/promises.py
  </files>
  <read_first>
    - backend/app/routers/promises.py — current full content; new route appended after existing routes
    - backend/app/auth/users.py — fastapi_users object; how to get current_user dependency
    - backend/app/models/promises.py — Promise model fields; PromiseElectionLink structure; ModerationStatus enum
    - backend/app/models/elections.py — Election model; needed for election_ids validation query
    - backend/app/schemas/promises.py — PromiseCreateIn and PromiseOut signatures (created in 05-01)
    - backend/app/models/promise_edits.py — not used in this task but needed context for plan 05-03
  </read_first>
  <behavior>
    - POST /api/promises with valid payload from eligible user → 201 PromiseOut with moderation_status=pending
    - POST /api/promises without auth cookie → 401
    - POST /api/promises with unverified user → 403 (fastapi-users enforces)
    - POST /api/promises with account_age_days < 7 → 403 detail="Account must be at least 7 days old to submit promises"
    - POST /api/promises with election_ids containing a UUID not in elections table → 422 detail="Invalid election IDs: {ids}"
    - Slug is auto-generated: slugify(title_hy, allow_unicode=False); if empty string, use "promise"; SELECT EXISTS for collision; if collides, append f"-{uuid4().hex[:6]}"; safety net: catch IntegrityError and retry with new suffix
    - await db.flush() after db.add(promise) before creating PromiseElectionLink rows
    - All PromiseElectionLink rows created for valid election_ids
    - Promise.created_by set to user.id
    - Promise.moderation_status = ModerationStatus.pending (never trust client value)
  </behavior>
  <action>
    Add to backend/app/routers/promises.py (after existing imports and existing routes):

    New imports to add: from app.auth.users import fastapi_users; from app.models.users import User; from app.models.promise_edits import PromiseEdit, PromiseEditElectionLink; from app.models.elections import Election; from app.schemas.promises import PromiseCreateIn, PromiseOut, PromiseEditIn, PromiseEditOut; from slugify import slugify; import uuid as _uuid; from sqlalchemy.exc import IntegrityError; from datetime import datetime, timezone.

    Define at module level (after imports): current_verified_user = fastapi_users.current_user(active=True, verified=True)

    Define helper function generate_slug(title_hy: str) -> str: returns slugify(title_hy, allow_unicode=False) or "promise" if empty.

    Implement @router.post("", status_code=201, response_model=PromiseOut). Signature: payload: PromiseCreateIn, db: AsyncSession = Depends(get_db), user: User = Depends(current_verified_user). Steps: (1) check user.account_age_days >= 7 else HTTPException 403. (2) If payload.election_ids non-empty: query SELECT election.id FROM elections WHERE id = ANY(election_ids); verify count matches; raise HTTPException 422 if mismatch, detail lists unknown IDs. (3) Generate slug: base = generate_slug(payload.title_hy); check SELECT 1 FROM promises WHERE slug=base; if exists slug=f"{base}-{_uuid.uuid4().hex[:6]}". (4) Create Promise object with all fields, moderation_status=ModerationStatus.pending, created_by=user.id, slug=slug. db.add(promise). (5) await db.flush(). (6) Create PromiseElectionLink for each election_id. (7) try: await db.commit(); except IntegrityError: await db.rollback(); append new UUID suffix to slug; retry once. (8) await db.refresh(promise). (9) Return PromiseOut(id=promise.id, slug=promise.slug, title_hy=promise.title_hy, moderation_status=promise.moderation_status, created_at=promise.created_at).
  </action>
  <verify>
    <automated>docker compose exec backend pytest tests/test_promises.py::test_create_promise_unauthenticated tests/test_promises.py::test_create_promise_too_young -x</automated>
  </verify>
  <done>
    - POST /api/promises endpoint registered in the router
    - test_create_promise_unauthenticated passes (401)
    - test_create_promise_too_young passes (403)
    - docker compose exec backend pytest tests/test_promises.py -x — no import errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PUT /api/promises/{slug} — append-only edit endpoint + account_age_days in UserRead</name>
  <files>
    backend/app/routers/promises.py
    backend/app/schemas/auth.py
  </files>
  <read_first>
    - backend/app/routers/promises.py — current state after Task 1; PUT route appended here
    - backend/app/schemas/auth.py — current UserRead schema; account_age_days must be added (if file does not exist, check backend/app/auth/ directory for where UserRead is defined)
    - backend/app/auth/users.py — fastapi_users already imported in Task 1; current_verified_user already defined
    - backend/app/models/promise_edits.py — PromiseEdit and PromiseEditElectionLink needed for the INSERT
    - .planning/phases/05-promise-submission/05-RESEARCH.md — Pattern 3 (append-only INSERT) and Pitfall 4 (no approved filter on edit lookup)
  </read_first>
  <behavior>
    - PUT /api/promises/{slug} with valid payload from eligible user → 201 PromiseEditOut; promise_edits row inserted with moderation_status=pending; live Promise row NOT updated
    - PUT /api/promises/{slug} without auth → 401
    - PUT /api/promises/{slug} on a rejected promise → 404
    - PUT /api/promises/{slug} on a pending or approved promise → 201 (both are editable)
    - account_age_days field appears in GET /api/users/me response
  </behavior>
  <action>
    Append @router.put("/{slug}", status_code=201, response_model=PromiseEditOut) to backend/app/routers/promises.py. Signature: slug: str, payload: PromiseEditIn, db: AsyncSession = Depends(get_db), user: User = Depends(current_verified_user). Steps: (1) check user.account_age_days >= 7 else HTTPException 403. (2) SELECT Promise WHERE slug=slug AND moderation_status != rejected; if not found raise HTTPException 404 detail="Promise not found". (3) If payload.election_ids non-empty: validate all IDs exist in elections table same as POST; raise 422 if invalid. (4) Create PromiseEdit(promise_id=promise.id, submitted_by=user.id, moderation_status=ModerationStatus.pending, title_hy=payload.title_hy, quote_hy=payload.quote_hy, description_hy=payload.description_hy, source_url=payload.source_url, made_date=payload.made_date, expected_date=payload.expected_date, created_at=datetime.now(timezone.utc)). db.add(edit). (5) await db.flush() to get edit.id. (6) Create PromiseEditElectionLink for each election_id. (7) await db.commit(). (8) await db.refresh(edit). (9) Return PromiseEditOut(id=edit.id, promise_id=edit.promise_id, moderation_status=edit.moderation_status, created_at=edit.created_at). DO NOT touch the live Promise row.

    Find the file defining UserRead schema (likely backend/app/auth/schemas.py or backend/app/schemas/users.py — grep for "class UserRead" to locate). Add account_age_days: int = 0 to the UserRead class. This makes the field appear in GET /api/users/me responses that the frontend AuthContext parses.
  </action>
  <verify>
    <automated>docker compose exec backend pytest tests/test_promises.py::test_edit_promise_unauthenticated -x && docker compose exec backend python -c "from app.schemas.promises import PromiseEditOut; print('OK')"</automated>
  </verify>
  <done>
    - PUT /api/promises/{slug} route registered in promises router
    - test_edit_promise_unauthenticated passes (401)
    - UserRead class contains account_age_days field
    - GET /api/users/me response includes account_age_days in JSON
    - docker compose exec backend pytest tests/ -x — full suite green (stubs may skip, not fail)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request body → Promise INSERT | User-supplied election_ids, politician_id, and content fields cross into DB write path |
| current_verified_user dependency → router | FastAPI-Users handles 401/403; account_age_days must be checked manually |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Spoofing | POST /api/promises — unverified/new accounts | mitigate | fastapi_users.current_user(verified=True) raises 403 for unverified; manual account_age_days >= 7 check raises 403 (D-01) |
| T-05-03 | Tampering | POST /api/promises — client-supplied moderation_status | mitigate | Backend always sets ModerationStatus.pending; PromiseCreateIn has no moderation_status field |
| T-05-04 | Tampering | POST /api/promises — invalid election_ids | mitigate | Validate all UUIDs via SELECT count before INSERT; raise 422 if any unknown (RESEARCH Pitfall 3) |
| T-05-05 | Tampering | PUT /api/promises/{slug} — mutating live promise | mitigate | Router only INSERTs into promise_edits; no UPDATE on promises table (D-04, D-06) |
| T-05-06 | Information Disclosure | PUT /api/promises/{slug} — 404 reveals pending promise existence | accept | Acceptable v1 trade-off; PUT returns 404 for rejected promises, 201 for pending/approved — low-sensitivity data |
| T-05-07 | Denial of Service | Mass promise submissions | mitigate | account_age_days >= 7 gate limits blast radius from newly created accounts; rate limiting deferred to Phase 8+ |
| T-05-08 | Tampering | CSRF on POST/PUT | mitigate | starlette_csrf middleware already active in main.py; x-csrftoken header required — Phase 4 established |
| T-05-SC | Tampering | No new package installs in this plan | accept | No new packages; slugify installed in plan 05-01 |
</threat_model>

<verification>
Run after both tasks complete:
- docker compose exec backend pytest tests/test_promises.py -x — no errors (stubs may skip)
- docker compose exec backend python -c "import app.routers.promises; print([r.path for r in app.routers.promises.router.routes])" — output includes /promises and /promises/{slug} with POST and PUT methods
- docker compose exec backend pytest tests/test_promises.py::test_create_promise_unauthenticated tests/test_promises.py::test_edit_promise_unauthenticated -x — both pass
</verification>

<success_criteria>
- POST /api/promises and PUT /api/promises/{slug} routes present in promises router
- POST returns 401 for unauthenticated (test passes)
- POST returns 403 for account_age_days < 7 (test passes)
- PUT returns 401 for unauthenticated (test passes)
- PUT does NOT touch live Promise row (append-only to promise_edits)
- account_age_days present in UserRead schema and in GET /api/users/me JSON response
- Full test suite exits 0
</success_criteria>

<output>
Create `.planning/phases/05-promise-submission/05-02-SUMMARY.md` when done
</output>
