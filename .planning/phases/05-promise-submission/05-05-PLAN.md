---
phase: 05-promise-submission
plan: 05
type: execute
wave: 4
depends_on:
  - "05-04"
files_modified:
  - frontend/src/pages/PromiseEditPage.tsx
  - frontend/src/pages/PromiseDetailPage.tsx
  - frontend/src/App.tsx
autonomous: false
requirements:
  - PROM-04
  - ELEC-03

must_haves:
  truths:
    - "Authenticated eligible user can open /promises/:slug/edit and submit an edit"
    - "PromiseEditPage pre-fills all content fields from the existing promise data"
    - "Politician field on edit page is read-only (displays current politician name, no combobox change)"
    - "Edit submission calls PUT /api/promises/:slug and redirects to /promises?submitted=1 on success"
    - "PromiseDetailPage shows Խmբagrel link for authenticated users"
    - "/promises/:slug/edit route is inside RequireAuth in App.tsx"
    - "Edit notice displayed below CardTitle: exact Armenian copy from UI-SPEC.md"
  artifacts:
    - path: "frontend/src/pages/PromiseEditPage.tsx"
      provides: "/promises/:slug/edit page with pre-filled form"
      contains: "PromiseEditPage"
    - path: "frontend/src/pages/PromiseDetailPage.tsx"
      provides: "Խmbagrel link visible to authenticated users"
      contains: "Խmbagrel"
    - path: "frontend/src/App.tsx"
      provides: "/promises/:slug/edit route inside RequireAuth"
      contains: "slug/edit"
  key_links:
    - from: "frontend/src/pages/PromiseEditPage.tsx"
      to: "frontend/src/api/client.ts"
      via: "apiClient.put('/api/promises/:slug', payload)"
      pattern: "apiClient.put"
    - from: "frontend/src/pages/PromiseDetailPage.tsx"
      to: "frontend/src/pages/PromiseEditPage.tsx"
      via: "Link to /promises/:slug/edit"
      pattern: "slug.*edit"
---

<objective>
Wave 3 — PromiseEditPage: the /promises/:slug/edit page that pre-fills from existing promise data and submits via PUT /api/promises/:slug. Also adds the "Խmbagrel" (Edit) link to PromiseDetailPage for authenticated users. This delivers PROM-04.

Purpose: Completes the edit flow so registered users can submit promise content corrections that enter the admin queue.
Output: PromiseEditPage.tsx, updated PromiseDetailPage.tsx, updated App.tsx.
</objective>

<execution_context>
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-promise-submission/05-CONTEXT.md
@.planning/phases/05-promise-submission/05-UI-SPEC.md
@.planning/phases/05-promise-submission/05-04-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/pages/PromiseSubmitPage.tsx (created in plan 05-04 — use as direct template):
- Exact same two-section structure, same eligibility gate, same field components
- Differences: CardTitle = "Խmbagrel խostamanǝ", edit notice below CardTitle, politician field is read-only, submit label = "Ներкayacnel p'vox'utyun", calls apiClient.put not apiClient.post

From frontend/src/pages/PromiseDetailPage.tsx (current):
- Shows title, quote, status badge, politician link, dates, source links
- Edit link added below source links, visible only when state.user is not null
- Link classes per UI-SPEC.md: "text-sm text-blue-600 underline"

From frontend/src/hooks/usePromise.ts (existing):
- usePromise(slug: string) → { data: PromiseDetailOut | undefined, isLoading, isError }
- PromiseDetailOut has: id, slug, title_hy, quote_hy, resolved_status, made_date, expected_date, source_url, archived_url, politician_name_hy, politician_slug

PromiseEditIn shape (for apiClient.put body):
{ title_hy: string, quote_hy: string, source_url: string, made_date: string | null, expected_date: string | null, description_hy: string | null, election_ids: string[] }

Note: PromiseEditIn does NOT include politician_id (only content fields are editable per D-05). The politician field in PromiseEditPage shows the current politician name (from usePromise data) as a read-only label — no Combobox.

UI-SPEC.md key specs for PromiseEditPage:
- CardTitle: "Խmbragurel khostamanǝ" — exact copy: "Խmbragrurel khostumǝ" — use UI-SPEC.md Copywriting Contract: "Խmbragrurel khostumǝ"
- Edit notice (below CardTitle, before Section 1): p className="text-sm text-zinc-500 mb-4" with text "Փokhox'utyunǝ kaugh'arkvum e stugman. Netkayis tarberaku kasharasuguma cducvel minch hastatum"
- Submit button label: "Ներкayacnel p'vox'utyun"

EXACT Armenian strings from UI-SPEC.md Copywriting Contract:
- Page title: "Խmbragrurel khostumǝ" → exact: "Խmbragrurel khostumǝ" — use the exact value from UI-SPEC.md
- Edit notice text: "Փokhox'utyunǝ kaugh'arkvum e stugman. Netkayis tarberaku kasharasuguma cduc'el minch' hastatum"
- Submit button: "Ներказачнел փոpoxutyun"

NOTE TO EXECUTOR: Use the exact Armenian strings from .planning/phases/05-promise-submission/05-UI-SPEC.md Copywriting Contract table, NOT the transliterations above. Read the UI-SPEC.md file directly for the correct Armenian text values.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PromiseEditPage + App.tsx route</name>
  <files>
    frontend/src/pages/PromiseEditPage.tsx
    frontend/src/App.tsx
  </files>
  <read_first>
    - frontend/src/pages/PromiseSubmitPage.tsx — created in plan 05-04; replicate structure for edit page; key differences are CardTitle, edit notice, read-only politician field, PUT call, no Combobox for politician
    - frontend/src/App.tsx — current state after plan 05-04; add /promises/:slug/edit route inside RequireAuth block
    - .planning/phases/05-promise-submission/05-UI-SPEC.md — PromiseEditPage section (page structure, CardTitle "Խmbragrurel khostumǝ", edit notice text, submit button label "Ներказачнел փopoxutyun"); Copywriting Contract table for exact Armenian strings
    - .planning/phases/05-promise-submission/05-CONTEXT.md — D-05 (editable fields: title_hy, quote_hy, description_hy, source_url, made_date, expected_date, election_ids — NOT politician_id); D-07 (same eligibility as new submission)
    - frontend/src/hooks/usePromise.ts — used to load existing promise data for pre-fill; PromiseDetailOut type fields
  </read_first>
  <behavior>
    - /promises/:slug/edit renders PromiseEditPage inside RequireAuth
    - Page loads existing promise data via usePromise(slug) to pre-fill all fields
    - While loading: show loading skeleton (same animate-pulse pattern as PromiseDetailPage)
    - If promise not found: show 404 message with link back to /promises
    - Politician field: read-only display (p or span showing politician_name_hy from promise data); NO combobox — user cannot change politician on edit
    - All content fields pre-filled from promise data: title_hy, quote_hy, source_url, made_date (or ""), expected_date (or ""), description_hy (or "")
    - Election picker pre-filled: if promise has election links, toggle checked and elections pre-selected (NOTE: PromiseDetailOut does not currently include election_ids — pre-fill election picker with empty state; elections can be newly linked via the picker)
    - Edit notice paragraph shown below CardTitle per UI-SPEC.md
    - Eligibility gate same as PromiseSubmitPage (is_verified && account_age_days >= 7)
    - On submit: call apiClient.put(`/api/promises/${slug}`, editPayload); payload matches PromiseEditIn shape (no politician_id)
    - On success: navigate to /promises?submitted=1 (same redirect as new submission)
    - Same error handling as PromiseSubmitPage (403, 422, network errors)
  </behavior>
  <action>
    Create frontend/src/pages/PromiseEditPage.tsx. Use useParams to get slug. Load data with usePromise(slug). Show loading skeleton when isLoading. Show 404 when !data. When data loaded, render same Card/two-section structure as PromiseSubmitPage with the following differences:
    - CardTitle text: exact Armenian for "Edit promise" from UI-SPEC.md Copywriting Contract
    - Below CardTitle: p.text-sm.text-zinc-500.mb-4 with exact edit notice text from UI-SPEC.md
    - Politician field in Section 1: render as a read-only display using Label + p (or span) showing promise.politician_name_hy — no Combobox component
    - Initialize all form state from promise data using useState with initializer: useState(promise.title_hy), etc.
    - Submit calls apiClient.put(`/api/promises/${slug}`, { title_hy, quote_hy, source_url, made_date: made_date||null, expected_date: expected_date||null, description_hy: description_hy||null, election_ids: linkElections ? selectedElectionIds : [] })
    - Submit button label: exact Armenian for "Submit edit" from UI-SPEC.md Copywriting Contract

    In App.tsx: add import for PromiseEditPage; add Route path="/promises/:slug/edit" inside RequireAuth block (alongside the existing /promises/new route).
  </action>
  <verify>
    <automated>cd C:\Users\MesropTarkhanyan\PycharmProjects\khostumner\frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - frontend/src/pages/PromiseEditPage.tsx exists
    - App.tsx contains Route path="/promises/:slug/edit" inside RequireAuth
    - npm run build exits 0 without TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Edit link on PromiseDetailPage</name>
  <files>
    frontend/src/pages/PromiseDetailPage.tsx
  </files>
  <read_first>
    - frontend/src/pages/PromiseDetailPage.tsx — current content; locate the source links section (after the metadata div); add edit link after the source links div
    - .planning/phases/05-promise-submission/05-UI-SPEC.md — Edit link on PromiseDetailPage section: location "bottom of promise detail after source links", visibility "authenticated users only", appearance "text-sm text-blue-600 underline", label "Խmbragrurel"
    - frontend/src/hooks/useAuth.ts — useAuth returns { state: { user: UserRead | null } }
  </read_first>
  <behavior>
    - Edit link visible ONLY when state.user is not null (any logged-in user, no eligibility check on the link itself)
    - Link renders as: Link to={`/promises/${data.slug}/edit`} className="text-sm text-blue-600 underline"
    - Link label: exact Armenian "Խmbragrurel" from UI-SPEC.md Copywriting Contract — use the exact value from the file
    - Link positioned after the source links div (after the Աղбйур and Wayback Machine buttons row)
  </behavior>
  <action>
    In PromiseDetailPage.tsx: import useAuth from "@/hooks/useAuth". Add const { state } = useAuth() at the top of the component body. After the source links div (after the Wayback Machine conditional), add: {state.user && ( Link to={`/promises/${data.slug}/edit`} className="text-sm text-blue-600 underline mt-4 inline-block" with Armenian "edit" label from UI-SPEC.md )}
  </action>
  <verify>
    <automated>cd C:\Users\MesropTarkhanyan\PycharmProjects\khostumner\frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - PromiseDetailPage.tsx imports useAuth
    - Conditional edit link present in JSX with correct classes
    - npm run build exits 0
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    PromiseEditPage at /promises/:slug/edit with pre-filled form, read-only politician, edit notice, PUT submission. Edit link on PromiseDetailPage for logged-in users.
  </what-built>
  <how-to-verify>
    1. Visit /promises — find any promise, click it to open the detail page.
    2. Verify: when NOT logged in, no "Խmbragrurel" link is visible.
    3. Log in, return to the promise detail page — verify the "Խmbragrurel" link appears below the source buttons.
    4. Click the edit link — verify navigation to /promises/{slug}/edit.
    5. On the edit page verify:
       a. CardTitle reads "Խmbragrurel khostumǝ" (in Armenian)
       b. Edit notice paragraph appears below the title
       c. Politician name shown as read-only text (no combobox / no ability to change)
       d. All content fields pre-filled from the original promise (title, quote, source URL, dates if present)
       e. Same election picker toggle as submit page; unchecked by default
    6. For ineligible account: verify submit button replaced with eligibility gate message
    7. For eligible account: modify the title slightly, click submit — verify redirect to /promises?submitted=1 with success banner.
    8. Verify the original promise on /promises/{slug} still shows its old title (edit is pending, not live).
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → PUT /api/promises/:slug | User-supplied edit content; server verifies eligibility and looks up promise without approved-only filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15 | Tampering | PromiseEditPage — mutating live promise | mitigate | PUT endpoint only INSERTs into promise_edits (D-04, D-06); live row untouched until Phase 6 admin approval |
| T-05-16 | Spoofing | /promises/:slug/edit — unauthenticated access | mitigate | RequireAuth redirects to /login; server returns 401 without valid auth cookie |
| T-05-17 | Tampering | Edit of a rejected promise via direct URL | mitigate | Backend returns 404 for rejected promises in PUT endpoint (RESEARCH Pitfall 4); frontend shows 404 from usePromise (which queries approved-only), so user cannot reach the form for a rejected promise via normal navigation — double guard |
| T-05-18 | Repudiation | Edit submitted by ineligible user | mitigate | Server-side eligibility check (account_age_days >= 7) returns 403; client gate is display-only |
| T-05-SC | Tampering | No new package installs | accept | No npm installs in this plan |
</threat_model>

<verification>
Run after all tasks complete:
- cd frontend && npm run build — exits 0
- grep "slug/edit" frontend/src/App.tsx — match found
- grep "useAuth" frontend/src/pages/PromiseDetailPage.tsx — match found
- Human verification checkpoint passed (edit page pre-fills correctly, PUT submits, edit link visible only when logged in)
</verification>

<success_criteria>
- /promises/:slug/edit route exists inside RequireAuth in App.tsx
- PromiseEditPage pre-fills all fields from usePromise data
- Politician field is read-only (no combobox)
- Edit notice shown below CardTitle
- PUT /api/promises/:slug called on submit with PromiseEditIn-shaped payload (no politician_id)
- Successful edit redirects to /promises?submitted=1
- PromiseDetailPage shows edit link for logged-in users only
- npm run build exits 0
- Human checkpoint approved
</success_criteria>

<output>
Create `.planning/phases/05-promise-submission/05-05-SUMMARY.md` when done
</output>
