---
phase: 05-promise-submission
plan: 03
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - frontend/src/api/client.ts
  - frontend/src/contexts/AuthContext.tsx
  - frontend/src/hooks/usePoliticianSearch.ts
  - frontend/src/hooks/useElectionsAll.ts
  - frontend/src/components/ui/command.tsx
  - frontend/src/components/ui/combobox.tsx
  - frontend/src/components/ui/checkbox.tsx
  - frontend/src/components/ui/radio-group.tsx
  - frontend/src/components/ui/label.tsx
autonomous: true
requirements:
  - PROM-03
  - PROM-04
  - ELEC-03

must_haves:
  truths:
    - "apiClient.post() sends credentials: include and x-csrftoken header"
    - "apiClient.put() sends credentials: include and x-csrftoken header"
    - "getCsrfToken() helper is defined in client.ts and exported"
    - "AuthContext UserRead type includes account_age_days: number field"
    - "usePoliticianSearch hook loads all politicians once (per_page=100) for client-side filter"
    - "useElectionsAll hook loads all elections for client-side level-filter"
    - "shadcn combobox, command, checkbox, radio-group, label components are generated and importable"
  artifacts:
    - path: "frontend/src/api/client.ts"
      provides: "post() and put() methods with CSRF + credentials"
      contains: "getCsrfToken"
    - path: "frontend/src/contexts/AuthContext.tsx"
      provides: "UserRead type with account_age_days"
      contains: "account_age_days"
    - path: "frontend/src/hooks/usePoliticianSearch.ts"
      provides: "Hook that loads politicians for combobox"
      exports: ["usePoliticianSearch"]
    - path: "frontend/src/hooks/useElectionsAll.ts"
      provides: "Hook that loads all elections for picker"
      exports: ["useElectionsAll"]
    - path: "frontend/src/components/ui/combobox.tsx"
      provides: "shadcn Combobox component"
      exports: ["Combobox"]
    - path: "frontend/src/components/ui/checkbox.tsx"
      provides: "shadcn Checkbox component"
      exports: ["Checkbox"]
    - path: "frontend/src/components/ui/radio-group.tsx"
      provides: "shadcn RadioGroup component"
      exports: ["RadioGroup", "RadioGroupItem"]
    - path: "frontend/src/components/ui/label.tsx"
      provides: "shadcn Label component"
      exports: ["Label"]
  key_links:
    - from: "frontend/src/pages/PromiseSubmitPage.tsx"
      to: "frontend/src/api/client.ts"
      via: "apiClient.post()"
      pattern: "apiClient.post"
    - from: "frontend/src/pages/PromiseSubmitPage.tsx"
      to: "frontend/src/contexts/AuthContext.tsx"
      via: "useAuth().state.user.account_age_days"
      pattern: "account_age_days"
---

<objective>
Wave 1b — Frontend infrastructure: extend apiClient with post/put methods, add account_age_days to UserRead, create usePoliticianSearch and useElectionsAll hooks, and generate shadcn UI components needed by the form pages. Runs in parallel with plan 05-02.

Purpose: Delivers all reusable frontend primitives that PromiseSubmitPage (plan 05-04) and PromiseEditPage (plan 05-05) depend on. No page routes yet.
Output: Extended apiClient, updated UserRead type, two new hooks, five new shadcn UI component files.
</objective>

<execution_context>
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-promise-submission/05-CONTEXT.md
@.planning/phases/05-promise-submission/05-RESEARCH.md
@.planning/phases/05-promise-submission/05-UI-SPEC.md
@.planning/phases/05-promise-submission/05-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/api/client.ts (current — only has get()):
const API_BASE = import.meta.env.VITE_API_URL ?? "http://localhost:8000"
const apiClient = { async get<T>(path: string): Promise<T> { ... } }
export { apiClient }

getCsrfToken pattern from frontend/src/pages/LoginPage.tsx:
function getCsrfToken(): string {
  return document.cookie.split("; ").find(r => r.startsWith("csrftoken="))?.split("=")[1] ?? ""
}

From frontend/src/contexts/AuthContext.tsx (current UserRead — missing account_age_days):
export type UserRead = { id: string; email: string; display_name: string; is_active: boolean; is_verified: boolean; role: string }

From frontend/src/hooks/usePoliticians.ts (pattern to replicate for usePoliticianSearch):
- Uses useQuery, apiClient.get, PaginatedResponse<PoliticianOut>
- Query key: ["politicians", { page, perPage, party, level }]
- Returns { data, isLoading, isError }

From frontend/src/hooks/useElections.ts (pattern to replicate for useElectionsAll):
- Uses useQuery, apiClient.get, PaginatedResponse<ElectionWithCountOut>
- Query key: ["elections", { page }]
- Returns { data, isLoading, isError }

From frontend/src/types/index.ts:
- PoliticianOut: { id: string; name_hy: string; slug: string; ... }
- ElectionOut: { id: string; name_hy: string; slug: string; election_date: string; level: "national" | "local" | "referendum"; description_hy: string | null }
- ElectionWithCountOut extends ElectionOut: { promise_count: number }

From frontend/src/components/ui/ (existing — DO NOT reinstall or modify):
- card.tsx, badge.tsx, button.tsx, select.tsx, pagination.tsx, input.tsx, separator.tsx

shadcn CLI command for new components (npx shadcn@latest add — uses .npmrc legacy-peer-deps):
- command.tsx: npx shadcn@latest add command --yes
- checkbox.tsx: npx shadcn@latest add checkbox --yes
- radio-group.tsx: npx shadcn@latest add radio-group --yes
- label.tsx: npx shadcn@latest add label --yes
- combobox.tsx: shadcn does not have a CLI-addable combobox; must be created manually as a reusable component wrapping Popover + Command (see RESEARCH.md Pattern 5 and UI-SPEC.md Component Inventory)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend apiClient + update AuthContext UserRead + generate shadcn components</name>
  <files>
    frontend/src/api/client.ts
    frontend/src/contexts/AuthContext.tsx
    frontend/src/components/ui/command.tsx
    frontend/src/components/ui/checkbox.tsx
    frontend/src/components/ui/radio-group.tsx
    frontend/src/components/ui/label.tsx
  </files>
  <read_first>
    - frontend/src/api/client.ts — current content; post() and put() methods appended; getCsrfToken() defined once here and removed from LoginPage.tsx (or left in both — keeping it in both is acceptable for Phase 5)
    - frontend/src/contexts/AuthContext.tsx — current UserRead type; add account_age_days: number field; also update AuthAction SET_USER payload type to include the new field
    - frontend/src/pages/LoginPage.tsx — getCsrfToken() defined here; check if it should be moved to client.ts or duplicated; for simplicity keep it in both files (no breakage)
    - .planning/phases/05-promise-submission/05-RESEARCH.md — Pattern 4 (post method implementation) and RESEARCH.md Standard Stack (shadcn CLI commands)
    - frontend/package.json — verify Radix deps are installed (from plan 05-01) before running shadcn CLI
  </read_first>
  <action>
    Update frontend/src/api/client.ts: Define getCsrfToken() function (reads csrftoken from document.cookie). Add post<T>(path: string, body: unknown): Promise<T> method with credentials: "include", Content-Type: "application/json", x-csrftoken: getCsrfToken() header, body: JSON.stringify(body). Throw { status, detail } object on non-ok responses (same pattern as RESEARCH.md Pattern 4). Add put<T>(path: string, body: unknown): Promise<T> with identical headers but method: "PUT". Export getCsrfToken as named export.

    Update frontend/src/contexts/AuthContext.tsx: Add account_age_days: number field to UserRead type. No other changes to reducer or AuthProvider.

    Run shadcn CLI to generate UI components (from frontend/ directory):
    cd frontend && npx shadcn@latest add command --yes
    cd frontend && npx shadcn@latest add checkbox --yes
    cd frontend && npx shadcn@latest add radio-group --yes
    cd frontend && npx shadcn@latest add label --yes

    If shadcn CLI fails for any component, manually create the file using the standard shadcn source from their GitHub (the components follow a predictable pattern using the installed Radix primitives). The files go into frontend/src/components/ui/.
  </action>
  <verify>
    <automated>cd C:\Users\MesropTarkhanyan\PycharmProjects\khostumner\frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - frontend/src/api/client.ts exports getCsrfToken, and apiClient has post() and put() methods
    - frontend/src/contexts/AuthContext.tsx UserRead type includes account_age_days: number
    - command.tsx, checkbox.tsx, radio-group.tsx, label.tsx exist in frontend/src/components/ui/
    - npm run build exits 0 (TypeScript type-checks and bundles successfully)
  </done>
</task>

<task type="auto">
  <name>Task 2: usePoliticianSearch hook + useElectionsAll hook + combobox.tsx component</name>
  <files>
    frontend/src/hooks/usePoliticianSearch.ts
    frontend/src/hooks/useElectionsAll.ts
    frontend/src/components/ui/combobox.tsx
  </files>
  <read_first>
    - frontend/src/hooks/usePoliticians.ts — exact pattern to replicate for usePoliticianSearch (useQuery, apiClient.get, PaginatedResponse)
    - frontend/src/hooks/useElections.ts — exact pattern to replicate for useElectionsAll
    - frontend/src/types/index.ts — PoliticianOut and ElectionOut interfaces; ElectionWithCountOut for useElectionsAll return type
    - frontend/src/components/ui/command.tsx — just generated; import it in combobox.tsx
    - .planning/phases/05-promise-submission/05-UI-SPEC.md — Combobox interaction states: trigger placeholder "Ընտրել քաղաքական գործչին", empty state "Ոչ մի արդյունք", selected state shows name_hy, Check icon on selected item
    - .planning/phases/05-promise-submission/05-RESEARCH.md — Pattern 5 (shadcn Combobox implementation)
  </read_first>
  <action>
    Create frontend/src/hooks/usePoliticianSearch.ts: useQuery key ["politicians-all"] fetching /api/politicians?page=1&per_page=100. Returns { politicians: PoliticianOut[], isLoading }. The hook loads all politicians once on mount and the component filters client-side via a searchQuery string. Per RESEARCH.md open question resolution (D-11): client-side filter is sufficient for v1 politician counts.

    Create frontend/src/hooks/useElectionsAll.ts: useQuery key ["elections-all"] fetching /api/elections?page=1&per_page=100. Returns { elections: ElectionOut[], isLoading }. Returns plain ElectionOut[] (strip promise_count). The PromiseSubmitPage filters client-side by level.

    Create frontend/src/components/ui/combobox.tsx: A controlled generic Combobox component. Props interface: items: { value: string; label: string }[], value: string, onChange: (value: string) => void, placeholder?: string, emptyText?: string, className?: string. Uses Popover + PopoverTrigger + PopoverContent + Command + CommandInput + CommandList + CommandEmpty + CommandItem from the newly-generated components. Trigger is a Button (variant="outline", role="combobox") full-width with ChevronDown icon (from lucide-react) on the right. Selected item shows a Check icon (from lucide-react). Follow exact structure from UI-SPEC.md Component Inventory Combobox section. Export as named export Combobox.
  </action>
  <verify>
    <automated>cd C:\Users\MesropTarkhanyan\PycharmProjects\khostumner\frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - usePoliticianSearch.ts exports usePoliticianSearch, returns { politicians: PoliticianOut[], isLoading }
    - useElectionsAll.ts exports useElectionsAll, returns { elections: ElectionOut[], isLoading }
    - combobox.tsx exports Combobox component
    - npm run build exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API POST/PUT | CSRF token must be included on all non-safe methods |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | apiClient.post — missing CSRF header | mitigate | getCsrfToken() included in post() and put() methods; starlette_csrf on backend enforces the header (Phase 4) |
| T-05-10 | Tampering | apiClient.post — missing credentials: include | mitigate | Both post() and put() methods include credentials: "include" (RESEARCH.md Pitfall 5) |
| T-05-SC | Tampering | shadcn CLI install | accept | shadcn CLI only generates code from official registry; no npm install of new packages beyond those already installed in plan 05-01 |
</threat_model>

<verification>
Run after both tasks complete:
- cd frontend && npm run build — exits 0, TypeScript clean
- grep "getCsrfToken" frontend/src/api/client.ts — match found
- grep "account_age_days" frontend/src/contexts/AuthContext.tsx — match found
- ls frontend/src/components/ui/combobox.tsx frontend/src/components/ui/checkbox.tsx frontend/src/components/ui/radio-group.tsx frontend/src/components/ui/label.tsx frontend/src/components/ui/command.tsx — all exist
- ls frontend/src/hooks/usePoliticianSearch.ts frontend/src/hooks/useElectionsAll.ts — both exist
</verification>

<success_criteria>
- apiClient.post() and apiClient.put() both exist with credentials + CSRF headers
- UserRead.account_age_days: number present in AuthContext.tsx
- usePoliticianSearch returns all politicians loaded at per_page=100
- useElectionsAll returns all elections
- 5 shadcn components exist in frontend/src/components/ui/: combobox.tsx, command.tsx, checkbox.tsx, radio-group.tsx, label.tsx
- npm run build exits 0
</success_criteria>

<output>
Create `.planning/phases/05-promise-submission/05-03-SUMMARY.md` when done
</output>
