---
phase: 02-politicians-parties-elections-browsing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/20260522_000001_add_party_slug.py
  - backend/app/models/parties.py
  - backend/app/services/seed.py
  - backend/app/schemas/common.py
  - backend/app/schemas/promises.py
  - backend/app/schemas/__init__.py
  - frontend/package.json
  - frontend/src/components/ui/card.tsx
  - frontend/src/components/ui/badge.tsx
  - frontend/src/components/ui/pagination.tsx
  - frontend/src/components/ui/button.tsx
  - frontend/src/components/ui/select.tsx
  - frontend/src/components/Layout.tsx
  - frontend/src/App.tsx
  - frontend/src/types/index.ts
  - backend/tests/test_politicians.py
  - backend/tests/test_parties.py
  - backend/tests/test_elections.py
  - frontend/src/pages/PersonsPage.test.tsx
autonomous: true
requirements:
  - POLS-01
  - POLS-02
  - POLS-03
  - ELEC-01
  - ELEC-02

must_haves:
  truths:
    - "Party model has a slug column (NOT NULL, UNIQUE) in the database"
    - "PaginatedResponse[T] generic schema exists and is importable from app.schemas.common"
    - "PromiseStubOut schema exists with id, slug, title_hy, quote_hy, resolved_status fields"
    - "shadcn/ui Card, Badge, Pagination, Button, Select components exist in frontend/src/components/ui/"
    - "Layout component renders the full nav header (5 links per D-02) and wraps child pages via <Outlet />"
    - "App.tsx registers routes for /persons, /persons/:slug, /parties/:slug, /elections, /elections/:slug"
    - "All 5 Phase requirement IDs have at least one failing test stub in Wave 0 test files"
    - "frontend/src/types/index.ts exports PaginatedResponse<T>, PoliticianOut, PartyOut, ElectionOut, ElectionWithCountOut, PromiseStubOut"
  artifacts:
    - path: "backend/alembic/versions/20260522_000001_add_party_slug.py"
      provides: "Alembic migration adding slug to parties table"
      contains: "add_column.*parties.*slug"
    - path: "backend/app/schemas/common.py"
      provides: "PaginatedResponse generic model"
      exports: ["PaginatedResponse"]
    - path: "backend/app/schemas/promises.py"
      provides: "PromiseStubOut schema"
      exports: ["PromiseStubOut"]
    - path: "frontend/src/components/Layout.tsx"
      provides: "Shared site layout with nav"
      contains: "Outlet"
    - path: "frontend/src/types/index.ts"
      provides: "All frontend TypeScript interfaces"
      exports: ["PaginatedResponse", "PoliticianOut", "PartyOut", "ElectionOut", "ElectionWithCountOut", "PromiseStubOut"]
    - path: "backend/tests/test_politicians.py"
      provides: "Wave 0 test stubs for POLS-01, POLS-02"
    - path: "backend/tests/test_parties.py"
      provides: "Wave 0 test stubs for POLS-03"
    - path: "backend/tests/test_elections.py"
      provides: "Wave 0 test stubs for ELEC-01, ELEC-02"
    - path: "frontend/src/pages/PersonsPage.test.tsx"
      provides: "Wave 0 frontend test stub for POLS-02"
  key_links:
    - from: "App.tsx"
      to: "Layout.tsx"
      via: "<Route element={<Layout />}> parent route"
      pattern: "element=.*Layout"
    - from: "backend/app/schemas/politicians.py (future)"
      to: "backend/app/schemas/common.py"
      via: "PaginatedResponse[PoliticianOut] import"
      pattern: "from app.schemas.common import PaginatedResponse"
    - from: "alembic migration"
      to: "backend/app/models/parties.py"
      via: "slug column must be reflected in model"
      pattern: "slug.*Mapped.*str"
---

<objective>
Wave 0 scaffolding: establishes every shared contract that plans 02–04 depend on before implementation begins. Delivers the Party slug migration, shared backend schemas, shadcn/ui component installs, frontend type contracts, shared Layout with nav, App.tsx routing skeleton, and all Wave 0 test stubs.

Purpose: Plans 02–04 must never wait on contracts being defined mid-execution. Every import path, type interface, and test file referenced by later plans exists after this plan completes.
Output: Migration file, 2 backend schemas, 5 shadcn/ui component files, Layout.tsx, updated App.tsx, updated types/index.ts, 4 test stub files.
</objective>

<execution_context>
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-politicians-parties-elections-browsing/02-CONTEXT.md
@.planning/phases/02-politicians-parties-elections-browsing/02-RESEARCH.md
@.planning/phases/02-politicians-parties-elections-browsing/02-PATTERNS.md
@.planning/phases/02-politicians-parties-elections-browsing/02-UI-SPEC.md

<interfaces>
<!-- Source of truth for existing code the executor will build on. -->

From backend/app/models/parties.py (current — NO slug field):
```python
class Party(Base):
    __tablename__ = "parties"
    id: Mapped[uuid.UUID] = mapped_column(primary_key=True, default=uuid.uuid4)
    name_hy: Mapped[str] = mapped_column(String(200), nullable=False)
    short_name_hy: Mapped[str | None] = mapped_column(String(50))
    logo_url: Mapped[str | None] = mapped_column(Text)
    founded_year: Mapped[int | None] = mapped_column(Integer)
    is_active: Mapped[bool] = mapped_column(Boolean, default=True)
    notes: Mapped[str | None] = mapped_column(Text)
```

From backend/alembic/versions/20260521_000001_initial_schema.py (existing migration header):
```python
revision: str = "20260521_000001"
down_revision: Union[str, None] = None
```

From backend/app/schemas/health.py (Pydantic pattern to replicate):
```python
from pydantic import BaseModel
class HealthResponse(BaseModel):
    status: str
```

From backend/app/models/promises.py (enums used in PromiseStubOut):
```python
class ResolvedStatus(str, enum.Enum):
    not_rated = "not_rated"
    kept = "kept"
    broken = "broken"
    in_progress = "in_progress"
    stalled = "stalled"
```

From backend/tests/conftest.py (test fixture to reuse):
```python
@pytest.fixture
async def client():
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as ac:
        yield ac
```

From frontend/src/App.tsx (current — to be updated):
```tsx
import { Routes, Route } from "react-router-dom"
import HomePage from "@/pages/HomePage"
import NotFoundPage from "@/pages/NotFoundPage"
export default function App() {
  return (
    <Routes>
      <Route path="/" element={<HomePage />} />
      <Route path="*" element={<NotFoundPage />} />
    </Routes>
  )
}
```

From frontend/src/types/index.ts (current — to be extended):
```typescript
export interface HealthResponse {
  status: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add slug to Party model and Alembic migration; update seed.py with explicit party slugs</name>
  <files>
    backend/alembic/versions/20260522_000001_add_party_slug.py
    backend/app/models/parties.py
    backend/app/services/seed.py
  </files>
  <read_first>
    - backend/app/models/parties.py — current Party model (no slug field; add it)
    - backend/alembic/versions/20260521_000001_initial_schema.py — existing migration; use revision ID as down_revision
    - backend/app/services/seed.py — seed Party creation; add slug to all 4 party insertions
  </read_first>
  <behavior>
    - After migration runs: SELECT slug FROM parties WHERE slug IS NULL returns 0 rows
    - After migration runs: SELECT COUNT(*) FROM parties GROUP BY slug returns all groups of size 1 (unique)
    - Migration downgrade drops the slug column and unique constraint cleanly
    - backend/app/models/parties.py Party class has slug field: Mapped[str] = mapped_column(String(200), unique=True, nullable=False)
    - seed.py Party instantiations include explicit slug values (e.g., civil_contract.slug = "qaghaqaciakan-paymanagir")
  </behavior>
  <action>
    Create backend/alembic/versions/20260522_000001_add_party_slug.py with:
    - revision = "20260522_000001", down_revision = "20260521_000001"
    - upgrade(): op.add_column("parties", sa.Column("slug", sa.String(200), nullable=True)); then op.execute to set slug for the 4 seed parties by name_hy match using explicit ASCII slugs; then op.alter_column("parties", "slug", nullable=False); then op.create_unique_constraint("uq__parties__slug", "parties", ["slug"])
    - The 4 explicit slug values to SET in the migration UPDATE: "Քաղաքացիական պայմանագիր" -> "qaghaqaciakan-paymanagir", "Հայ Հեղափոխական Դաշնակցություն" -> "hay-heghapokhakan-dashnaktsutyun", "Բարգավաճ Հայաստան" -> "bargavaj-hayastan", "Ազգային ժողով" (or the 4th party's name as seen in seed.py) -> read seed.py to confirm the 4th party name and derive the slug
    - downgrade(): op.drop_constraint("uq__parties__slug", "parties", type_="unique"); op.drop_column("parties", "slug")

    Update backend/app/models/parties.py: add slug field after notes: slug: Mapped[str] = mapped_column(String(200), unique=True, nullable=False)

    Update backend/app/services/seed.py: add slug= parameter to each of the 4 Party(...) constructor calls using the same ASCII slug values as in the migration UPDATE. This ensures re-runs after a fresh DB also have slugs set.

    Do NOT use regexp_replace in the migration — only explicit name-to-slug mapping for the known 4 seed parties. Any future party must set slug explicitly.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_schema.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - backend/alembic/versions/20260522_000001_add_party_slug.py exists with correct revision chain
    - backend/app/models/parties.py Party class includes slug: Mapped[str]
    - backend/app/services/seed.py all 4 Party() calls include slug= argument
    - test_schema.py passes (schema integrity check)
  </done>
</task>

<task type="auto">
  <name>Task 2: Install shadcn/ui peer deps + generate components; create shared backend schemas (common.py, promises.py) and backend/app/schemas/__init__.py</name>
  <files>
    frontend/package.json
    frontend/src/components/ui/card.tsx
    frontend/src/components/ui/badge.tsx
    frontend/src/components/ui/pagination.tsx
    frontend/src/components/ui/button.tsx
    frontend/src/components/ui/select.tsx
    backend/app/schemas/common.py
    backend/app/schemas/promises.py
    backend/app/schemas/__init__.py
  </files>
  <read_first>
    - frontend/package.json — current deps; verify @radix-ui/react-slot, lucide-react, class-variance-authority, clsx, tailwind-merge are NOT already listed before installing
    - frontend/components.json — confirm shadcn config (zinc base, CSS variables, @/components alias)
    - backend/app/schemas/health.py — Pydantic pattern baseline (no from_attributes needed for non-ORM schemas)
    - backend/app/models/promises.py — ResolvedStatus enum import path for PromiseStubOut
  </read_first>
  <action>
    Step 1 — shadcn peer deps (run from frontend/ directory):
    npm install @radix-ui/react-slot lucide-react class-variance-authority clsx tailwind-merge

    Step 2 — shadcn component generation (run from frontend/ directory):
    npx shadcn@latest add card badge pagination button select --yes

    This copies source files into frontend/src/components/ui/. Do not manually create these files.

    Step 3 — Create backend/app/schemas/common.py:
    - from typing import Generic, TypeVar; from pydantic import BaseModel
    - T = TypeVar("T")
    - class PaginatedResponse(BaseModel, Generic[T]): fields: items: list[T], total: int, page: int, per_page: int, pages: int
    - No model_config needed (not ORM-mapped; constructed from Python values)

    Step 4 — Create backend/app/schemas/promises.py:
    - from pydantic import BaseModel, ConfigDict; import uuid; from app.models.promises import ResolvedStatus
    - class PromiseStubOut(BaseModel): model_config = ConfigDict(from_attributes=True); fields: id: uuid.UUID, slug: str, title_hy: str, quote_hy: str, resolved_status: ResolvedStatus

    Step 5 — Create backend/app/schemas/__init__.py as empty file (makes schemas a package, enabling "from app.schemas.common import PaginatedResponse" imports in routers).
  </action>
  <verify>
    <automated>ls frontend/src/components/ui/ && cd backend && python -c "from app.schemas.common import PaginatedResponse; from app.schemas.promises import PromiseStubOut; print('OK')"</automated>
  </verify>
  <done>
    - frontend/src/components/ui/ contains card.tsx, badge.tsx, pagination.tsx, button.tsx, select.tsx
    - frontend/package.json lists @radix-ui/react-slot, lucide-react, class-variance-authority, clsx, tailwind-merge in dependencies
    - backend/app/schemas/common.py imports successfully; PaginatedResponse[str] is constructable
    - backend/app/schemas/promises.py imports successfully; PromiseStubOut has all 5 declared fields
    - backend/app/schemas/__init__.py exists
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Layout component, update App.tsx routing skeleton, update frontend types, and create Wave 0 test stubs</name>
  <files>
    frontend/src/components/Layout.tsx
    frontend/src/App.tsx
    frontend/src/types/index.ts
    backend/tests/test_politicians.py
    backend/tests/test_parties.py
    backend/tests/test_elections.py
    frontend/src/pages/PersonsPage.test.tsx
  </files>
  <read_first>
    - frontend/src/App.tsx — current routing; must preserve existing / and * routes while wrapping in Layout
    - frontend/src/pages/NotFoundPage.tsx — existing page used by stub routes
    - frontend/src/pages/HomePage.tsx — existing page that goes under Layout wrapper
    - frontend/src/types/index.ts — current content (single HealthResponse); extend without removing it
    - backend/tests/conftest.py — shared client fixture; use it in new test files
    - backend/tests/test_health.py — test pattern to replicate in stub files
    - .planning/phases/02-politicians-parties-elections-browsing/02-UI-SPEC.md — nav link copy and Layout spec (lines 123–143)
  </read_first>
  <action>
    1. Create frontend/src/components/Layout.tsx:
    - Import Link, NavLink, Outlet from "react-router-dom"
    - Outer div: "min-h-screen bg-zinc-50"
    - header: "sticky top-0 z-10 bg-white border-b border-zinc-200"
    - nav: "max-w-7xl mx-auto px-4 flex items-center gap-6 h-14"
    - Logo: NavLink to="/" className="font-bold text-lg text-zinc-900" text: "Խոստումներ"
    - Nav links using NavLink with className callback — active: "text-sm font-semibold text-zinc-900", inactive: "text-sm text-zinc-600 hover:text-zinc-900"
    - Links (in order per D-02): to="/fulfilled" "Կատարված", to="/unfulfilled" "Չկատարված", to="/persons" "Պերսոններ", to="/elections" "Ընտրություններ"
    - main: "max-w-7xl mx-auto px-4 py-8" containing {<Outlet />}

    2. Update frontend/src/App.tsx:
    - Keep existing imports for HomePage and NotFoundPage
    - Add imports for Layout and the 5 new page components (PersonsPage, PoliticianProfilePage, PartyPage, ElectionsListPage, ElectionDetailPage) — these page files do not exist yet; import them anyway (plans 02–04 create them). TypeScript will error until those plans run; that is acceptable for a stub routing skeleton.
    - Actually: to avoid TS errors in CI, create minimal stub placeholder files for the 5 pages in this task (each exports a default function returning <div>...</div>). Plans 02–04 will replace them.
    - Routing structure: <Routes><Route element={<Layout />}> wrapping all routes including /, /persons, /persons/:slug, /parties/:slug, /elections, /elections/:slug, and * (NotFoundPage). Keep NotFoundPage as the wildcard child.

    3. Update frontend/src/types/index.ts:
    - Preserve existing HealthResponse interface
    - Add: PaginatedResponse<T> generic interface, PoliticianOut, PartyOut, PartyListItemOut, ElectionOut, ElectionWithCountOut, PromiseStubOut
    - Use exact field names from PATTERNS.md types/index.ts section. For level fields use string literal union types.
    - PoliticianOut.level: "national" | "local" | "party"
    - ElectionOut.level: "national" | "local" | "referendum"
    - PromiseStubOut.resolved_status: "not_rated" | "kept" | "broken" | "in_progress" | "stalled"

    4. Create backend/tests/test_politicians.py (failing stubs — RED state is correct):
    - Import pytest, use conftest client fixture
    - test_list_politicians(client): GET /api/politicians — assert 200, body has "items"/"total"/"page"/"per_page"/"pages" keys
    - test_filter_by_party(client): GET /api/politicians?party=00000000-0000-0000-0000-000000000000 — assert 200
    - test_filter_by_level(client): GET /api/politicians?level=national — assert 200
    - test_get_politician_by_slug(client): GET /api/politicians/does-not-exist-yet — assert one of [200, 404] (stub accepts both; will be tightened in plan 02)
    - test_politician_not_found(client): GET /api/politicians/zzz-nonexistent — assert 404
    - Mark all tests with @pytest.mark.asyncio

    5. Create backend/tests/test_parties.py (failing stubs):
    - test_list_parties(client): GET /api/parties — assert 200
    - test_get_party(client): GET /api/parties/nonexistent-slug — assert 404

    6. Create backend/tests/test_elections.py (failing stubs):
    - test_list_elections(client): GET /api/elections — assert 200, body has "items" key
    - test_get_election(client): GET /api/elections/nonexistent-slug — assert 404
    - test_election_promises(client): GET /api/elections/nonexistent-slug/promises — assert 404

    7. Create frontend/src/pages/PersonsPage.test.tsx (stub — no router needed yet):
    - Import { render } from "@testing-library/react"; import PersonsPage from "./PersonsPage"
    - test("PersonsPage renders without crashing"): wrap in MemoryRouter + QueryClientProvider, render <PersonsPage />, assert truthy container
    - This test will fail until plan 02 creates the real PersonsPage — RED state is correct for Wave 0
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS" || true</automated>
  </verify>
  <done>
    - frontend/src/components/Layout.tsx exists with Outlet and all 5 nav links per D-02
    - frontend/src/App.tsx wraps all routes in Layout with the 6 route paths registered
    - frontend/src/types/index.ts exports PaginatedResponse, PoliticianOut, PartyOut, PartyListItemOut, ElectionOut, ElectionWithCountOut, PromiseStubOut
    - backend/tests/test_politicians.py exists with 5 test functions decorated @pytest.mark.asyncio
    - backend/tests/test_parties.py exists with 2 test functions
    - backend/tests/test_elections.py exists with 3 test functions
    - frontend/src/pages/PersonsPage.test.tsx exists
    - tsc --noEmit passes (0 TS errors) — stub page files keep TypeScript happy
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → filesystem | shadcn CLI writes component files into src/components/ui/; executor must verify output |
| migration → PostgreSQL | Alembic migration modifies live schema; downgrade path must be tested |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Alembic migration (slug column) | mitigate | Migration uses explicit slug values only (no regexp_replace on user content); nullable=True first, then NOT NULL after UPDATE ensures no data loss |
| T-02-SC | Tampering | npm install @radix-ui/react-slot lucide-react class-variance-authority clsx tailwind-merge | mitigate | All packages verified [VERIFIED: npm registry] in RESEARCH.md Package Legitimacy Audit; no postinstall scripts confirmed |
| T-02-03 | Information Disclosure | Party slug in URL | accept | Slugs are public ASCII transliterations of party names — no sensitive data exposed |
</threat_model>

<verification>
After all 3 tasks complete:

1. Migration file chain: python -c "from alembic.config import Config; c = Config('alembic.ini'); print('OK')" — no import errors
2. Shared schemas importable: cd backend && python -c "from app.schemas.common import PaginatedResponse; from app.schemas.promises import PromiseStubOut; print('schemas OK')"
3. shadcn components present: ls frontend/src/components/ui/ | grep -E "card|badge|pagination|button|select"
4. Types compile: cd frontend && npx tsc --noEmit
5. Test stubs exist and are collected: cd backend && pytest tests/test_politicians.py tests/test_parties.py tests/test_elections.py --collect-only -q
6. Party model has slug: grep -n "slug" backend/app/models/parties.py
</verification>

<success_criteria>
- Party slug Alembic migration exists with correct down_revision = "20260521_000001"
- backend/app/models/parties.py Party class has slug: Mapped[str]
- backend/app/services/seed.py all 4 Party() calls include slug= argument
- backend/app/schemas/common.py exports PaginatedResponse[T]
- backend/app/schemas/promises.py exports PromiseStubOut with 5 fields
- frontend/src/components/ui/ has card.tsx, badge.tsx, pagination.tsx, button.tsx, select.tsx
- frontend/src/components/Layout.tsx uses Outlet and renders nav with 5 links (Խոստումներ, Կատարված, Չկատարված, Պերսոններ, Ընտրություններ)
- frontend/src/App.tsx wraps all routes in Layout and registers /persons, /persons/:slug, /parties/:slug, /elections, /elections/:slug
- frontend/src/types/index.ts exports 7 interfaces including PaginatedResponse<T> and ElectionWithCountOut
- 4 Wave 0 test stub files exist (test_politicians.py, test_parties.py, test_elections.py, PersonsPage.test.tsx)
- cd frontend && npx tsc --noEmit exits 0
</success_criteria>

<output>
Create `.planning/phases/02-politicians-parties-elections-browsing/02-01-SUMMARY.md` when done.
</output>
