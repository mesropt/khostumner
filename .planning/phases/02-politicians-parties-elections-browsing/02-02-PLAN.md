---
phase: 02-politicians-parties-elections-browsing
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - backend/app/schemas/politicians.py
  - backend/app/routers/politicians.py
  - backend/app/main.py
  - frontend/src/hooks/usePoliticians.ts
  - frontend/src/hooks/usePolitician.ts
  - frontend/src/hooks/useParties.ts
  - frontend/src/hooks/usePoliticianPromises.ts
  - frontend/src/components/Avatar.tsx
  - frontend/src/components/ResolvedStatusBadge.tsx
  - frontend/src/components/PromiseStub.tsx
  - frontend/src/components/PoliticianCard.tsx
  - frontend/src/components/PaginationControls.tsx
  - frontend/src/pages/PersonsPage.tsx
  - frontend/src/pages/PoliticianProfilePage.tsx
autonomous: true
requirements:
  - POLS-01
  - POLS-02

must_haves:
  truths:
    - "Visitor can open /persons and see a paginated grid of politician cards"
    - "Visitor can filter by party (select dropdown) and results update without page reload"
    - "Visitor can filter by level (national/local/party) and results update without page reload"
    - "Visitor can open /persons/:slug and see politician name, avatar, role, and linked promises list"
    - "GET /api/politicians returns {items, total, page, per_page, pages} with moderation-approved promise data"
    - "GET /api/politicians?party=<uuid>&level=national filters correctly"
    - "GET /api/politicians/{slug} returns politician detail; nonexistent slug returns 404"
    - "GET /api/politicians/{slug}/promises returns approved promises paginated with per_page=10"
  artifacts:
    - path: "backend/app/schemas/politicians.py"
      provides: "PoliticianOut Pydantic schema"
      exports: ["PoliticianOut"]
    - path: "backend/app/routers/politicians.py"
      provides: "3 endpoints: list, detail, promises"
      contains: "async def list_politicians"
    - path: "frontend/src/pages/PersonsPage.tsx"
      provides: "PersonsPage with filters + pagination"
      contains: "usePoliticians"
    - path: "frontend/src/pages/PoliticianProfilePage.tsx"
      provides: "Profile page with avatar, role, promises list"
      contains: "usePolitician"
  key_links:
    - from: "backend/app/routers/politicians.py"
      to: "backend/app/main.py"
      via: "include_router(politicians.router, prefix='/api')"
      pattern: "include_router.*politicians"
    - from: "frontend/src/pages/PersonsPage.tsx"
      to: "frontend/src/hooks/usePoliticians.ts"
      via: "usePoliticians({ page, party, level })"
      pattern: "usePoliticians"
    - from: "frontend/src/hooks/usePoliticians.ts"
      to: "GET /api/politicians"
      via: "apiClient.get('/api/politicians?...')"
      pattern: "api/politicians"
---

<objective>
Politicians browsing vertical slice: delivers the complete POLS-01 and POLS-02 features end-to-end — from the PostgreSQL query through FastAPI router to React page. After this plan, a visitor can browse /persons (paginated, filtered by party/level) and open any politician's profile page at /persons/:slug.

Purpose: First user-visible feature of Phase 2. Establishes the full-stack pattern that party (plan 03) and elections (plan 04) slices replicate.
Output: politicians router (3 endpoints), PoliticianOut schema, 5 frontend hooks/components/pages, PaginationControls, Avatar, PromiseStub, ResolvedStatusBadge.
</objective>

<execution_context>
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-politicians-parties-elections-browsing/02-CONTEXT.md
@.planning/phases/02-politicians-parties-elections-browsing/02-RESEARCH.md
@.planning/phases/02-politicians-parties-elections-browsing/02-PATTERNS.md
@.planning/phases/02-politicians-parties-elections-browsing/02-UI-SPEC.md
@.planning/phases/02-politicians-parties-elections-browsing/02-01-SUMMARY.md

<interfaces>
<!-- Key contracts executor needs. Extracted from codebase + plan 01 output. -->

From backend/app/routers/health.py (router pattern to replicate):
```python
from fastapi import APIRouter
router = APIRouter()
@router.get("/health")
async def health_check():
    return {"status": "ok"}
```

From backend/app/main.py (include_router call site):
```python
from app.routers import health
app.include_router(health.router)
```

From backend/app/models/politicians.py:
```python
class PoliticianLevel(str, enum.Enum):
    national = "national"
    local = "local"
    party = "party"

class Politician(Base):
    __tablename__ = "politicians"
    id: Mapped[uuid.UUID]
    name_hy: Mapped[str]
    slug: Mapped[str]  # unique
    photo_url: Mapped[str | None]
    position: Mapped[str | None]
    level: Mapped[PoliticianLevel]
    party_id: Mapped[uuid.UUID | None]
    bio_hy: Mapped[str | None]
    is_active: Mapped[bool]
    created_at: Mapped[datetime]
    created_by: Mapped[uuid.UUID | None]
```

From backend/app/models/promises.py:
```python
class ModerationStatus(str, enum.Enum):
    pending = "pending"; approved = "approved"; rejected = "rejected"

class Promise(Base):
    __tablename__ = "promises"
    id: Mapped[uuid.UUID]; title_hy: Mapped[str]; quote_hy: Mapped[str]
    politician_id: Mapped[uuid.UUID]; slug: Mapped[str]
    moderation_status: Mapped[ModerationStatus]
    resolved_status: Mapped[ResolvedStatus]
    created_at: Mapped[datetime]
```

From backend/app/schemas/common.py (created in plan 01):
```python
class PaginatedResponse(BaseModel, Generic[T]):
    items: list[T]; total: int; page: int; per_page: int; pages: int
```

From backend/app/schemas/promises.py (created in plan 01):
```python
class PromiseStubOut(BaseModel):
    model_config = ConfigDict(from_attributes=True)
    id: uuid.UUID; slug: str; title_hy: str; quote_hy: str
    resolved_status: ResolvedStatus
```

From backend/app/database.py:
```python
async def get_db() -> AsyncGenerator[AsyncSession, None]:
    async with AsyncSessionLocal() as session:
        try:
            yield session; await session.commit()
        except Exception: await session.rollback(); raise
```

From backend/tests/conftest.py:
```python
@pytest.fixture
async def client():
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as ac:
        yield ac
```

From frontend/src/hooks/useHealth.ts (hook pattern to replicate):
```typescript
import { useQuery } from "@tanstack/react-query"
import { apiClient } from "@/api/client"
export function useHealth() {
  const { data, isLoading, isError } = useQuery<HealthResponse>({
    queryKey: ["health"],
    queryFn: () => apiClient.get<HealthResponse>("/health"),
    retry: 3,
  })
  return { status: data?.status ?? null, isLoading, isError }
}
```

From frontend/src/types/index.ts (created in plan 01 — interfaces available):
- PaginatedResponse<T>, PoliticianOut, PartyOut, PartyListItemOut, PromiseStubOut
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Politicians backend — PoliticianOut schema + politicians router (3 endpoints) + main.py wiring + also add GET /api/parties list for filter dropdown</name>
  <files>
    backend/app/schemas/politicians.py
    backend/app/routers/politicians.py
    backend/app/main.py
  </files>
  <read_first>
    - backend/app/routers/health.py — router pattern (APIRouter, async def, return dict)
    - backend/app/main.py — include_router call site; import block at top; add politicians router here
    - backend/app/models/politicians.py — all Politician fields and PoliticianLevel enum
    - backend/app/models/promises.py — ModerationStatus enum for approved filter
    - backend/app/schemas/common.py — PaginatedResponse import path
    - backend/app/schemas/promises.py — PromiseStubOut import path
    - backend/app/database.py — get_db dependency import path
    - backend/tests/test_politicians.py — test stubs created in plan 01; these tests must pass GREEN after this task
    - backend/app/schemas/parties.py — does NOT exist yet; create a minimal PartyListItemOut here for the /api/parties list used by the filter dropdown on PersonsPage; full PartyOut is created in plan 03
  </read_first>
  <behavior>
    - GET /api/politicians returns 200 with body {"items": [...], "total": N, "page": 1, "per_page": 20, "pages": N}
    - GET /api/politicians?party=<invalid-uuid> returns 422 (FastAPI validates UUID type)
    - GET /api/politicians?level=national returns 200 with items where all items have level="national"
    - GET /api/politicians/{slug} with valid slug returns 200 with politician fields
    - GET /api/politicians/zzz-nonexistent returns 404 with detail="Politician not found"
    - GET /api/politicians/{slug}/promises returns 200 with paginated PromiseStubOut; only approved promises returned
    - GET /api/parties returns 200 with list of {id, name_hy, short_name_hy} for the Party filter dropdown (non-paginated)
    - pytest tests/test_politicians.py -x exits 0 (all 5 tests green)
  </behavior>
  <action>
    1. Create backend/app/schemas/politicians.py:
    - from pydantic import BaseModel, ConfigDict; import uuid; from app.models.politicians import PoliticianLevel
    - class PoliticianOut(BaseModel): model_config = ConfigDict(from_attributes=True)
    - Fields: id: uuid.UUID, name_hy: str, slug: str, photo_url: str | None, position: str | None, level: PoliticianLevel, party_id: uuid.UUID | None, bio_hy: str | None, is_active: bool

    2. Create backend/app/schemas/parties.py with just PartyListItemOut (Plan 03 adds PartyOut):
    - class PartyListItemOut(BaseModel): model_config = ConfigDict(from_attributes=True)
    - Fields: id: uuid.UUID, name_hy: str, short_name_hy: str | None

    3. Create backend/app/routers/politicians.py:
    - router = APIRouter(prefix="/politicians", tags=["politicians"])
    - GET "/" — list_politicians: filters is_active=True, optionally party (UUID), level (PoliticianLevel); count subquery; order by name_hy; PaginatedResponse[PoliticianOut]
    - GET "/{slug}" — get_politician: select by slug; 404 if not found; returns PoliticianOut
    - GET "/{slug}/promises" — get_politician_promises: 404 if politician not found; select Promise where politician_id=politician.id AND moderation_status=ModerationStatus.approved; per_page default 10; order by created_at desc; PaginatedResponse[PromiseStubOut]

    Also add a minimal parties router here (just GET /parties list) OR create backend/app/routers/parties.py with only the list endpoint now:
    - Create backend/app/routers/parties.py with router = APIRouter(prefix="/parties", tags=["parties"]) and GET "/" returning list[PartyListItemOut] of all active parties (select Party where is_active=True, order by name_hy) — NOT paginated, simple list for filter dropdown

    4. Update backend/app/main.py:
    - Add imports: from app.routers import politicians, parties
    - Add after health router: app.include_router(politicians.router, prefix="/api"); app.include_router(parties.router, prefix="/api")
    - Do NOT change CORS or anything else

    Key constraints:
    - Always filter ModerationStatus.approved on promise queries (CLAUDE.md rule + Pitfall 4)
    - Use Depends(get_db) on every handler that queries DB
    - Count query uses select(func.count()).select_from(stmt.subquery()) — not len(items)
    - SQLAlchemy: stmt.offset((page-1)*per_page).limit(per_page); results via .scalars().all()
    - PaginatedResponse pages formula: (total + per_page - 1) // per_page
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_politicians.py tests/test_parties.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    - backend/app/schemas/politicians.py exports PoliticianOut with model_config = ConfigDict(from_attributes=True)
    - backend/app/schemas/parties.py exports PartyListItemOut
    - backend/app/routers/politicians.py has 3 async route handlers
    - backend/app/routers/parties.py has GET / list endpoint
    - backend/app/main.py includes both routers with prefix="/api"
    - pytest tests/test_politicians.py tests/test_parties.py -x exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Shared reusable components (Avatar, ResolvedStatusBadge, PromiseStub, PoliticianCard, PaginationControls) + useParties hook</name>
  <files>
    frontend/src/components/Avatar.tsx
    frontend/src/components/ResolvedStatusBadge.tsx
    frontend/src/components/PromiseStub.tsx
    frontend/src/components/PoliticianCard.tsx
    frontend/src/components/PaginationControls.tsx
    frontend/src/hooks/useParties.ts
  </files>
  <read_first>
    - frontend/src/components/ui/badge.tsx — shadcn Badge component API (generated in plan 01); inspect props
    - frontend/src/components/ui/card.tsx — shadcn Card component API; inspect CardHeader, CardContent exports
    - frontend/src/components/ui/pagination.tsx — shadcn Pagination; inspect exported primitives
    - frontend/src/types/index.ts — PoliticianOut, PromiseStubOut, PartyListItemOut interfaces
    - .planning/phases/02-politicians-parties-elections-browsing/02-UI-SPEC.md — Avatar sizes, badge colors, card layout (lines 80–120, 160–170, 285–295)
    - frontend/src/hooks/useHealth.ts — hook pattern baseline
    - frontend/src/api/client.ts — apiClient.get signature
  </read_first>
  <action>
    1. Create frontend/src/components/Avatar.tsx:
    - Props: { photoUrl: string | null; nameHy: string; size?: "sm" | "md" | "lg" }
    - size classes: sm="w-8 h-8 text-xs", md="w-16 h-16 text-sm" (64px grid card), lg="w-24 h-24 text-lg" (96px profile hero)
    - If photoUrl: render <img src={photoUrl} alt={nameHy} className={`${sizeClass} rounded-full object-cover`} />
    - If null: <div className={`${sizeClass} flex items-center justify-center rounded-full bg-zinc-200 text-zinc-700 font-semibold`}>{getInitials(nameHy)}</div>
    - getInitials(nameHy: string): split on /\s+/; return first char of parts[0] + first char of parts[last] (Unicode-safe charAt(0))

    2. Create frontend/src/components/ResolvedStatusBadge.tsx:
    - Props: { status: PromiseStubOut["resolved_status"] }
    - STATUS_CONFIG map with all 5 statuses — exact color classes from UI-SPEC.md Color table:
      kept: bg-green-100 text-green-800 border-green-200, label "Կատարված"
      broken: bg-red-100 text-red-800 border-red-200, label "Չկատարված"
      in_progress: bg-yellow-100 text-yellow-800 border-yellow-200, label "Ընթացքի մեջ"
      stalled: bg-gray-100 text-gray-600 border-gray-200, label "Կասեցված"
      not_rated: bg-zinc-100 text-zinc-500 border-zinc-200, label "Չգնահատված"
    - Render: <span className={`inline-flex items-center px-2 py-0.5 rounded border text-xs font-medium ${config.className}`}>{config.label}</span>
    - Fallback to not_rated config if unknown status

    3. Create frontend/src/components/PromiseStub.tsx:
    - Props: { promise: PromiseStubOut }
    - Render as <Link to={`/promises/${promise.slug}`}> — full row clickable per D-07
    - Row: flex justify-between items-start, border-b border-zinc-100, py-3, hover:bg-zinc-50 cursor-pointer
    - Left: <p className="text-sm text-zinc-700 line-clamp-2">{truncated}</p> where truncated = promise.quote_hy.length > 120 ? promise.quote_hy.slice(0, 120) + "…" : promise.quote_hy
    - Right: <ResolvedStatusBadge status={promise.resolved_status} />

    4. Create frontend/src/components/PoliticianCard.tsx:
    - Props: { politician: PoliticianOut }
    - Use shadcn Card (CardContent only — no CardHeader needed for compact card)
    - Structure: <Link to={`/persons/${politician.slug}`}><Card className="hover:shadow-md transition-shadow cursor-pointer"><CardContent className="pt-4 flex flex-col items-center text-center gap-2"><Avatar photoUrl={politician.photo_url} nameHy={politician.name_hy} size="md" /><p className="font-semibold text-zinc-900 text-sm">{politician.name_hy}</p>{politician.position && <p className="text-xs text-zinc-500">{politician.position}</p>}</CardContent></Card></Link>
    - Do NOT inline party badge in this card — no party name available in PoliticianOut without a join; party_id is a UUID. Plans 03 can revisit once parties API is available.

    5. Create frontend/src/components/PaginationControls.tsx:
    - Props: { currentPage: number; totalPages: number }
    - Use useSearchParams from react-router-dom
    - goToPage(p: number): setSearchParams via callback preserving all other params
    - Render shadcn Pagination > PaginationContent with PaginationPrevious + page links + PaginationNext
    - Prev aria-disabled when currentPage <= 1; Next aria-disabled when currentPage >= totalPages
    - Show pages: if totalPages <= 7 show all; else show first 3, PaginationEllipsis, last 3
    - Wrap in <div className="flex justify-center mt-6">

    6. Create frontend/src/hooks/useParties.ts:
    - useQuery<PartyListItemOut[]> with queryKey: ["parties"]
    - queryFn: apiClient.get<PartyListItemOut[]>("/api/parties")
    - No placeholderData needed (small static list)
    - Returns { data, isLoading, isError }
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 errors"</automated>
  </verify>
  <done>
    - Avatar.tsx handles both photo and null case; getInitials splits on whitespace
    - ResolvedStatusBadge.tsx has STATUS_CONFIG for all 5 resolved_status values with Armenian labels and correct Tailwind color classes
    - PromiseStub.tsx truncates quote_hy at 120 chars with "…" and links to /promises/{slug}
    - PoliticianCard.tsx uses shadcn Card and links to /persons/{slug}
    - PaginationControls.tsx uses useSearchParams to store page in URL
    - useParties.ts fetches /api/parties and returns PartyListItemOut[]
    - tsc --noEmit exits 0
  </done>
</task>

<task type="auto">
  <name>Task 3: PersonsPage and PoliticianProfilePage — usePoliticians, usePolitician, usePoliticianPromises hooks + full page components</name>
  <files>
    frontend/src/hooks/usePoliticians.ts
    frontend/src/hooks/usePolitician.ts
    frontend/src/hooks/usePoliticianPromises.ts
    frontend/src/pages/PersonsPage.tsx
    frontend/src/pages/PoliticianProfilePage.tsx
  </files>
  <read_first>
    - frontend/src/hooks/useHealth.ts — hook pattern baseline (useQuery, typed response, error/loading)
    - frontend/src/api/client.ts — apiClient.get signature; query strings appended directly to path
    - frontend/src/pages/HomePage.tsx — page pattern baseline (isLoading/isError/data state rendering)
    - frontend/src/types/index.ts — PaginatedResponse<T>, PoliticianOut, PromiseStubOut interfaces (created in plan 01)
    - frontend/src/components/ — Avatar, ResolvedStatusBadge, PromiseStub, PoliticianCard, PaginationControls (created in task 2)
    - .planning/phases/02-politicians-parties-elections-browsing/02-UI-SPEC.md — PersonsPage layout (lines 145–170), PoliticianProfilePage layout (lines 173–196), filter interaction contract (lines 256–270), copywriting (lines 317–343)
    - .planning/phases/02-politicians-parties-elections-browsing/02-CONTEXT.md — D-04 (server-side filters), D-05 (two select dropdowns), D-09 (offset+limit page params), D-11 (shadcn Pagination)
  </read_first>
  <action>
    1. Create frontend/src/hooks/usePoliticians.ts:
    - Params: { page: number; perPage?: number; party?: string | null; level?: string | null }
    - useQuery<PaginatedResponse<PoliticianOut>>({ queryKey: ["politicians", { page, perPage, party, level }], ... })
    - queryFn builds URLSearchParams: always set page and per_page; conditionally set party and level if non-null
    - Fetch path: /api/politicians?${params}
    - placeholderData: keepPreviousData (import keepPreviousData from "@tanstack/react-query")
    - Do NOT write keepPreviousData: true (v4 syntax — Pitfall 5 in RESEARCH.md)

    2. Create frontend/src/hooks/usePolitician.ts:
    - Param: slug: string
    - useQuery<PoliticianOut>({ queryKey: ["politician", slug], queryFn: () => apiClient.get(`/api/politicians/${slug}`), enabled: Boolean(slug) })

    3. Create frontend/src/hooks/usePoliticianPromises.ts:
    - Params: { slug: string; page: number; perPage?: number }
    - useQuery<PaginatedResponse<PromiseStubOut>>({ queryKey: ["politician-promises", slug, { page, perPage }], ... })
    - queryFn: apiClient.get(`/api/politicians/${slug}/promises?page=${page}&per_page=${perPage ?? 10}`)
    - placeholderData: keepPreviousData
    - enabled: Boolean(slug)

    4. Create/replace frontend/src/pages/PersonsPage.tsx (plan 01 created a stub — replace with full implementation):
    - useSearchParams for page, party, level query params (per D-04)
    - usePoliticians({ page, party, level }) and useParties()
    - Filter row: two shadcn Select components (not native <select>); per D-05 and UI-SPEC Interaction Contracts
      Party Select: label "Կուսակցություն", default "Բոլոր կուսակցությունները"; options from partiesData
      Level Select: label "Մակարդակ", default "Բոլոր մակարդակները"; options Ազգային/Տեղական/Կուսակցական
      On value change: update query params AND reset page=1
    - Loading state: "animate-pulse" skeleton grid (6 cards, 2 rows of 3, bg-zinc-200 rounded)
    - Error state: AlertCircle icon (from lucide-react), "Տվյալները բեռնելու ժամանակ սխալ է տեղի ունեցել", retry link
    - Empty state (data.items.length === 0): "Ոչ մի պերսոն չի գտնվել" heading + "Փոխեք ֆիլտրի կարգավորումները..." body
    - Grid: "grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 mb-6" with PoliticianCard for each item
    - PaginationControls at bottom when data.pages > 1
    - Page heading: <h1 className="text-2xl font-semibold text-zinc-900 mb-6">Պերսոններ</h1>

    5. Create/replace frontend/src/pages/PoliticianProfilePage.tsx:
    - useParams<{ slug: string }>() to get slug
    - usePolitician(slug!) and usePoliticianPromises({ slug: slug!, page: promisePage })
    - promisePage from useSearchParams (separate param key "ppage" to avoid conflict with parent page param)
    - Loading: skeleton (circle 96px + 3 text lines + 5 promise stub skeletons)
    - 404/error: "Պերսոնը չի գտնվել" h1 + <Link to="/persons">Վերադառնալ պերսոնների ցուցակ</Link>
    - Hero section: Avatar size="lg" + name (h1 28px semibold) + position (zinc-500) — per UI-SPEC
    - Bio: if data.bio_hy, render <p className="text-sm text-zinc-700 max-w-prose mt-4">{data.bio_hy}</p>
    - Promises section: <h2 className="text-xl font-semibold text-zinc-900 mt-8 mb-4">Խոստումներ</h2>
    - List of PromiseStub components; empty state "Խոստումներ չեն գտնվել"
    - PaginationControls with promisePage and promisesData.pages
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run 2>&1 | tail -15</automated>
  </verify>
  <done>
    - usePoliticians.ts uses placeholderData: keepPreviousData (not keepPreviousData: true)
    - usePolitician.ts has enabled: Boolean(slug) guard
    - PersonsPage.tsx reads page/party/level from useSearchParams and resets page=1 on filter change
    - PersonsPage.tsx uses shadcn Select (not native select) for filter dropdowns per D-05 and UI-SPEC
    - PoliticianProfilePage.tsx shows 404 state when usePolitician returns error
    - npm test -- --run exits 0 (PersonsPage.test.tsx green after stub page replaced with real component)
    - tsc --noEmit exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET /api/politicians | Untrusted query params (page, per_page, party, level) cross from client to FastAPI |
| FastAPI → PostgreSQL | SQLAlchemy parameterized queries only; no string formatting into SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | GET /api/politicians?per_page=N | mitigate | per_page capped at le=100 via FastAPI Query(per_page, ge=1, le=100); values above 100 get 422 |
| T-02-05 | Tampering | GET /api/politicians?party=<arbitrary> | mitigate | FastAPI validates UUID type on party param; non-UUID strings return 422 automatically |
| T-02-06 | Information Disclosure | PromiseStubOut exposes promise data | mitigate | moderation_status=approved filter applied in every promise query; pending/rejected promises never returned |
| T-02-07 | Denial of Service | Deep pagination GET /api/politicians?page=999999 | accept | Offset-based pagination; large offsets are slow but not exploitable for data exfiltration; acceptable for Phase 2 read-only MVP |
| T-02-08 | Tampering | SQL injection via slug path param | mitigate | SQLAlchemy ORM parameterized query: select(Politician).where(Politician.slug == slug) — no string interpolation |
</threat_model>

<verification>
After all 3 tasks complete:

1. Backend tests green: cd backend && pytest tests/test_politicians.py tests/test_parties.py -v exits 0
2. Endpoint shape: the following curl (against a running backend) returns correct envelope:
   GET /api/politicians -> {"items":[], "total":0, "page":1, "per_page":20, "pages":0} (empty if no DB; 200 always)
3. 404 behavior: GET /api/politicians/nonexistent-slug -> 404 {"detail":"Politician not found"}
4. Schema import: cd backend && python -c "from app.schemas.politicians import PoliticianOut; print(PoliticianOut.model_fields.keys())"
5. Frontend TypeScript: cd frontend && npx tsc --noEmit exits 0
6. Frontend tests: cd frontend && npm test -- --run exits 0
7. Moderation filter present: grep -n "ModerationStatus.approved" backend/app/routers/politicians.py returns at least 1 match
</verification>

<success_criteria>
- backend/app/schemas/politicians.py exports PoliticianOut with model_config = ConfigDict(from_attributes=True)
- backend/app/routers/politicians.py has list_politicians, get_politician, get_politician_promises async handlers
- GET /api/politicians returns PaginatedResponse shape; GET /api/politicians/{slug} returns 404 for missing slug
- GET /api/politicians/{slug}/promises only returns promises with moderation_status=approved
- backend/app/main.py includes both politicians.router and parties.router with prefix="/api"
- pytest tests/test_politicians.py -x exits 0 (all 5 tests green)
- Avatar.tsx renders initials when photo_url is null
- ResolvedStatusBadge.tsx has all 5 status colors matching UI-SPEC.md color table
- PaginationControls.tsx stores page state in URL query param (not React state)
- PersonsPage.tsx filter dropdowns reset page=1 on change
- PoliticianProfilePage.tsx shows 404 state and back link
- cd frontend && npx tsc --noEmit exits 0
</success_criteria>

<output>
Create `.planning/phases/02-politicians-parties-elections-browsing/02-02-SUMMARY.md` when done.
</output>
