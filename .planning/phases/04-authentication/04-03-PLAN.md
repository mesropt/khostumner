---
phase: 04-authentication
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - backend/app/main.py
  - backend/app/routers/auth.py
  - docker-compose.yml
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05

must_haves:
  truths:
    - "GET /health returns 200 (existing routes not broken)"
    - "POST /api/auth/register endpoint exists and is reachable"
    - "POST /api/auth/login endpoint exists and is reachable"
    - "GET /api/users/me endpoint exists and returns 401 when no cookie present"
    - "POST /api/auth/refresh endpoint exists"
    - "CSRFMiddleware is registered AFTER CORSMiddleware in main.py"
    - "mailhog service is present in docker-compose.yml on ports 1025 and 8025"
    - "backend depends_on mailhog in docker-compose.yml"
  artifacts:
    - path: "backend/app/main.py"
      provides: "All auth routers registered + CSRFMiddleware registered after CORSMiddleware"
      contains: "CSRFMiddleware"
    - path: "backend/app/routers/auth.py"
      provides: "Custom /auth/refresh endpoint"
      exports: ["router"]
    - path: "docker-compose.yml"
      provides: "mailhog service + backend environment vars for SMTP/JWT"
      contains: "mailhog/mailhog"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/auth/users.py"
      via: "fastapi_users import"
      pattern: "from app.auth.users import fastapi_users"
    - from: "backend/app/main.py"
      to: "starlette_csrf.CSRFMiddleware"
      via: "middleware registration after CORSMiddleware"
      pattern: "app.add_middleware.*CSRFMiddleware"
---

<objective>
Wire the auth layer into the running application: register all FastAPI-Users routers in main.py, add the CSRF middleware (in correct order), create the custom /auth/refresh router, and add Mailhog to docker-compose.yml.

Purpose: After this plan, the backend accepts auth requests and sends emails via Mailhog in Docker.
Output: Updated main.py, new routers/auth.py, updated docker-compose.yml.
</objective>

<execution_context>
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-authentication/04-CONTEXT.md
@.planning/phases/04-authentication/04-RESEARCH.md
@.planning/phases/04-authentication/04-PATTERNS.md

@backend/app/main.py
@docker-compose.yml
@backend/app/routers/health.py
</context>

<interfaces>
<!-- From backend/app/auth/ package (created in 04-02): -->
<!-- from app.auth.users import fastapi_users, auth_backend_access, auth_backend_refresh -->
<!-- from app.auth.schemas import UserRead, UserCreate, UserUpdate -->
<!-- from app.auth.oauth import google_oauth_client, facebook_oauth_client -->
<!-- from app.config import settings (for JWT_SECRET, CSRF_SECRET) -->

<!-- FastAPI-Users router factory methods (all return APIRouter): -->
<!-- fastapi_users.get_auth_router(auth_backend_access) -> login/logout endpoints -->
<!-- fastapi_users.get_register_router(UserRead, UserCreate) -> /register endpoint -->
<!-- fastapi_users.get_verify_router(UserRead) -> /request-verify-token + /verify endpoints -->
<!-- fastapi_users.get_reset_password_router() -> /forgot-password + /reset-password endpoints -->
<!-- fastapi_users.get_users_router(UserRead, UserUpdate) -> /users/me + /users/{id} endpoints -->
<!-- fastapi_users.get_oauth_router(client, backend, secret, associate_by_email, is_verified_by_default) -->

<!-- CRITICAL middleware order (per RESEARCH.md Pitfall 4 + Pattern 3): -->
<!-- FastAPI processes middleware in REVERSE registration order -->
<!-- Register CORSMiddleware FIRST, CSRFMiddleware SECOND -->
<!-- Result: CORS outermost, CSRF innermost = CORS executes before CSRF -->

<!-- Existing main.py routers (keep, append auth before these): -->
<!-- health.router (no prefix), politicians, parties, elections, promises, stats, og (all /api prefix) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create routers/auth.py with custom /auth/refresh endpoint</name>
  <files>backend/app/routers/auth.py</files>
  <read_first>
    - backend/app/routers/health.py (thin router pattern — APIRouter, single route function)
    - .planning/phases/04-authentication/04-PATTERNS.md §"backend/app/routers/auth.py" (target file verbatim)
    - .planning/phases/04-authentication/04-RESEARCH.md §"Pattern 2" (refresh endpoint signature)
  </read_first>
  <action>
    Create backend/app/routers/auth.py with:
    - Imports: from fastapi import APIRouter, Depends, Response; from app.auth.users import fastapi_users, auth_backend_access; from app.auth.backends import get_access_strategy
    - router = APIRouter(tags=["auth"])
    - @router.post("/auth/refresh") async def refresh_access_token(response: Response, user=Depends(fastapi_users.current_user(active=True))): return await auth_backend_access.login(get_access_strategy(), user, response)

    NOTE: The /auth/refresh endpoint issues a new access cookie using the access backend. It relies on fastapi_users.current_user(active=True) which will be authenticated via the refresh cookie (the frontend sends refresh cookie when calling this endpoint).
    NOTE: This router is registered in main.py with prefix="/api" — final path is POST /api/auth/refresh.
  </action>
  <verify>
    <automated>cd /c/Users/MesropTarkhanyan/PycharmProjects/khostumner/backend && python -c "from app.routers.auth import router; print('auth router OK, routes:', [r.path for r in router.routes])"</automated>
  </verify>
  <done>backend/app/routers/auth.py exists; router has POST /auth/refresh route; importable without error</done>
</task>

<task type="auto">
  <name>Task 2: Wire auth into main.py and add CSRFMiddleware</name>
  <files>backend/app/main.py</files>
  <read_first>
    - backend/app/main.py (existing content — will modify in-place, preserve all existing routes)
    - .planning/phases/04-authentication/04-PATTERNS.md §"backend/app/main.py" (target additions verbatim)
    - .planning/phases/04-authentication/04-RESEARCH.md §"Pattern 3" (CSRFMiddleware config — cookie_name, header_name, safe_methods)
    - .planning/phases/04-authentication/04-RESEARCH.md §"Router Registration in main.py" (exact router include calls)
    - .planning/phases/04-authentication/04-RESEARCH.md Pitfall 4 (CSRF registered AFTER CORS — critical ordering)
  </read_first>
  <action>
    Modify backend/app/main.py:

    1. Add to imports section: from starlette_csrf import CSRFMiddleware; from app.config import settings; from app.routers import auth (add "auth" to the existing routers import line)

    2. Add auth module imports: from app.auth.users import fastapi_users, auth_backend_access, auth_backend_refresh; from app.auth.schemas import UserRead, UserCreate, UserUpdate; from app.auth.oauth import google_oauth_client, facebook_oauth_client

    3. Add CSRFMiddleware registration AFTER the existing CORSMiddleware block:
       app.add_middleware(CSRFMiddleware, secret=settings.CSRF_SECRET, cookie_name="csrftoken", header_name="x-csrftoken", cookie_samesite="lax", safe_methods={"GET", "HEAD", "OPTIONS", "TRACE"})
       CRITICAL: This must appear AFTER app.add_middleware(CORSMiddleware, ...). FastAPI processes middleware in reverse order of registration — CSRF must be registered second so it runs before CORS (inner to outer). Per RESEARCH.md Pitfall 4.

    4. Add auth router registrations BEFORE the existing health.router line:
       app.include_router(auth.router, prefix="/api")
       app.include_router(fastapi_users.get_auth_router(auth_backend_access), prefix="/api/auth", tags=["auth"])
       app.include_router(fastapi_users.get_register_router(UserRead, UserCreate), prefix="/api/auth", tags=["auth"])
       app.include_router(fastapi_users.get_verify_router(UserRead), prefix="/api/auth", tags=["auth"])
       app.include_router(fastapi_users.get_reset_password_router(), prefix="/api/auth", tags=["auth"])
       app.include_router(fastapi_users.get_users_router(UserRead, UserUpdate), prefix="/api/users", tags=["users"])
       app.include_router(fastapi_users.get_oauth_router(google_oauth_client, auth_backend_access, settings.JWT_SECRET, associate_by_email=True, is_verified_by_default=True), prefix="/api/auth/google", tags=["auth"])
       app.include_router(fastapi_users.get_oauth_router(facebook_oauth_client, auth_backend_access, settings.JWT_SECRET, associate_by_email=True, is_verified_by_default=True), prefix="/api/auth/facebook", tags=["auth"])

    5. Keep all existing router registrations (health, politicians, parties, elections, promises, stats, og) unchanged after the auth routers.

    ANTI-PATTERN WARNING: Do NOT add `allow_headers=["x-csrftoken"]` to CORSMiddleware — it already has allow_headers=["*"]. Do NOT change allow_origins — it is already ["http://localhost:5173"].
  </action>
  <verify>
    <automated>cd /c/Users/MesropTarkhanyan/PycharmProjects/khostumner/backend && python -c "from app.main import app; routes = [r.path for r in app.routes]; print('has /api/auth/login:', any('login' in r for r in routes))"</automated>
  </verify>
  <done>
    - main.py imports and registers CSRFMiddleware after CORSMiddleware
    - main.py registers all 8 FastAPI-Users auth routers + custom auth.router (9 total auth registrations)
    - All existing routes (health, politicians, parties, elections, promises, stats, og) still present
    - python -c "from app.main import app" succeeds without import error
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Mailhog service to docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (existing file — will add mailhog service and update backend environment)
    - .planning/phases/04-authentication/04-PATTERNS.md §"docker-compose.yml" (target mailhog service block + backend env additions)
    - .planning/phases/04-authentication/04-CONTEXT.md §D-08 (mailhog/mailhog image, ports 1025+8025)
  </read_first>
  <action>
    In docker-compose.yml:

    1. Add mailhog service after the postgres service block and before the backend service block:
       mailhog:
         image: mailhog/mailhog
         ports:
           - "1025:1025"
           - "8025:8025"
         restart: unless-stopped

    2. In the backend service environment block, add these key-value pairs after the existing ENVIRONMENT: development line:
       SMTP_HOST: mailhog
       SMTP_PORT: "1025"
       SMTP_FROM: noreply@khostumner.am
       JWT_SECRET: dev-jwt-secret-change-in-production
       CSRF_SECRET: dev-csrf-secret-change-in-production
       FRONTEND_URL: http://localhost:5173

    3. Add mailhog to the backend service depends_on block:
       depends_on:
         postgres:
           condition: service_healthy
         mailhog:
           condition: service_started

    NOTE: mailhog does not have a healthcheck so use condition: service_started for the mailhog dependency.
    NOTE: GOOGLE_CLIENT_ID and FACEBOOK_CLIENT_ID are NOT added here — they require real credentials (handled in plan 04-06 OAuth checkpoint).
  </action>
  <verify>
    <automated>grep -c "mailhog/mailhog" /c/Users/MesropTarkhanyan/PycharmProjects/khostumner/docker-compose.yml</automated>
  </verify>
  <done>
    - docker-compose.yml contains mailhog service with image: mailhog/mailhog, ports 1025:1025 and 8025:8025
    - backend service environment block contains SMTP_HOST: mailhog, JWT_SECRET, CSRF_SECRET, FRONTEND_URL
    - backend depends_on block includes mailhog: condition: service_started
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → main.py (CSRF boundary) | All POST/PUT/DELETE must include x-csrftoken header |
| main.py → OAuth providers | Redirect-based; state parameter validated by httpx-oauth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Tampering | POST /api/auth/* without CSRF token | mitigate | starlette-csrf rejects requests missing x-csrftoken header for non-safe methods |
| T-04-15 | Spoofing | CORS + CSRF middleware ordering | mitigate | CORSMiddleware registered BEFORE CSRFMiddleware — FastAPI reversal ensures CORS outermost (per Pitfall 4) |
| T-04-16 | Information Disclosure | JWT_SECRET in docker-compose.yml | accept for dev | "dev-jwt-secret-change-in-production" is clearly a dev placeholder; production uses env-injected secret |
</threat_model>

<verification>
1. CSRFMiddleware registered: grep "CSRFMiddleware" backend/app/main.py
2. CSRF after CORS in file: verify CSRFMiddleware line appears after CORSMiddleware block in main.py
3. Mailhog in docker-compose: grep "mailhog/mailhog" docker-compose.yml
4. Backend SMTP config: grep "SMTP_HOST" docker-compose.yml
5. Auth routes reachable: python -c "from app.main import app; routes=[r.path for r in app.routes]; assert any('login' in r for r in routes)"
6. Existing health route intact: python -c "from app.main import app; routes=[r.path for r in app.routes]; assert '/health' in routes"
</verification>

<success_criteria>
- backend/app/routers/auth.py exists with POST /auth/refresh route
- main.py registers CSRFMiddleware AFTER CORSMiddleware (FastAPI reverse order = CSRF inner)
- main.py has all FastAPI-Users router registrations + custom auth.router
- All existing routes (health, politicians, parties, elections, promises, stats, og) still present
- docker-compose.yml has mailhog service on ports 1025/8025
- backend service has SMTP_HOST=mailhog and depends_on mailhog
</success_criteria>

<output>
Create .planning/phases/04-authentication/04-03-SUMMARY.md when done
</output>
