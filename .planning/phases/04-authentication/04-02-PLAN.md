---
phase: 04-authentication
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - backend/app/models/users.py
  - backend/app/config.py
  - backend/app/auth/__init__.py
  - backend/app/auth/backends.py
  - backend/app/auth/schemas.py
  - backend/app/auth/email.py
  - backend/app/auth/users.py
  - backend/app/auth/oauth.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05

must_haves:
  truths:
    - "User model inherits from SQLAlchemyBaseUserTableUUID and removes duplicate column declarations"
    - "OAuthAccount model exists with access_token overridden to String(4096)"
    - "auth/ package exposes fastapi_users instance, auth_backend_access, auth_backend_refresh"
    - "UserCreate schema includes display_name field"
    - "email.py sends Armenian-text emails via fastapi-mail ConnectionConfig"
    - "config.py has JWT_SECRET, CSRF_SECRET, SMTP_*, OAuth client IDs, FRONTEND_URL"
  artifacts:
    - path: "backend/app/models/users.py"
      provides: "User(SQLAlchemyBaseUserTableUUID, Base) + OAuthAccount class"
      contains: "SQLAlchemyBaseUserTableUUID"
    - path: "backend/app/auth/users.py"
      provides: "UserManager, fastapi_users instance, get_user_manager dependency"
      exports: ["fastapi_users", "get_user_manager", "auth_backend_access", "auth_backend_refresh"]
    - path: "backend/app/auth/backends.py"
      provides: "Two AuthenticationBackend instances (access 1hr, refresh 30d)"
      contains: "khostumner_access"
    - path: "backend/app/auth/email.py"
      provides: "fastapi-mail send functions for verification and password reset"
      exports: ["send_verification_email", "send_password_reset_email"]
    - path: "backend/app/auth/schemas.py"
      provides: "UserRead, UserCreate (with display_name), UserUpdate schemas"
      exports: ["UserRead", "UserCreate", "UserUpdate"]
    - path: "backend/app/auth/oauth.py"
      provides: "GoogleOAuth2 and FacebookOAuth2 client instances"
      exports: ["google_oauth_client", "facebook_oauth_client"]
  key_links:
    - from: "backend/app/auth/users.py"
      to: "backend/app/database.py"
      via: "AsyncSessionLocal import"
      pattern: "from app.database import AsyncSessionLocal"
    - from: "backend/app/auth/users.py"
      to: "backend/app/auth/backends.py"
      via: "auth_backend_access + auth_backend_refresh imports"
      pattern: "from app.auth.backends import auth_backend_access"
---

<objective>
Build the complete backend auth layer: adapt the User model for FastAPI-Users, add config settings, and create the auth/ package with backends, schemas, email helpers, UserManager, and OAuth clients.

Purpose: Wave 2 wires the FastAPI-Users library to the project's database layer. Plan 04-03 (which adds routes) imports from this package.
Output: Updated users.py model, updated config.py, full backend/app/auth/ package.
</objective>

<execution_context>
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-authentication/04-CONTEXT.md
@.planning/phases/04-authentication/04-RESEARCH.md
@.planning/phases/04-authentication/04-PATTERNS.md

@backend/app/models/users.py
@backend/app/config.py
@backend/app/database.py
</context>

<interfaces>
<!-- From backend/app/database.py (existing — reuse, don't reimport engine) -->
<!-- AsyncSessionLocal: async_sessionmaker[AsyncSession] — use for get_async_session in auth/users.py -->

<!-- From backend/app/models/users.py (existing — will be modified) -->
<!-- UserRole enum: registered | moderator | admin (keep intact) -->
<!-- existing custom columns: display_name, role, created_at, account_age_days (keep as extensions) -->
<!-- Remove explicit: id, email, is_active, password_hash, email_verified (provided by mixin after migration) -->

<!-- FastAPI-Users base class provides (DO NOT redeclare): -->
<!--   id: Mapped[uuid.UUID] -->
<!--   email: Mapped[str] (String 320, unique, indexed) -->
<!--   hashed_password: Mapped[str] (String 1024) -->
<!--   is_active: Mapped[bool] (default True) -->
<!--   is_superuser: Mapped[bool] (default False) -->
<!--   is_verified: Mapped[bool] (default False) -->

<!-- from fastapi_users.db: SQLAlchemyBaseUserTableUUID, SQLAlchemyBaseOAuthAccountTableUUID -->
<!-- from fastapi_users: BaseUserManager, FastAPIUsers, UUIDIDMixin, schemas -->
<!-- from fastapi_users.authentication: AuthenticationBackend, CookieTransport, JWTStrategy -->
<!-- from fastapi_users.db: SQLAlchemyUserDatabase -->
<!-- from httpx_oauth.clients.google: GoogleOAuth2 -->
<!-- from httpx_oauth.clients.facebook: FacebookOAuth2 -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update User model and config.py</name>
  <files>backend/app/models/users.py, backend/app/config.py</files>
  <read_first>
    - backend/app/models/users.py (existing file — will replace entire content)
    - backend/app/config.py (existing Settings class — append Phase 4 fields)
    - backend/app/database.py (do not modify; confirms AsyncSessionLocal exists)
    - .planning/phases/04-authentication/04-PATTERNS.md §"backend/app/models/users.py" (target form)
    - .planning/phases/04-authentication/04-PATTERNS.md §"backend/app/config.py" (target field additions)
    - .planning/phases/04-authentication/04-RESEARCH.md §"Pattern 1" (SQLAlchemyBaseUserTableUUID columns provided by base)
  </read_first>
  <action>
    In backend/app/models/users.py, rewrite the User class to:
    - Add imports: from fastapi_users.db import SQLAlchemyBaseUserTableUUID, SQLAlchemyBaseOAuthAccountTableUUID; from sqlalchemy.orm import relationship
    - Add OAuthAccount class ABOVE User class:
        class OAuthAccount(SQLAlchemyBaseOAuthAccountTableUUID, Base):
            __tablename__ = "oauth_accounts"
            access_token: Mapped[str] = mapped_column(String(4096), nullable=False)  -- override String(4096) per RESEARCH Pitfall 3
    - Change User class declaration to: class User(SQLAlchemyBaseUserTableUUID, Base):
    - Remove these explicit column declarations (now provided by SQLAlchemyBaseUserTableUUID):
        id, email, password_hash (renamed to hashed_password by migration), email_verified (renamed to is_verified by migration), is_active
    - Keep these custom extension columns exactly as they are: display_name, role (SAEnum), created_at, account_age_days
    - Add oauth_accounts relationship after account_age_days:
        oauth_accounts: Mapped[list["OAuthAccount"]] = relationship("OAuthAccount", lazy="joined")

    In backend/app/config.py, add Phase 4 fields inside the Settings class, AFTER the existing ENVIRONMENT field and BEFORE model_config:
        JWT_SECRET: str = "change-me-in-production"
        CSRF_SECRET: str = "change-me-in-production-csrf"
        SMTP_HOST: str = "mailhog"
        SMTP_PORT: int = 1025
        SMTP_USERNAME: str = ""
        SMTP_PASSWORD: str = ""
        SMTP_FROM: str = "noreply@khostumner.am"
        GOOGLE_CLIENT_ID: str = ""
        GOOGLE_CLIENT_SECRET: str = ""
        FACEBOOK_CLIENT_ID: str = ""
        FACEBOOK_CLIENT_SECRET: str = ""
        FRONTEND_URL: str = "http://localhost:5173"

    CRITICAL: Do NOT declare hashed_password or is_verified or is_superuser on the User class — these come from SQLAlchemyBaseUserTableUUID. Declaring them again causes "duplicate column" SQLAlchemy errors.
    CRITICAL: Do NOT use `model_config = ConfigDict(from_attributes=True)` on OAuthAccount or User — the mixin handles this.
  </action>
  <verify>
    <automated>cd /c/Users/MesropTarkhanyan/PycharmProjects/khostumner/backend && python -c "from app.models.users import User, OAuthAccount, UserRole; print('User bases:', [b.__name__ for b in User.__mro__])"</automated>
  </verify>
  <done>
    - users.py: User inherits from SQLAlchemyBaseUserTableUUID and Base; OAuthAccount class exists with access_token String(4096); oauth_accounts relationship on User
    - config.py: Settings class contains JWT_SECRET, CSRF_SECRET, SMTP_HOST, SMTP_PORT, SMTP_USERNAME, SMTP_PASSWORD, SMTP_FROM, GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, FACEBOOK_CLIENT_ID, FACEBOOK_CLIENT_SECRET, FRONTEND_URL
    - Python import "from app.models.users import User, OAuthAccount, UserRole" succeeds without error
  </done>
</task>

<task type="auto">
  <name>Task 2: Create auth/ package — backends, schemas, email</name>
  <files>backend/app/auth/__init__.py, backend/app/auth/backends.py, backend/app/auth/schemas.py, backend/app/auth/email.py</files>
  <read_first>
    - .planning/phases/04-authentication/04-RESEARCH.md §"Pattern 2" (exact CookieTransport + JWTStrategy config — cookie names, max_age values)
    - .planning/phases/04-authentication/04-RESEARCH.md §"Pattern 3" (CSRFMiddleware config — reference only, not in this task)
    - .planning/phases/04-authentication/04-RESEARCH.md §"Pattern 5" (fastapi-mail ConnectionConfig + send functions with Armenian text)
    - .planning/phases/04-authentication/04-PATTERNS.md §"backend/app/auth/backends.py" (target file verbatim)
    - .planning/phases/04-authentication/04-PATTERNS.md §"backend/app/auth/schemas.py" (target file verbatim)
    - .planning/phases/04-authentication/04-PATTERNS.md §"backend/app/auth/email.py" (target file verbatim)
  </read_first>
  <action>
    Create backend/app/auth/__init__.py: empty file (package marker).

    Create backend/app/auth/backends.py with:
    - Imports: from fastapi_users.authentication import AuthenticationBackend, CookieTransport, JWTStrategy; from app.config import settings
    - cookie_transport_access = CookieTransport(cookie_name="khostumner_access", cookie_max_age=3600, cookie_httponly=True, cookie_secure=False, cookie_samesite="lax")
    - cookie_transport_refresh = CookieTransport(cookie_name="khostumner_refresh", cookie_max_age=2592000, cookie_httponly=True, cookie_secure=False, cookie_samesite="lax")
      NOTE: 2592000 = 60*60*24*30 (30 days in seconds)
    - def get_access_strategy() -> JWTStrategy: return JWTStrategy(secret=settings.JWT_SECRET, lifetime_seconds=3600)
    - def get_refresh_strategy() -> JWTStrategy: return JWTStrategy(secret=settings.JWT_SECRET, lifetime_seconds=2592000)
    - auth_backend_access = AuthenticationBackend(name="access_cookie", transport=cookie_transport_access, get_strategy=get_access_strategy)
    - auth_backend_refresh = AuthenticationBackend(name="refresh_cookie", transport=cookie_transport_refresh, get_strategy=get_refresh_strategy)

    Create backend/app/auth/schemas.py with:
    - Imports: import uuid; from fastapi_users import schemas; from app.models.users import UserRole
    - class UserRead(schemas.BaseUser[uuid.UUID]): display_name: str; role: str
    - class UserCreate(schemas.BaseUserCreate): display_name: str  — required, per RESEARCH Pitfall 7
    - class UserUpdate(schemas.BaseUserUpdate): display_name: str | None = None
    NOTE: Do NOT add model_config to these classes — FastAPI-Users base schemas already include from_attributes=True.

    Create backend/app/auth/email.py with:
    - Imports: from fastapi_mail import FastMail, MessageSchema, ConnectionConfig, MessageType; from app.config import settings
    - mail_config = ConnectionConfig(MAIL_USERNAME=settings.SMTP_USERNAME, MAIL_PASSWORD=settings.SMTP_PASSWORD, MAIL_FROM=settings.SMTP_FROM, MAIL_PORT=settings.SMTP_PORT, MAIL_SERVER=settings.SMTP_HOST, MAIL_FROM_NAME="Խոստումներ", MAIL_STARTTLS=False, MAIL_SSL_TLS=False, USE_CREDENTIALS=False)
    - fm = FastMail(mail_config)
    - async def send_verification_email(email: str, token: str) -> None: build verify_url = f"{settings.FRONTEND_URL}/verify-email?token={token}"; send MessageSchema with subject "Հաստատեք Ձեր հաշիվը — Խոստումներ", Armenian body HTML with clickable link
    - async def send_password_reset_email(email: str, token: str) -> None: build reset_url = f"{settings.FRONTEND_URL}/reset-password?token={token}"; send MessageSchema with subject "Գաղտնաբառի վերականգնում — Խոստումներ", Armenian body HTML with clickable link
  </action>
  <verify>
    <automated>cd /c/Users/MesropTarkhanyan/PycharmProjects/khostumner/backend && python -c "from app.auth.backends import auth_backend_access, auth_backend_refresh; from app.auth.schemas import UserRead, UserCreate, UserUpdate; from app.auth.email import send_verification_email; print('auth package OK')"</automated>
  </verify>
  <done>
    - backend/app/auth/__init__.py exists (empty)
    - backend/app/auth/backends.py: auth_backend_access (name="access_cookie", 3600s) and auth_backend_refresh (name="refresh_cookie", 2592000s) importable without error
    - backend/app/auth/schemas.py: UserCreate has display_name field; all three schemas importable
    - backend/app/auth/email.py: send_verification_email and send_password_reset_email functions defined; mail_config uses settings.SMTP_* values
  </done>
</task>

<task type="auto">
  <name>Task 3: Create auth/users.py (UserManager + fastapi_users instance) and auth/oauth.py</name>
  <files>backend/app/auth/users.py, backend/app/auth/oauth.py</files>
  <read_first>
    - backend/app/database.py (AsyncSessionLocal — reuse, not engine directly)
    - .planning/phases/04-authentication/04-PATTERNS.md §"backend/app/auth/users.py" (target file verbatim)
    - .planning/phases/04-authentication/04-PATTERNS.md §"backend/app/auth/oauth.py" exists in RESEARCH.md Pattern 4
    - .planning/phases/04-authentication/04-RESEARCH.md §"Pattern 4" (GoogleOAuth2 + FacebookOAuth2 client setup)
    - .planning/phases/04-authentication/04-RESEARCH.md §"Full UserManager with Hooks" (code example)
    - backend/app/auth/backends.py (must import auth_backend_access, auth_backend_refresh from here)
    - backend/app/auth/email.py (must call send_verification_email, send_password_reset_email from hooks)
    - backend/app/auth/schemas.py (UserCreate — for display_name population note)
  </read_first>
  <action>
    Create backend/app/auth/users.py with:
    - Imports: import uuid; from fastapi import Depends; from fastapi_users import BaseUserManager, FastAPIUsers, UUIDIDMixin; from fastapi_users.db import SQLAlchemyUserDatabase; from app.models.users import User, OAuthAccount; from app.database import AsyncSessionLocal; from app.config import settings; from app.auth.backends import auth_backend_access, auth_backend_refresh; from app.auth.email import send_verification_email, send_password_reset_email
    - async def get_async_session(): async with AsyncSessionLocal() as session: yield session
    - async def get_user_db(session=Depends(get_async_session)): yield SQLAlchemyUserDatabase(session, User, OAuthAccount)
    - class UserManager(UUIDIDMixin, BaseUserManager[User, uuid.UUID]):
        reset_password_token_secret = settings.JWT_SECRET
        verification_token_secret = settings.JWT_SECRET
        async def on_after_register(self, user: User, request=None): await send_verification_email(user.email, await self.request_verify(user))
        NOTE: on_after_register does NOT call request_verify directly — that creates a circular flow. Instead: on_after_register should trigger a verification request. FastAPI-Users pattern: call on_after_register after registration; for email, it's on_after_request_verify that receives the token. Correct hooks:
          - on_after_register: log registration (no token available here yet)
          - on_after_request_verify(user, token): call send_verification_email(user.email, token)
          - on_after_forgot_password(user, token): call send_password_reset_email(user.email, token)
        For display_name on OAuth users: override on_after_oauth_register (if exposed by FastAPI-Users 15.0.5) OR handle missing display_name in create() override. Minimum acceptable: set display_name to email.split("@")[0] if display_name is empty after OAuth account creation — implement in on_after_register if user.display_name == "".
    - async def get_user_manager(user_db=Depends(get_user_db)): yield UserManager(user_db)
    - fastapi_users = FastAPIUsers[User, uuid.UUID](get_user_manager, [auth_backend_access, auth_backend_refresh])

    Create backend/app/auth/oauth.py with:
    - Imports: from httpx_oauth.clients.google import GoogleOAuth2; from httpx_oauth.clients.facebook import FacebookOAuth2; from app.config import settings
    - google_oauth_client = GoogleOAuth2(client_id=settings.GOOGLE_CLIENT_ID, client_secret=settings.GOOGLE_CLIENT_SECRET)
    - facebook_oauth_client = FacebookOAuth2(client_id=settings.FACEBOOK_CLIENT_ID, client_secret=settings.FACEBOOK_CLIENT_SECRET)
  </action>
  <verify>
    <automated>cd /c/Users/MesropTarkhanyan/PycharmProjects/khostumner/backend && python -c "from app.auth.users import fastapi_users, get_user_manager; from app.auth.oauth import google_oauth_client, facebook_oauth_client; print('users.py + oauth.py OK')"</automated>
  </verify>
  <done>
    - backend/app/auth/users.py: fastapi_users instance created with [auth_backend_access, auth_backend_refresh]; UserManager has on_after_request_verify calling send_verification_email; on_after_forgot_password calling send_password_reset_email
    - backend/app/auth/oauth.py: google_oauth_client and facebook_oauth_client importable without error
    - Import "from app.auth.users import fastapi_users" succeeds without error
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Auth package → Database | UserManager reads/writes user rows via SQLAlchemyUserDatabase |
| SMTP connection → Mailhog | Email tokens sent in plaintext links (dev only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Spoofing | UserCreate.display_name | mitigate | Pydantic v2 validates display_name is a non-empty string; max length enforced by DB column String(150) |
| T-04-12 | Information Disclosure | JWT_SECRET default value | mitigate | Default "change-me-in-production" is non-secret; docker-compose.yml overrides with dev-jwt-secret; production must set a real secret via env |
| T-04-13 | Elevation of Privilege | is_superuser vs role field confusion | accept | Anti-pattern documented in RESEARCH.md: use UserRole.admin role for authorization, NOT is_superuser. is_superuser only used by FastAPI-Users internal user management endpoints |
</threat_model>

<verification>
1. User model update: python -c "from app.models.users import User; print(User.__bases__)" shows SQLAlchemyBaseUserTableUUID
2. OAuthAccount String(4096): grep "String(4096)" backend/app/models/users.py
3. Config fields: grep "JWT_SECRET" backend/app/config.py
4. UserCreate has display_name: python -c "from app.auth.schemas import UserCreate; print(UserCreate.model_fields.keys())"
5. fastapi_users importable: python -c "from app.auth.users import fastapi_users; print('ok')"
6. OAuth clients importable: python -c "from app.auth.oauth import google_oauth_client; print('ok')"
</verification>

<success_criteria>
- User(SQLAlchemyBaseUserTableUUID, Base) with custom columns display_name, role, created_at, account_age_days + oauth_accounts relationship
- OAuthAccount(SQLAlchemyBaseOAuthAccountTableUUID, Base) with access_token String(4096) override
- config.py Settings has all 12 new Phase 4 fields
- auth/ package has 6 modules: __init__.py, backends.py, schemas.py, email.py, users.py, oauth.py
- All modules importable without error from backend/ context
- UserManager.on_after_request_verify calls send_verification_email; on_after_forgot_password calls send_password_reset_email
</success_criteria>

<output>
Create .planning/phases/04-authentication/04-02-SUMMARY.md when done
</output>
