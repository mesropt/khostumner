---
phase: 04-authentication
plan: 05
type: execute
wave: 4
depends_on:
  - 04-03
  - 04-04
files_modified:
  - frontend/src/pages/LoginPage.tsx
  - frontend/src/pages/RegisterPage.tsx
  - frontend/src/pages/VerifyEmailPage.tsx
  - frontend/src/pages/ResetPasswordPage.tsx
  - frontend/src/pages/LoginPage.test.tsx
  - backend/tests/test_auth.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-05

must_haves:
  truths:
    - "User can fill in display_name, email, password on /register and submit to POST /api/auth/register"
    - "Successful registration shows Armenian success message with link to /login"
    - "User can fill in email and password on /login and submit to POST /api/auth/login"
    - "Successful login dispatches SET_USER to AuthContext and navigates to /"
    - "Failed login shows Armenian error message"
    - "Visiting /verify-email?token=X posts to POST /api/auth/verify automatically on mount"
    - "User can request password reset on /reset-password (no token) via POST /api/auth/forgot-password"
    - "User can set new password on /reset-password?token=X via POST /api/auth/reset-password"
    - "All API calls from auth pages include credentials: include"
    - "All UI text is in Armenian (հայերեն)"
    - "Test stubs in test_auth.py updated from xfail to real assertions"
  artifacts:
    - path: "frontend/src/pages/LoginPage.tsx"
      provides: "Login form with email/password, calls POST /api/auth/login"
      contains: "credentials.*include"
    - path: "frontend/src/pages/RegisterPage.tsx"
      provides: "Registration form with display_name/email/password/confirm, calls POST /api/auth/register"
      contains: "display_name"
    - path: "frontend/src/pages/VerifyEmailPage.tsx"
      provides: "Token-triggered verification page, calls POST /api/auth/verify on mount"
      contains: "verify"
    - path: "frontend/src/pages/ResetPasswordPage.tsx"
      provides: "Two-step reset: forgot-password email form + new password form"
      contains: "forgot-password"
  key_links:
    - from: "frontend/src/pages/LoginPage.tsx"
      to: "POST /api/auth/login"
      via: "fetch with credentials:include"
      pattern: "application/x-www-form-urlencoded"
    - from: "frontend/src/pages/RegisterPage.tsx"
      to: "POST /api/auth/register"
      via: "fetch with credentials:include"
      pattern: "display_name"
---

<objective>
Implement the four auth pages (Login, Register, VerifyEmail, ResetPassword), replacing the placeholder stubs from plan 04-04, and update the test_auth.py stubs from xfail to real integration assertions.

Purpose: Completes the user-facing authentication UI. After this plan, all five AUTH requirements are testable end-to-end.
Output: Four complete auth page components, updated test_auth.py with real assertions for the email/password flow.
</objective>

<execution_context>
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-authentication/04-CONTEXT.md
@.planning/phases/04-authentication/04-RESEARCH.md
@.planning/phases/04-authentication/04-PATTERNS.md

@frontend/src/pages/PromisesListPage.tsx
@frontend/src/pages/AboutPage.tsx
@frontend/src/hooks/useAuth.ts
@frontend/src/contexts/AuthContext.tsx
@backend/tests/test_auth.py
</context>

<interfaces>
<!-- API endpoints (registered in plan 04-03): -->
<!-- POST /api/auth/login — body: application/x-www-form-urlencoded, fields: username (email), password -->
<!--   Returns: 200 + sets httpOnly access + refresh cookies -->
<!-- POST /api/auth/register — body: JSON { email: string, password: string, display_name: string } -->
<!--   Returns: 201 { id, email, display_name, is_active, is_verified, is_superuser, role } -->
<!-- POST /api/auth/request-verify-token — body: JSON { email: string } -->
<!--   Returns: 202 (always, regardless of email existence) -->
<!-- POST /api/auth/verify — body: JSON { token: string } -->
<!--   Returns: 200 { user object with is_verified: true } -->
<!-- POST /api/auth/forgot-password — body: JSON { email: string } -->
<!--   Returns: 202 (always) -->
<!-- POST /api/auth/reset-password — body: JSON { token: string, password: string } -->
<!--   Returns: 200 -->
<!-- GET /api/users/me — cookie auth -->
<!--   Returns: 200 { id, email, display_name, is_active, is_verified, role } | 401 -->

<!-- CSRF: all POST calls must include x-csrftoken header -->
<!-- Helper: read document.cookie for csrftoken= value -->

<!-- Login body format: FastAPI-Users uses OAuth2PasswordRequestForm — MUST use application/x-www-form-urlencoded -->
<!-- field name for email: "username" (not "email") — FastAPI OAuth2 form spec -->

<!-- UI components (all installed in Phase 2): -->
<!-- Card, CardContent, CardHeader, CardTitle from @/components/ui/card -->
<!-- Input from @/components/ui/input -->
<!-- Button from @/components/ui/button -->

<!-- Design pattern: max-w-md mx-auto px-4 py-16 for auth form pages -->
<!-- Armenian text only: Մուտք, Ելք, Գրանցվել, Ուղարկել, Հաստատել -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement LoginPage.tsx and RegisterPage.tsx</name>
  <files>frontend/src/pages/LoginPage.tsx, frontend/src/pages/RegisterPage.tsx, frontend/src/pages/LoginPage.test.tsx</files>
  <read_first>
    - frontend/src/pages/LoginPage.tsx (currently a stub from 04-04 — replace entirely)
    - frontend/src/pages/RegisterPage.tsx (currently a stub from 04-04 — replace entirely)
    - .planning/phases/04-authentication/04-PATTERNS.md §"frontend/src/pages/LoginPage.tsx" (full target implementation)
    - .planning/phases/04-authentication/04-PATTERNS.md §"frontend/src/pages/RegisterPage.tsx" (target structure)
    - frontend/src/contexts/AuthContext.tsx (UserRead type, dispatch signature)
    - frontend/src/pages/PromisesListPage.tsx (Input usage pattern, error state pattern)
  </read_first>
  <behavior>
    - Test (LoginPage): form submission calls fetch to /api/auth/login with Content-Type: application/x-www-form-urlencoded and credentials:include
    - Test (LoginPage): on successful login (200), fetches /api/users/me then dispatches SET_USER and navigates to /
    - Test (LoginPage): on 400/401 response, shows "Սխալ էլ. հասցե կամ գաղտնաբառ" error text
  </behavior>
  <action>
    Create frontend/src/pages/LoginPage.tsx (replace stub) with:
    - const API_BASE = import.meta.env.VITE_API_URL ?? "http://localhost:8000"
    - getCsrfToken helper: reads csrftoken from document.cookie: document.cookie.split("; ").find(r => r.startsWith("csrftoken="))?.split("=")[1] ?? ""
    - State: email (string), password (string), error (string|null), isSubmitting (boolean)
    - handleSubmit: e.preventDefault(); set isSubmitting=true; build URLSearchParams({ username: email, password }); POST to /api/auth/login with Content-Type: application/x-www-form-urlencoded, credentials:include, x-csrftoken header; on ok (200): fetch /api/users/me with credentials:include, dispatch SET_USER, navigate("/"); on error: setError("Սխալ էլ. հասցե կամ գաղտնաբառ")
    - Layout: <main className="max-w-md mx-auto px-4 py-16"> wrapping Card > CardHeader (CardTitle "Մուտք") + CardContent with form, email Input, password Input, error paragraph, submit Button "Մուտք", link to /register "Հաշիվ չունե՞ք Գրանցվել"

    Create frontend/src/pages/RegisterPage.tsx (replace stub) with:
    - State: displayName (string), email (string), password (string), confirmPassword (string), error (string|null), success (boolean), isSubmitting (boolean)
    - handleSubmit: validate confirmPassword === password (show "Գաղտնաբառերը չեն համընկնում"); POST to /api/auth/register with JSON body { email, password, display_name: displayName } and credentials:include, x-csrftoken header, Content-Type: application/json; on 201: setSuccess(true); on 400 (duplicate email): setError("Այս էլ. հասցեն արդեն գրանցված է"); on other error: setError("Գրանցման ժամանակ սխալ է տեղի ունեցել")
    - When success=true: show Armenian success message "Շնորհակալություն։ Ստուգեք Ձեր փոստը հաստատման հղման համար" and link to /login
    - Layout: same max-w-md pattern; fields: Անուն (display_name), Էլ. հասցե (email), Գաղտնաբառ (password), Կրկնեք գաղտնաբառը (confirmPassword); submit Button "Գրանցվել"

    Create frontend/src/pages/LoginPage.test.tsx with the three behavior tests using vi.fn() to mock fetch.

    NOTE: FastAPI-Users login field is "username" not "email" in the URLSearchParams body. This is the OAuth2PasswordRequestForm spec. Getting this wrong causes 422 errors.
    NOTE: The x-csrftoken header must be included on both POST calls. Starlette-csrf will reject requests missing this header for non-safe methods.
  </action>
  <verify>
    <automated>cd /c/Users/MesropTarkhanyan/PycharmProjects/khostumner/frontend && npm run test -- --run src/pages/LoginPage.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <done>
    - LoginPage.tsx: form posts to /api/auth/login with URLSearchParams username/password; credentials:include; x-csrftoken; on success fetches /me, dispatches SET_USER, navigates to /
    - RegisterPage.tsx: form posts to /api/auth/register with JSON { email, password, display_name }; credentials:include; x-csrftoken; on 201 shows Armenian success message
    - LoginPage.test.tsx: all 3 behavior tests pass
    - All UI text in Armenian
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement VerifyEmailPage.tsx and ResetPasswordPage.tsx</name>
  <files>frontend/src/pages/VerifyEmailPage.tsx, frontend/src/pages/ResetPasswordPage.tsx</files>
  <read_first>
    - frontend/src/pages/VerifyEmailPage.tsx (currently a stub from 04-04 — replace entirely)
    - frontend/src/pages/ResetPasswordPage.tsx (currently a stub from 04-04 — replace entirely)
    - .planning/phases/04-authentication/04-PATTERNS.md §"frontend/src/pages/VerifyEmailPage.tsx" (target structure)
    - .planning/phases/04-authentication/04-PATTERNS.md §"frontend/src/pages/ResetPasswordPage.tsx" (target structure)
    - frontend/src/pages/AboutPage.tsx (static page wrapper pattern: max-w-2xl mx-auto px-4 py-8)
    - frontend/src/contexts/AuthContext.tsx (getCsrfToken pattern — copy inline in these files)
  </read_first>
  <action>
    Create frontend/src/pages/VerifyEmailPage.tsx (replace stub) with:
    - Imports: useEffect, useState from react; useSearchParams from react-router-dom; Card/CardContent/CardHeader/CardTitle from ui/card
    - const API_BASE = import.meta.env.VITE_API_URL ?? "http://localhost:8000"
    - getCsrfToken helper (same as LoginPage)
    - State: status ("loading" | "success" | "error")
    - useEffect with [] deps: read token from useSearchParams().get("token"); if !token: setStatus("error") immediately; else POST to /api/auth/verify with JSON { token } and credentials:include, x-csrftoken; on 200: setStatus("success"); on error: setStatus("error")
    - Render: loading state shows "Ստուգվում է..." spinner; success shows "Ձեր հաշիվը հաստատվել է" + link to /login; error shows "Հաստատման հղումն անվավեր է կամ ժամկետանց"
    - Layout: <main className="max-w-md mx-auto px-4 py-16">

    Create frontend/src/pages/ResetPasswordPage.tsx (replace stub) with:
    - State: step ("request" | "reset"), email (string), password (string), confirmPassword (string), message (string|null), error (string|null), isSubmitting (boolean)
    - On mount: read token from useSearchParams().get("token"); if token present: setStep("reset")
    - Step "request" form: email Input + Button "Ուղարկել"; on submit: POST /api/auth/forgot-password with JSON { email } and credentials:include, x-csrftoken; on 202: setMessage("Եթե այդ հասցեն գրանցված է, ստուգեք Ձեր փոստը")
    - Step "reset" form: new password Input + confirm password Input + Button "Հաստատել"; on submit: validate passwords match; POST /api/auth/reset-password with JSON { token: searchParams.get("token"), password } and credentials:include, x-csrftoken; on 200: setMessage("Գաղտնաբառը հաջողությամբ փոխվել է") + navigate("/login") after 2s
    - Layout: <main className="max-w-md mx-auto px-4 py-16"> wrapping Card

    NOTE: POST /api/auth/forgot-password always returns 202 — never indicate whether the email exists or not (anti-enumeration, RESEARCH.md T-04-04).
  </action>
  <verify>
    <automated>cd /c/Users/MesropTarkhanyan/PycharmProjects/khostumner/frontend && npm run build 2>&1 | grep -E "error|Error" | head -10</automated>
  </verify>
  <done>
    - VerifyEmailPage.tsx: reads ?token= on mount; POSTs to /api/auth/verify; shows Armenian loading/success/error messages
    - ResetPasswordPage.tsx: two-step flow (forgot-password email form + reset form); detects ?token= to show step 2
    - npm run build exits 0 for all four auth pages
    - All UI text in Armenian
  </done>
</task>

<task type="auto">
  <name>Task 3: Update test_auth.py from xfail stubs to real assertions</name>
  <files>backend/tests/test_auth.py</files>
  <read_first>
    - backend/tests/test_auth.py (xfail stubs from plan 04-01 — replace stub bodies with real assertions)
    - backend/tests/test_health.py (client fixture pattern — confirm async client setup)
    - backend/tests/test_promises.py (or any existing test with parametrize/fixture pattern)
    - .planning/phases/04-authentication/04-RESEARCH.md §"Phase Requirements → Test Map" (exact assertion targets per test)
  </read_first>
  <action>
    Update backend/tests/test_auth.py — replace the xfail stub bodies with real assertions:

    test_register_success: POST /api/auth/register with body {"email": "test@example.com", "password": "TestPass123!", "display_name": "Test User"}; assert response.status_code == 201; assert response.json()["email"] == "test@example.com"; assert "id" in response.json()

    test_register_duplicate_email: register the same email twice; assert second registration returns status_code == 400

    test_request_verify: POST /api/auth/request-verify-token with {"email": "test@example.com"} (register first in the test body); assert response.status_code == 202

    test_verify_email: register + request verify token; capture token from SMTP mock or use UserManager directly in the test to get the token; assert POST /api/auth/verify with token returns 200; assert response.json()["is_verified"] == True
    NOTE: For testing email verification without a running SMTP server, use pytest monkeypatch to mock the UserManager.on_after_request_verify hook and capture the token parameter. This is the standard fastapi-users test approach.

    test_forgot_password: POST /api/auth/forgot-password with {"email": "test@example.com"}; assert response.status_code == 202

    test_reset_password: Use monkeypatch to mock on_after_forgot_password and capture the token; POST /api/auth/reset-password with {"token": captured_token, "password": "NewPass123!"}; assert response.status_code == 200

    test_oauth_google_authorize: GET /api/auth/google/authorize; assert response.status_code in (302, 303, 307); assert "accounts.google.com" in response.headers.get("location", "")
    NOTE: This test will return an error if GOOGLE_CLIENT_ID is empty string (FastAPI-Users may return 400 or redirect to an empty URL). Mark this test with pytest.mark.skipif(not settings.GOOGLE_CLIENT_ID, reason="No Google OAuth credentials configured") — it skips in CI and runs when credentials are set.

    test_me_authenticated: Register + login to get access cookie; GET /api/users/me with the cookie; assert response.status_code == 200; assert "email" in response.json()
    NOTE: In httpx test client, cookies are managed via response.cookies — set cookies on subsequent requests via client.cookies.set("khostumner_access", cookie_value).

    test_me_unauthenticated: GET /api/users/me without any cookie; assert response.status_code == 401

    Remove all @pytest.mark.xfail decorators after writing real assertions.
    Keep @pytest.mark.skipif on test_oauth_google_authorize.
  </action>
  <verify>
    <automated>cd /c/Users/MesropTarkhanyan/PycharmProjects/khostumner/backend && python -m pytest tests/test_auth.py -x --ignore=tests/test_auth.py -k "test_me_unauthenticated or test_register_success" 2>&1 | tail -10</automated>
  </verify>
  <done>
    - test_auth.py has no @pytest.mark.xfail decorators remaining
    - test_me_unauthenticated passes (401 without cookie)
    - test_oauth_google_authorize is marked skipif when GOOGLE_CLIENT_ID is empty
    - All other tests have real assertions (not raise NotImplementedError)
    - pytest tests/test_auth.py -x runs without collection errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User browser → LoginPage → POST /api/auth/login | Password in form body — HTTPS in production |
| VerifyEmailPage → POST /api/auth/verify | Token from email link in URL query param |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-20 | Information Disclosure | POST /api/auth/forgot-password response | mitigate | Always return 202 regardless of email existence — prevents email enumeration (FastAPI-Users built-in behavior, confirmed in RESEARCH.md) |
| T-04-21 | Tampering | Login form CSRF | mitigate | x-csrftoken header extracted from csrftoken cookie and sent on all POST requests |
| T-04-22 | Elevation of Privilege | Password reset token in URL | accept | Tokens are single-use and short-lived (FastAPI-Users JWTStrategy); URL tokens visible in browser history — acceptable for MVP |
</threat_model>

<verification>
1. LoginPage has credentials:include: grep "credentials.*include" frontend/src/pages/LoginPage.tsx
2. Login uses URLSearchParams username field: grep "username" frontend/src/pages/LoginPage.tsx
3. Register sends display_name: grep "display_name" frontend/src/pages/RegisterPage.tsx
4. All text Armenian: grep -v "^import\|^const\|^export\|^type\|^function\|^//\|^\s*[{}]" frontend/src/pages/LoginPage.tsx | grep -E "[a-zA-Z]{4,}" (should return nothing meaningful — all labels are Armenian)
5. No xfail in test_auth.py: grep "xfail" backend/tests/test_auth.py (expect 0 lines)
6. Frontend build: npm run build (exit 0)
7. Backend tests collect: pytest tests/test_auth.py --co -q (no import errors)
</verification>

<success_criteria>
- LoginPage.tsx: form POSTs URLSearchParams(username, password) to /api/auth/login with credentials:include and x-csrftoken; on success fetches /me, dispatches SET_USER, navigates to /
- RegisterPage.tsx: form POSTs JSON(email, password, display_name) to /api/auth/register; shows Armenian success message on 201
- VerifyEmailPage.tsx: reads ?token= on mount; POSTs to /api/auth/verify; shows Armenian status messages
- ResetPasswordPage.tsx: two-step (forgot-password email form + reset-password form); detects ?token= presence
- test_auth.py: no xfail decorators; all 9 tests have real assertions; test_oauth_google_authorize has skipif guard
- npm run build exits 0
- pytest tests/test_auth.py -x passes (except skipped OAuth test)
</success_criteria>

<output>
Create .planning/phases/04-authentication/04-05-SUMMARY.md when done
</output>
