---
phase: 03-promise-browsing-homepage
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - backend/app/routers/stats.py
  - frontend/src/hooks/useStats.ts
  - frontend/src/pages/HomePage.tsx
autonomous: true
requirements: [DISC-01]

must_haves:
  truths:
    - "GET /api/stats returns 200 with total count and by_status breakdown for all 5 resolved_status values"
    - "Homepage shows a stats block with counts per status and a recent promises list (last 10 by created_at)"
    - "Homepage replaces the health-check placeholder entirely"
    - "Stats block uses status-semantic colors matching Phase 2 badge colors"
  artifacts:
    - path: "backend/app/routers/stats.py"
      provides: "GET /api/stats aggregate endpoint"
      contains: "get_stats"
    - path: "frontend/src/hooks/useStats.ts"
      provides: "TanStack Query hook for stats endpoint"
      contains: "useStats"
    - path: "frontend/src/pages/HomePage.tsx"
      provides: "Homepage with stats block + recent promises section"
      contains: "useStats"
  key_links:
    - from: "frontend/src/hooks/useStats.ts"
      to: "/api/stats"
      via: "apiClient.get<StatsOut>('/api/stats')"
      pattern: "apiClient\\.get.*api/stats"
    - from: "frontend/src/pages/HomePage.tsx"
      to: "frontend/src/hooks/useStats.ts"
      via: "useStats() import"
      pattern: "useStats"
---

<objective>
Stats vertical slice: implement GET /api/stats backend endpoint, useStats frontend hook, and replace the HomePage placeholder with a real stats block + recent promises section.

Purpose: Delivers DISC-01 — homepage shows fulfillment breakdown by status. This plan does NOT wire stats.py into main.py (that happens in 03-04 to avoid main.py conflict with 03-03).
Output: stats router, useStats hook, new HomePage component.
</objective>

<execution_context>
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-promise-browsing-homepage/03-CONTEXT.md
@.planning/phases/03-promise-browsing-homepage/03-RESEARCH.md
@.planning/phases/03-promise-browsing-homepage/03-PATTERNS.md
@.planning/phases/03-promise-browsing-homepage/03-UI-SPEC.md
@.planning/phases/03-promise-browsing-homepage/03-01-SUMMARY.md

<interfaces>
From backend/app/schemas/stats.py (created in 03-01):
```python
class StatsByStatus(BaseModel):
    kept: int
    broken: int
    in_progress: int
    stalled: int
    not_rated: int

class StatsOut(BaseModel):
    total: int
    by_status: StatsByStatus
```

From backend/app/models/promises.py:
- ModerationStatus.approved — the only filter to apply
- ResolvedStatus enum: kept, broken, in_progress, stalled, not_rated

From backend/app/routers/elections.py (pattern to follow):
```python
router = APIRouter(prefix="/...", tags=["..."])
# func.count() + .group_by() pattern
```

From frontend/src/hooks/useElections.ts (useStats pattern):
```typescript
export function useElections(page: number) {
  const { data, isLoading, isError } = useQuery<PaginatedResponse<ElectionWithCountOut>>({
    queryKey: ["elections", page],
    queryFn: () => apiClient.get<...>(`/api/elections?page=${page}`),
  })
  return { data, isLoading, isError }
}
```

From frontend/src/types/index.ts (added in 03-01):
```typescript
export interface StatsByStatus { kept: number; broken: number; in_progress: number; stalled: number; not_rated: number }
export interface StatsOut { total: number; by_status: StatsByStatus }
```

From frontend/src/pages/ElectionsListPage.tsx (page structure pattern):
- Loading: `if (isLoading) return <main ...>skeleton</main>`
- Error: `if (isError) return <main ...>error message</main>`
- Data: `return <main className="max-w-4xl mx-auto px-4 py-8">...`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement GET /api/stats endpoint</name>
  <files>backend/app/routers/stats.py</files>
  <read_first>
    - backend/app/schemas/stats.py — StatsOut, StatsByStatus (created in 03-01)
    - backend/app/models/promises.py — ModerationStatus, ResolvedStatus enums; Promise model
    - backend/app/routers/elections.py — router pattern: APIRouter declaration, Depends(get_db), func.count(), group_by
    - backend/app/database.py — get_db dependency import path
  </read_first>
  <behavior>
    - GET /api/stats returns 200 with body {"total": N, "by_status": {"kept": N, "broken": N, "in_progress": N, "stalled": N, "not_rated": N}}
    - All 5 status keys present even when count is 0
    - Only counts promises where moderation_status == ModerationStatus.approved
    - Total equals sum of all by_status values
  </behavior>
  <action>
    Create backend/app/routers/stats.py. Imports: fastapi.APIRouter, fastapi.Depends, sqlalchemy.func, sqlalchemy.select, sqlalchemy.ext.asyncio.AsyncSession, app.database.get_db, app.models.promises.ModerationStatus + Promise + ResolvedStatus, app.schemas.stats.StatsOut + StatsByStatus.

    Declare: `router = APIRouter(prefix="/stats", tags=["stats"])`

    Implement `@router.get("", response_model=StatsOut)` async function `get_stats(db: AsyncSession = Depends(get_db)) -> StatsOut`.

    Body: Build stmt = select(Promise.resolved_status, func.count().label("cnt")).where(Promise.moderation_status == ModerationStatus.approved).group_by(Promise.resolved_status). Execute with `await db.execute(stmt)`. Call `.all()` on result. Build dict `by_status_dict = {r.resolved_status: r.cnt for r in rows}`. Compute `total = sum(by_status_dict.values())`. Return StatsOut(total=total, by_status=StatsByStatus(kept=by_status_dict.get(ResolvedStatus.kept, 0), broken=by_status_dict.get(ResolvedStatus.broken, 0), in_progress=by_status_dict.get(ResolvedStatus.in_progress, 0), stalled=by_status_dict.get(ResolvedStatus.stalled, 0), not_rated=by_status_dict.get(ResolvedStatus.not_rated, 0))).

    NOTE: Do NOT add `include_router(stats.router)` to main.py here — that is done in plan 03-04 to avoid concurrent file conflicts with plan 03-03.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.routers.stats import router; print('OK')"</automated>
  </verify>
  <done>stats.py importable; router has GET "" endpoint; response_model=StatsOut; uses ModerationStatus.approved filter; all 5 ResolvedStatus keys defaulted to 0 if absent.</done>
</task>

<task type="auto">
  <name>Task 2: Create useStats hook + replace HomePage with stats + recent promises</name>
  <files>frontend/src/hooks/useStats.ts, frontend/src/pages/HomePage.tsx</files>
  <read_first>
    - frontend/src/hooks/useElections.ts — hook pattern to copy (single endpoint, no params, destructure {data, isLoading, isError})
    - frontend/src/pages/ElectionsListPage.tsx — loading/error/data state pattern; page container className
    - frontend/src/components/PromiseStub.tsx — component to reuse for recent promises list (already handles /promises/:slug links)
    - frontend/src/pages/HomePage.tsx — current file; replace entirely (D-03)
    - .planning/phases/03-promise-browsing-homepage/03-UI-SPEC.md — HomePage layout spec (stats block colors, heading copy, "Բոլոր խոստումները →" link)
  </read_first>
  <action>
    Create frontend/src/hooks/useStats.ts. Imports: useQuery from @tanstack/react-query; apiClient from @/api/client; StatsOut from @/types. Export function useStats() using useQuery with queryKey ["stats"], queryFn calling apiClient.get<StatsOut>("/api/stats"), staleTime: 5 * 60 * 1000 (5 minutes — stats tolerate minor staleness on homepage). Destructure and return {data, isLoading, isError}.

    Replace frontend/src/pages/HomePage.tsx entirely. Imports: Link from react-router-dom; useStats from @/hooks/useStats; usePromises from @/hooks/usePromises (created in 03-03, but import is forward-declared here since it's in the same Wave 2 build — OR defer the recent promises section to a separate task). 

    IMPORTANT: Since usePromises is created in plan 03-03 (parallel Wave 2), implement HomePage in two parts: (a) the stats block using useStats, and (b) the recent promises section using a placeholder skeleton div with a TODO comment "// TODO(03-03): replace with usePromises({page:1, perPage:10}) once 03-03 is merged". This prevents a circular dependency during parallel Wave 2 execution.

    HomePage layout (per D-01, D-03, UI-SPEC):
    - Page container: `<main className="max-w-4xl mx-auto px-4 py-8">`
    - Stats block: `<section className="bg-zinc-100 rounded p-6 mb-8">` containing total count (28px semibold zinc-900) labeled "Ընդամենը" (14px zinc-500), then a flex-wrap row of 5 stat cards
    - Each stat card: `<div className="bg-zinc-50 rounded p-2 min-w-[80px]">` with the count in 28px semibold and the Armenian status label in semantic color text class
    - Status label+color map (per UI-SPEC): kept="Կատարված" green-800 text, broken="Չկատarvac" red-800, in_progress="Ընthacqi mej" yellow-800, stalled="Kasezvac" gray-600, not_rated="Չgnahatvan" zinc-500
    - Armenian status labels EXACT per UI-SPEC: kept→"Կatarвac", broken→"Чkatarвac", in_progress→"Ընthacqi mej", stalled→"Kasezvac", not_rated→"Чgnahatvan" — use the EXACT Armenian text from the Copywriting Contract in UI-SPEC.md (Status Labels table)
    - Loading: pulse skeleton for stats block (5 boxes in flex row) + 10 skeleton rows
    - Error: `<p className="text-sm text-red-600">Տвяалнерэ berrnaelu jamanak sxal e teli unecel</p>` — use EXACT Armenian from UI-SPEC error copy
    - Recent promises section heading: "Ветьin xostumnera" (H2 20px semibold) with `<Link to="/promises" className="text-sm text-blue-600 underline">Бoлor xostumnera →</Link>` — use EXACT Armenian copy from UI-SPEC Copywriting Contract
    - Recent promises list: placeholder `<div className="space-y-2"><p className="text-sm text-zinc-500">// TODO: wire usePromises after 03-03</p></div>` until 03-03 merges

    CRITICAL: All Armenian copy must match EXACTLY the strings in 03-UI-SPEC.md Copywriting Contract section, not transliterated approximations used in this action description.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>useStats.ts exports useStats() returning {data, isLoading, isError}. HomePage.tsx imports useStats and renders stats block with all 5 status counts. TypeScript compiles without errors. Stats block has bg-zinc-100 container; stat cards have bg-zinc-50; total count is labeled "Ընδамenь".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → DB | SQLAlchemy ORM with parameterized queries; no string interpolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Information Disclosure | GET /api/stats | mitigate | Only counts approved promises (ModerationStatus.approved WHERE clause in stats query) — pending/rejected counts not exposed |
| T-03-03 | Tampering | stats GROUP BY query | accept | SQLAlchemy ORM uses parameterized queries; no user input in stats query; no injection surface |
</threat_model>

<verification>
- `python -c "from app.routers.stats import router"` succeeds in backend/
- GET /api/stats (once wired in 03-04) returns {"total": N, "by_status": {"kept":N, "broken":N, "in_progress":N, "stalled":N, "not_rated":N}}
- `npx tsc --noEmit` in frontend/ passes
- HomePage renders stats block with 5 stat entries (kept/broken/in_progress/stalled/not_rated) + their Armenian labels in correct semantic colors
- Stats block has `bg-zinc-100 rounded p-6` container class
</verification>

<success_criteria>
- backend/app/routers/stats.py exists with GET "" route returning StatsOut
- Only moderation_status=approved promises counted (never pending/rejected)
- All 5 ResolvedStatus keys always present in response (default 0 when absent from DB)
- useStats hook: queryKey ["stats"], staleTime 5 minutes, returns {data, isLoading, isError}
- HomePage: stats block shows total + 5 status counts with Armenian labels in semantic colors
</success_criteria>

<output>
Create `.planning/phases/03-promise-browsing-homepage/03-02-SUMMARY.md` when done
</output>
