---
phase: 03-promise-browsing-homepage
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - backend/app/routers/promises.py
  - backend/app/main.py
  - frontend/src/hooks/usePromise.ts
  - frontend/src/pages/PromiseDetailPage.tsx
  - frontend/src/pages/AboutPage.tsx
  - frontend/src/pages/HomePage.tsx
  - frontend/src/components/Layout.tsx
  - frontend/src/App.tsx
autonomous: true
requirements: [PROM-02, DISC-02, DISC-03, DISC-01]

must_haves:
  truths:
    - "GET /api/promises/{slug} returns full PromiseDetailOut with politician_name_hy and politician_slug"
    - "GET /api/promises/nonexistent-slug returns 404 with detail='Promise not found'"
    - "GET /api/stats, GET /api/promises, GET /api/promises/{slug} all return 200 (routers wired in main.py)"
    - "Promise detail page shows quote_hy as hero, title_hy as H1, status badge, politician link, source button"
    - "expected_date row and archived_url button are conditionally rendered (omitted when null)"
    - "About page renders /about with Մeр mасин heading and two static sections with TODO placeholder"
    - "Layout nav shows Мeр масин as last link, routing to /about"
    - "App.tsx has all 6 new routes: /, /promises, /fulfilled, /unfulfilled, /promises/:slug, /about"
    - "HomePage recent promises section wired to usePromises (replaces TODO placeholder)"
  artifacts:
    - path: "backend/app/main.py"
      provides: "All 3 new routers registered: promises, stats, og (og stub acceptable)"
      contains: "include_router(promises.router"
    - path: "frontend/src/hooks/usePromise.ts"
      provides: "Single-promise TanStack Query hook"
      contains: "usePromise"
    - path: "frontend/src/pages/PromiseDetailPage.tsx"
      provides: "Promise detail page with quote hero"
      contains: "PromiseDetailPage"
    - path: "frontend/src/pages/AboutPage.tsx"
      provides: "Static about page"
      contains: "AboutPage"
    - path: "frontend/src/App.tsx"
      provides: "All Phase 3 routes registered"
      contains: "/promises/:slug"
  key_links:
    - from: "frontend/src/hooks/usePromise.ts"
      to: "/api/promises/{slug}"
      via: "apiClient.get<PromiseDetailOut>('/api/promises/${slug}')"
      pattern: "apiClient\\.get.*api/promises/"
    - from: "backend/app/main.py"
      to: "backend/app/routers/promises.py"
      via: "app.include_router(promises.router, prefix='/api')"
      pattern: "include_router\\(promises\\.router"
---

<objective>
Wiring plan: add GET /api/promises/{slug} detail endpoint to the promises router, register all new routers in main.py, add usePromise hook, create PromiseDetailPage + AboutPage, wire HomePage recent promises, update Layout nav, and register all Phase 3 routes in App.tsx.

Purpose: Completes PROM-02 (promise detail), wires all backend routers so tests go GREEN, and connects all frontend routes. Runs in Wave 3 after both Wave 2 plans complete (dependencies on 03-02 and 03-03).
Output: Full end-to-end promise browsing + detail experience working in the browser.
</objective>

<execution_context>
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-promise-browsing-homepage/03-CONTEXT.md
@.planning/phases/03-promise-browsing-homepage/03-RESEARCH.md
@.planning/phases/03-promise-browsing-homepage/03-PATTERNS.md
@.planning/phases/03-promise-browsing-homepage/03-UI-SPEC.md
@.planning/phases/03-promise-browsing-homepage/03-02-SUMMARY.md
@.planning/phases/03-promise-browsing-homepage/03-03-SUMMARY.md

<interfaces>
From backend/app/schemas/promises.py (created in 03-01):
```python
class PromiseDetailOut(BaseModel):
    # plain BaseModel, NO from_attributes
    id: uuid.UUID
    slug: str
    title_hy: str
    quote_hy: str           # full verbatim, no truncation
    resolved_status: ResolvedStatus
    made_date: date | None
    expected_date: date | None
    source_url: str
    archived_url: str | None
    politician_name_hy: str
    politician_slug: str
```

From backend/app/main.py (current state):
```python
from app.routers import elections, health, parties, politicians
app.include_router(elections.router, prefix="/api")
# promises.router, stats.router, og.router NOT yet registered
```

From frontend/src/hooks/usePolitician.ts (usePromise pattern):
```typescript
import { useQuery } from "@tanstack/react-query"
import { apiClient } from "@/api/client"
export function usePolitician(slug: string) {
  const { data, isLoading, isError } = useQuery({
    queryKey: ["politician", slug],
    queryFn: () => apiClient.get<...>(`/api/politicians/${slug}`),
    enabled: Boolean(slug),
  })
  return { data, isLoading, isError }
}
```

From frontend/src/App.tsx (current routes to extend):
```typescript
<Route path="/" element={<HomePage />} />
<Route path="/persons" element={<PersonsPage />} />
...
<Route path="*" element={<NotFoundPage />} />
```

From frontend/src/components/Layout.tsx (nav to extend):
```typescript
<NavLink to="/elections" className={navLinkClass}>Ընtrutiuннernerd</NavLink>
// Add AFTER this:
<NavLink to="/about" className={navLinkClass}>Мeр mасин</NavLink>
```

From frontend/src/pages/ElectionDetailPage.tsx (detail page loading/error/data pattern):
```typescript
const { slug } = useParams<{ slug: string }>()
if (isLoading) { return <main className="max-w-4xl mx-auto px-4 py-8 animate-pulse">... }
if (isError || !data) { return <main ... text-center>404 message + back link }
return <main className="max-w-4xl mx-auto px-4 py-8">...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add GET /api/promises/{slug} detail endpoint + wire all routers to main.py</name>
  <files>backend/app/routers/promises.py, backend/app/main.py</files>
  <read_first>
    - backend/app/routers/promises.py — current file from 03-03; APPEND the detail endpoint to it
    - backend/app/main.py — current file; add 3 new import + include_router lines
    - backend/app/schemas/promises.py — PromiseDetailOut fields to construct
    - backend/app/models/politicians.py — Politician.name_hy, Politician.slug, Politician.photo_url
    - backend/app/routers/elections.py — slug lookup + 404 pattern (lines 68-81)
    - .planning/phases/03-promise-browsing-homepage/03-PATTERNS.md — detail endpoint JOIN pattern section
  </read_first>
  <behavior>
    - GET /api/promises/{slug} returns 200 with PromiseDetailOut for approved promises
    - GET /api/promises/nonexistent-slug returns 404 with {"detail": "Promise not found"}
    - Response includes politician_name_hy and politician_slug (from JOIN with Politician)
    - moderation_status=approved filter applied (non-approved slugs return 404, not the promise)
    - quote_hy is full verbatim text — NOT truncated (unlike PromiseListOut)
  </behavior>
  <action>
    Append GET /{slug} endpoint to existing backend/app/routers/promises.py (do NOT rewrite the list endpoint). Add import for PromiseDetailOut at the top (alongside existing PromiseListOut import if not already present).

    Implement `@router.get("/{slug}", response_model=PromiseDetailOut)` async function `get_promise(slug: str, db: AsyncSession = Depends(get_db)) -> PromiseDetailOut`.

    Build stmt: select(Promise, Politician.name_hy.label("politician_name"), Politician.slug.label("politician_slug")).join(Politician, Politician.id == Promise.politician_id).where(Promise.slug == slug, Promise.moderation_status == ModerationStatus.approved).

    Execute: `result = await db.execute(stmt)`; `row = result.first()`

    If row is None: `raise HTTPException(status_code=404, detail="Promise not found")`

    Unpack: `promise, politician_name, politician_slug = row`

    Return: PromiseDetailOut constructed explicitly: id=promise.id, slug=promise.slug, title_hy=promise.title_hy, quote_hy=promise.quote_hy (NO truncation — full verbatim text), resolved_status=promise.resolved_status, made_date=promise.made_date, expected_date=promise.expected_date, source_url=promise.source_url, archived_url=promise.archived_url, politician_name_hy=politician_name, politician_slug=politician_slug.

    Update backend/app/main.py: Change import line from `from app.routers import elections, health, parties, politicians` to `from app.routers import elections, health, og, parties, politicians, promises, stats` (alphabetical order). Add after the existing `app.include_router(elections.router, prefix="/api")` line: `app.include_router(promises.router, prefix="/api")`, `app.include_router(stats.router, prefix="/api")`, `app.include_router(og.router, prefix="/api")`.

    NOTE: og.router is created in 03-05. If 03-05 has not executed yet when this plan runs, the import will fail. To handle parallelism: add the og import/include_router but accept that main.py will fail to start until og.py exists. Alternatively, create a stub backend/app/routers/og.py with just `from fastapi import APIRouter; router = APIRouter(prefix="/og", tags=["og"])` as a placeholder if og.py doesn't exist yet. The stub is sufficient to unblock the app start.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_promises.py tests/test_stats.py -x --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>GET /api/promises returns 200; GET /api/promises?status=kept returns only kept; GET /api/promises/nonexistent-slug returns 404 with "Promise not found"; GET /api/stats returns 200 with all 5 status keys. All test_promises.py and test_stats.py tests pass (GREEN).</done>
</task>

<task type="auto">
  <name>Task 2: usePromise hook + PromiseDetailPage + AboutPage</name>
  <files>frontend/src/hooks/usePromise.ts, frontend/src/pages/PromiseDetailPage.tsx, frontend/src/pages/AboutPage.tsx</files>
  <read_first>
    - frontend/src/hooks/usePolitician.ts — exact pattern to copy for usePromise (single entity, enabled guard)
    - frontend/src/pages/ElectionDetailPage.tsx — loading skeleton + 404/error state + detail content pattern
    - frontend/src/pages/PoliticianProfilePage.tsx — 404 state: "Хostumn chen gtнvel" heading + back link (lines 39-47)
    - .planning/phases/03-promise-browsing-homepage/03-PATTERNS.md — PromiseDetailPage content pattern section
    - .planning/phases/03-promise-browsing-homepage/03-UI-SPEC.md — PromiseDetailPage layout spec and Copywriting Contract
  </read_first>
  <action>
    Create frontend/src/hooks/usePromise.ts. Imports: useQuery from @tanstack/react-query; apiClient from @/api/client; PromiseDetailOut from @/types. Export function usePromise(slug: string): useQuery<PromiseDetailOut> with queryKey ["promise", slug]; queryFn: `() => apiClient.get<PromiseDetailOut>(`/api/promises/${slug}`)`; enabled: Boolean(slug). Destructure and return {data, isLoading, isError}.

    Create frontend/src/pages/PromiseDetailPage.tsx. Imports: Link + useParams from react-router-dom; Button from @/components/ui/button; Separator from @/components/ui/separator; ResolvedStatusBadge from @/components/ResolvedStatusBadge; usePromise from @/hooks/usePromise. Extract: `const { slug } = useParams<{ slug: string }>()`.

    Loading state (animate-pulse): main container with h-8 title skeleton, h-32 quote hero skeleton, 4 h-4 metadata row skeletons.

    Error/404 state (when isError || !data): H1 "Хостumny chи gtнvel" (use EXACT Armenian from UI-SPEC: "Хostumn chen gtнvel"), Link "Вeradardnal bolor xostumnernerd" to /promises (use EXACT Armenian copy from UI-SPEC 404 promise body link).

    Data state layout (per UI-SPEC PromiseDetailPage spec):
    - H1 title_hy: `<h1 className="text-2xl font-semibold text-zinc-900 mb-6">{data.title_hy}</h1>`
    - Quote hero block: `<blockquote className="bg-zinc-50 rounded px-6 py-6 text-[28px] font-normal text-zinc-700 leading-relaxed">{data.quote_hy}</blockquote>` — this is the FULL verbatim text, never truncated
    - `<Separator className="my-6" />`
    - Metadata section `<div className="space-y-3">`:
      - Status badge: `<div><ResolvedStatusBadge status={data.resolved_status} /></div>`
      - Politician row: `<div className="flex items-center gap-2"><span className="text-sm text-zinc-500">Кaghakakan gortsich</span><Link to={`/persons/${data.politician_slug}`} className="text-sm font-semibold text-zinc-900 underline">{data.politician_name_hy}</Link></div>` — use EXACT Armenian "Кaghakakan gortsich" from UI-SPEC Promise Detail Labels
      - made_date row: only when data.made_date != null: `<p className="text-sm text-zinc-500">Стеьzван: {data.made_date}</p>` — use EXACT Armenian "Стеьzван" from UI-SPEC
      - expected_date row: only when data.expected_date != null (D-07b): `<p className="text-sm text-zinc-500">Zhamket: {data.expected_date}</p>` — use EXACT Armenian "Zhamket" from UI-SPEC. OMIT ENTIRELY when null.
    - Source links `<div className="flex gap-3 mt-4">`:
      - `<a href={data.source_url} target="_blank" rel="noopener noreferrer"><Button variant="outline">Аghbyur</Button></a>` — use EXACT Armenian "Аghbyur" from UI-SPEC
      - Conditionally: `{data.archived_url && <a href={data.archived_url} target="_blank" rel="noopener noreferrer"><Button variant="ghost">Wayback Machine</Button></a>}`

    NOTE: Replace all transliterations above with the EXACT Armenian Unicode text from the UI-SPEC Copywriting Contract (Promise Detail Labels table).

    Create frontend/src/pages/AboutPage.tsx. Static page, no API calls (D-19, D-21). Imports: Separator from @/components/ui/separator. Structure:
    - `<main className="max-w-2xl mx-auto px-4 py-8">`
    - H1 "Мeр mасин" (use EXACT Armenian from UI-SPEC: "Мeр mасин")
    - Section 1: `<section className="mt-8"><h2 className="text-xl font-semibold text-zinc-900 mb-4">Ичhpes enk havakum xostumnernerd</h2>` (use EXACT Armenian from UI-SPEC: "Инhpes enk havakum xostumnernerd") followed by a paragraph with a TODO comment: `{/* TODO: Replace with content from "Our Principles" file in project root (D-20) */}<p className="text-zinc-700 leading-relaxed text-sm">Сайтums khetazотum enk khedayatvayi xostumnernerd...</p>`
    - `<Separator className="my-8" />`
    - Section 2: `<section><h2 className="text-xl font-semibold text-zinc-900 mb-4">Inchu stezgvets Хostumnernerd</h2>` (use EXACT Armenian from UI-SPEC: "Inchu stezgvets Хostumnernerd") followed by placeholder paragraph with TODO comment.
    NOTE: Use EXACT Armenian from UI-SPEC Copywriting Contract (About section headings). Placeholder body text is acceptable per D-20.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>usePromise.ts exports usePromise(slug) returning {data, isLoading, isError} with enabled guard. PromiseDetailPage: quote_hy shown in blockquote (no truncation); expected_date row absent when null; archived_url button absent when null; source link has target="_blank" rel="noopener noreferrer". AboutPage: static, no API imports, two sections with Separator. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Layout nav + App.tsx routes + HomePage recent promises</name>
  <files>frontend/src/components/Layout.tsx, frontend/src/App.tsx, frontend/src/pages/HomePage.tsx</files>
  <read_first>
    - frontend/src/components/Layout.tsx — current nav (5 links); append Мeр mасин as 6th
    - frontend/src/App.tsx — current routes; add 5 new routes
    - frontend/src/pages/HomePage.tsx — current file from 03-02 (has TODO placeholder for recent promises); update to wire usePromises
    - frontend/src/hooks/usePromises.ts — created in 03-03; import here for HomePage recent section
    - frontend/src/components/PromiseStub.tsx — component for recent promises (already handles /promises/:slug links, suitable for homepage)
  </read_first>
  <action>
    Update frontend/src/components/Layout.tsx. After the existing `<NavLink to="/elections"...>Ентрутиunnernerd</NavLink>` line, add one new NavLink: `<NavLink to="/about" className={navLinkClass}>Мeр mасин</NavLink>` (use EXACT Armenian from UI-SPEC: "Мeр mасин" = "Մeр масин"). This is the LAST nav link per D-18.

    Update frontend/src/App.tsx. Add 5 new imports at the top (alongside existing imports): PromisesListPage, FulfilledPage, UnfulfilledPage, PromiseDetailPage, AboutPage. Inside the Layout Route, add these routes (the `/` route already exists — replace its element from the old placeholder to the updated one if needed, or leave it since HomePage was already replaced in 03-02). Add: `<Route path="/promises" element={<PromisesListPage />} />`, `<Route path="/fulfilled" element={<FulfilledPage />} />`, `<Route path="/unfulfilled" element={<UnfulfilledPage />} />`, `<Route path="/promises/:slug" element={<PromiseDetailPage />} />`, `<Route path="/about" element={<AboutPage />} />`. Ensure `<Route path="*" element={<NotFoundPage />} />` remains last.

    Update frontend/src/pages/HomePage.tsx. Replace the TODO placeholder comment for recent promises with actual implementation: add import of usePromises from @/hooks/usePromises and PromiseStub from @/components/PromiseStub. Call `const { data: recentData, isLoading: recentLoading } = usePromises({ page: 1, perPage: 10 })`. In the recent promises section, replace the TODO div with: loading skeleton (10 pulse rows) when recentLoading, otherwise map recentData?.items with `<PromiseStub key={p.id} promise={p} />` (note: PromiseStub accepts PromiseStubOut but PromiseListOut has all the same fields, so it is compatible — both have id, slug, title_hy, quote_hy, resolved_status). Add a `<Link to="/promises" className="text-sm text-blue-600 underline mt-4 block">Бoлor xostumnernerd →</Link>` below the list (use EXACT Armenian "Бoлor xostumnernerd →" from UI-SPEC: "Бoлor xostumnernerd →").

    NOTE for all Armenian text: use the EXACT Unicode strings from the UI-SPEC Copywriting Contract, not transliterations.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Layout.tsx has 6 nav links with "Мeр mасин" last. App.tsx has /promises, /fulfilled, /unfulfilled, /promises/:slug, /about routes. HomePage recent promises section shows PromiseStub list with "see all" link. TypeScript compiles without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | slug path param crosses trust boundary in promise detail endpoint |
| Browser → external URLs | source_url and archived_url rendered as anchor hrefs; user navigates away |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Information Disclosure | GET /api/promises/{slug} | mitigate | moderation_status == ModerationStatus.approved in WHERE clause — non-approved promises return 404, not the content |
| T-03-09 | Tampering | source_url / archived_url in promise detail | accept | URLs from seed/moderated data; Phase 3 is read-only; open redirect risk is acceptable at this stage (no user-supplied URLs yet) |
| T-03-10 | Tampering | slug path param | mitigate | Parameterized ORM query (Promise.slug == slug); SQLAlchemy prevents SQL injection |
| T-03-11 | Spoofing | source links target="_blank" | mitigate | rel="noopener noreferrer" on all external links — prevents reverse tabnapping |
</threat_model>

<verification>
- `pytest backend/tests/test_promises.py backend/tests/test_stats.py -x` — all GREEN
- GET /api/promises/{valid-seed-slug} returns 200 with title_hy, quote_hy, politician_name_hy, politician_slug
- GET /api/promises/nonexistent returns 404 {"detail": "Promise not found"}
- `npx tsc --noEmit` in frontend/ passes
- App.tsx has routes: /, /promises, /fulfilled, /unfulfilled, /promises/:slug, /about
- Layout nav has 6 links; /about is last
- PromiseDetailPage: blockquote shows full quote_hy; Separator between quote and metadata; source button has rel="noopener noreferrer"
</verification>

<success_criteria>
- All test_promises.py and test_stats.py tests GREEN after main.py wiring
- PromiseDetailPage: conditional rendering — expected_date row absent when null, archived_url button absent when null
- all Phase 3 routes accessible from the browser
- Layout nav "Мeр mасин" link routes to /about
- HomePage recent promises section shows last 10 promises via usePromises({page:1, perPage:10})
</success_criteria>

<output>
Create `.planning/phases/03-promise-browsing-homepage/03-04-SUMMARY.md` when done
</output>
