---
phase: 03-promise-browsing-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/schemas/promises.py
  - backend/app/schemas/stats.py
  - backend/tests/test_promises.py
  - backend/tests/test_stats.py
  - backend/tests/test_og.py
  - frontend/src/types/index.ts
autonomous: true
requirements: [PROM-01, PROM-02, DISC-01, DISC-05]

must_haves:
  truths:
    - "PromiseListOut and PromiseDetailOut schemas exist as plain BaseModel (no from_attributes)"
    - "StatsOut and StatsByStatus schemas exist as plain BaseModel"
    - "All 9 test stubs in test_promises.py, test_stats.py, test_og.py are RED (skip-flagged, not missing)"
    - "Frontend PromiseListOut, PromiseDetailOut, StatsOut, StatsByStatus interfaces are exported from types/index.ts"
  artifacts:
    - path: "backend/app/schemas/promises.py"
      provides: "PromiseListOut + PromiseDetailOut schemas"
      contains: "class PromiseListOut"
    - path: "backend/app/schemas/stats.py"
      provides: "StatsOut + StatsByStatus schemas"
      contains: "class StatsOut"
    - path: "backend/tests/test_promises.py"
      provides: "Wave 0 RED test stubs for PROM-01, PROM-02"
      contains: "test_list_promises"
    - path: "backend/tests/test_stats.py"
      provides: "Wave 0 RED test stubs for DISC-01"
      contains: "test_get_stats"
    - path: "backend/tests/test_og.py"
      provides: "Wave 0 RED test stubs for DISC-05"
      contains: "test_og_promise"
    - path: "frontend/src/types/index.ts"
      provides: "PromiseListOut, PromiseDetailOut, StatsOut, StatsByStatus interfaces"
      contains: "PromiseListOut"
  key_links:
    - from: "backend/app/schemas/promises.py"
      to: "backend/app/routers/promises.py"
      via: "PromiseListOut + PromiseDetailOut imports in router"
      pattern: "from app\\.schemas\\.promises import PromiseListOut"
    - from: "backend/app/schemas/stats.py"
      to: "backend/app/routers/stats.py"
      via: "StatsOut import in router"
      pattern: "from app\\.schemas\\.stats import StatsOut"
---

<objective>
Wave 0 scaffolding: define all backend Pydantic schemas for the promises and stats domain, write RED test stubs, install shadcn input/separator components, and extend frontend types.

Purpose: Downstream plans (03-02, 03-03) implement against defined contracts without discovery. Test stubs enforce RED state — routers are not yet wired.
Output: 5 new/modified files establishing the full type contract for Phase 3.
</objective>

## Phase Goal

**As a** visitor, **I want to** browse promise lists, view promise details, and see homepage statistics, **so that** I can understand what promises Armenian politicians made and whether they kept them.

<execution_context>
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MesropTarkhanyan/PycharmProjects/khostumner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-promise-browsing-homepage/03-CONTEXT.md
@.planning/phases/03-promise-browsing-homepage/03-RESEARCH.md
@.planning/phases/03-promise-browsing-homepage/03-PATTERNS.md

<interfaces>
<!-- Existing schemas to extend (from backend/app/schemas/promises.py): -->
<!-- PromiseStubOut — stays unchanged; PromiseListOut and PromiseDetailOut are APPENDED -->

From backend/app/schemas/promises.py (current):
```python
class PromiseStubOut(BaseModel):
    model_config = ConfigDict(from_attributes=True)
    id: uuid.UUID
    slug: str
    title_hy: str
    quote_hy: str
    resolved_status: ResolvedStatus
```

From backend/app/models/promises.py (fields available):
- title_hy: str, quote_hy: str, slug: str, made_date: date|None,
- expected_date: date|None, source_url: str, archived_url: str|None
- moderation_status: ModerationStatus, resolved_status: ResolvedStatus
- politician_id: UUID, created_at: datetime

From frontend/src/types/index.ts (existing types to preserve):
- HealthResponse, PaginatedResponse<T>, PoliticianOut, PartyOut,
- PartyListItemOut, ElectionOut, ElectionWithCountOut, PromiseStubOut
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PromiseListOut + PromiseDetailOut to backend schemas</name>
  <files>backend/app/schemas/promises.py, backend/app/schemas/stats.py</files>
  <read_first>
    - backend/app/schemas/promises.py — current file to extend (DO NOT remove PromiseStubOut)
    - backend/app/schemas/elections.py — ElectionWithCountOut pattern: plain BaseModel no from_attributes
    - backend/app/models/promises.py — exact field types: made_date, expected_date (date|None), source_url (str), archived_url (str|None)
  </read_first>
  <action>
    Append two new classes to backend/app/schemas/promises.py. Add `from datetime import date` import at the top (alongside existing `import uuid`).

    PromiseListOut (plain BaseModel, NO model_config, NO from_attributes): fields id (uuid.UUID), slug (str), title_hy (str), quote_hy (str — pre-truncated by router to ~120 chars), resolved_status (ResolvedStatus), politician_name_hy (str), made_date (date | None). No politician_slug needed on list items.

    PromiseDetailOut (plain BaseModel, NO model_config, NO from_attributes): fields id (uuid.UUID), slug (str), title_hy (str), quote_hy (str — full verbatim), resolved_status (ResolvedStatus), made_date (date | None), expected_date (date | None), source_url (str), archived_url (str | None), politician_name_hy (str), politician_slug (str).

    Create backend/app/schemas/stats.py as a new file. Imports: from pydantic import BaseModel. Define StatsByStatus (plain BaseModel): fields kept (int), broken (int), in_progress (int), stalled (int), not_rated (int). Define StatsOut (plain BaseModel): fields total (int), by_status (StatsByStatus).

    CRITICAL: Neither PromiseListOut nor PromiseDetailOut may have `model_config = ConfigDict(from_attributes=True)` — they are constructed from JOIN Row tuples, not ORM objects (per codebase note 02-04 and RESEARCH.md Pitfall 1).
  </action>
  <verify>
    <automated>cd backend && python -c "from app.schemas.promises import PromiseListOut, PromiseDetailOut; from app.schemas.stats import StatsOut, StatsByStatus; print('OK')"</automated>
  </verify>
  <done>Both schemas importable; PromiseListOut has politician_name_hy field; PromiseDetailOut has politician_slug + archived_url fields; StatsOut has total and by_status fields; no from_attributes on any new schema.</done>
</task>

<task type="auto">
  <name>Task 2: Write RED test stubs for promises, stats, og</name>
  <files>backend/tests/test_promises.py, backend/tests/test_stats.py, backend/tests/test_og.py</files>
  <read_first>
    - backend/tests/test_elections.py — exact pytest-asyncio test structure to replicate
    - backend/tests/conftest.py — client fixture (reused unchanged; auto-discovered by pytest)
  </read_first>
  <action>
    Create backend/tests/test_promises.py. Module docstring: "Tests for PROM-01 (promise list) and PROM-02 (promise detail). RED state — promises router not yet wired in 03-01.". Import pytest. All tests use @pytest.mark.asyncio and the `client` fixture.

    Write these 6 test functions:
    - test_list_promises: GET /api/promises → assert 200; assert "items", "total", "page", "per_page", "pages" in body
    - test_list_promises_filter_status: GET /api/promises?status=kept → assert 200; all items have resolved_status == "kept"
    - test_list_promises_multi_status: GET /api/promises?status=broken,stalled → assert 200; all items have resolved_status in ("broken", "stalled")
    - test_list_promises_moderation_filter: GET /api/promises → assert 200; body["items"] is a list (moderation guard verified by integration with seed data — all items are approved)
    - test_get_promise_not_found: GET /api/promises/nonexistent-slug → assert 404; body["detail"] == "Promise not found"
    - test_get_promise: GET /api/promises/{slug} where slug is first item from list response (skip test if no items); assert 200; assert "title_hy", "quote_hy", "politician_name_hy", "politician_slug" in body

    Create backend/tests/test_stats.py. Module docstring: "Tests for DISC-01 (stats endpoint). RED state — stats router not yet wired in 03-01.". Write test_get_stats: GET /api/stats → assert 200; "total" in body; "by_status" in body; all of ("kept","broken","in_progress","stalled","not_rated") in body["by_status"].

    Create backend/tests/test_og.py. Module docstring: "Tests for DISC-05 (OG endpoint). RED state — og router not yet wired in 03-01.". Write test_og_promise_not_found: GET /api/og/promises/nonexistent-slug → assert 404. Write test_og_promise_returns_html: GET /api/og/promises/{slug} where slug is first from list endpoint (skip if no items); assert 200; assert "og:title" in response.text; assert "og:description" in response.text.

    At this wave, all tests will be RED (404 from FastAPI because routers are not yet included in main.py). That is correct and expected.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_promises.py tests/test_stats.py tests/test_og.py -x --tb=short 2>&1 | head -30</automated>
  </verify>
  <done>All 9 test functions exist. Tests fail with 404 (routers not wired yet) — RED state confirmed. No ImportError or SyntaxError.</done>
</task>

<task type="auto">
  <name>Task 3: Install shadcn input/separator + extend frontend types</name>
  <files>frontend/src/types/index.ts</files>
  <read_first>
    - frontend/src/types/index.ts — existing interfaces to preserve; append new ones at end
    - .planning/phases/03-promise-browsing-homepage/03-RESEARCH.md — Package Legitimacy Audit confirms input/separator are official registry
  </read_first>
  <action>
    Run the shadcn component installs (via Docker exec into the frontend container, or directly in frontend/ if running locally). Command: `npx shadcn@latest add input separator` inside the frontend directory. This installs frontend/src/components/ui/input.tsx and frontend/src/components/ui/separator.tsx.

    Append these four interfaces to the END of frontend/src/types/index.ts (after the existing PromiseStubOut interface). Do NOT remove or modify any existing interfaces:

    PromiseListOut: id (string), slug (string), title_hy (string), quote_hy (string — pre-truncated by backend), resolved_status ("not_rated" | "kept" | "broken" | "in_progress" | "stalled"), politician_name_hy (string), made_date (string | null).

    PromiseDetailOut: id (string), slug (string), title_hy (string), quote_hy (string — full text), resolved_status ("not_rated" | "kept" | "broken" | "in_progress" | "stalled"), made_date (string | null), expected_date (string | null), source_url (string), archived_url (string | null), politician_name_hy (string), politician_slug (string).

    StatsByStatus: kept (number), broken (number), in_progress (number), stalled (number), not_rated (number).

    StatsOut: total (number), by_status (StatsByStatus).

    All interfaces exported with the `export interface` keyword.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>frontend/src/components/ui/input.tsx and separator.tsx exist. types/index.ts exports PromiseListOut, PromiseDetailOut, StatsByStatus, StatsOut. TypeScript compiles with no new errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema layer | Pydantic validates all inbound query params; ORM results are typed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | PromiseListOut/PromiseDetailOut schemas | mitigate | No moderation_status field exposed in output schemas — moderation gate is applied in router WHERE clause, not exposed to client |
| T-03-SC | Tampering | npx shadcn@latest add input separator | accept | Both components confirmed as official shadcn registry (ui.shadcn.com/r/) in RESEARCH.md Package Legitimacy Audit — no third-party registry |
</threat_model>

<verification>
- `python -c "from app.schemas.promises import PromiseListOut, PromiseDetailOut"` succeeds in backend/
- `python -c "from app.schemas.stats import StatsOut, StatsByStatus"` succeeds in backend/
- `npx tsc --noEmit` in frontend/ passes with no new errors
- `pytest backend/tests/test_promises.py backend/tests/test_stats.py backend/tests/test_og.py` — all RED (404 responses from unregistered routers)
- `frontend/src/components/ui/input.tsx` exists
- `frontend/src/components/ui/separator.tsx` exists
</verification>

<success_criteria>
- PromiseListOut has politician_name_hy (str), no from_attributes
- PromiseDetailOut has politician_slug (str) + archived_url (str|None), no from_attributes
- StatsOut has total (int) + by_status (StatsByStatus) with all 5 status keys
- 9 test functions exist across 3 test files; all RED due to unregistered routers
- Frontend types index exports 4 new interfaces matching backend schema field names exactly
</success_criteria>

<output>
Create `.planning/phases/03-promise-browsing-homepage/03-01-SUMMARY.md` when done
</output>
